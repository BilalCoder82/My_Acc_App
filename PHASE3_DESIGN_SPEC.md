# Phase 3 — مواصفة التصميم التنفيذية
### الأرصدة الافتتاحية + محرك السندات العامة + Settlement متعدد الفواتير
**الحالة: مُعتمَدة (Bilal) — القرارات الأربعة محسومة. لا كود Voucher/Opening
Balance بعد؛ هذا المستند نفسه هو "القواعد التنفيذية واختبارات القبول"
المطلوبة قبل بدء 3B-1.**

---

## 0. القرار السابق (Phase 3A — جزء 1، مُنفَّذ ومُختبَر فعلاً)

`base_currency`: `registry.db` (`CompanyRecord.base_currency`) هو مصدر
الحقيقة الوحيد القابل للتعديل. عند كل `open_company_db()`، تُزامَن
القيمة تلقائياً إلى `Settings['base_currency']` بقاعدة الشركة كـcache
للقراءة فقط — تُستبدَل بقيمة `registry.db` الحقيقية في كل فتح، حتى لو
عُدِّلت يدوياً من داخل قاعدة الشركة نفسها. `get_base_currency(session)`
هي المدخل الوحيد المعتمد؛ ترفع خطأ واضحاً لو غابت، لا تفترض SYP أبداً.
مُختبَر فعلياً بـ`tests/test_base_currency_source_of_truth.py` (7
تحقّقات: شركة جديدة، تغيير لاحق بالـregistry ينعكس تلقائياً، قاعدة
يتيمة بلا سجل تُرفَض بوضوح، تلاعب يدوي مباشر بالـcache يُستبدَل تلقائياً).

**الأثر على البقية**: كل جدول أدناه فيه عمود "المعادل الأساسي" يعني
`amount × exchange_rate` محسوباً بعملة `get_base_currency()` الفعلية
لهذه الشركة، لا SYP مفترضة.

---

## 1. سند القبض / سند الدفع — محرك عام موحَّد

**قرار**: لا شاشتان منفصلتان "سند عميل" و"سند مصروف". شاشة واحدة لكل
اتجاه (قبض/دفع)، فوق نموذج بيانات مشترك.

### جدول جديد: `vouchers`
| العمود | النوع | ملاحظة |
|---|---|---|
| `id` | PK | |
| `voucher_no` | str | يُولَّد عند الترحيل (نفس فجوة §54 بالفواتير — سيُحسَم بنفس القرار، لا افتراض هنا) |
| `kind` | enum | `RECEIPT` \| `PAYMENT` |
| `date` | date | |
| `cash_account_id` | FK→accounts | حساب الصندوق/البنك (الطرف الثابت لكل سطور السند) |
| `currency_code` | str(3) | عملة السند نفسه |
| `exchange_rate` | numeric | سعر صرف السند وقت الترحيل |
| `status` | enum | `DRAFT` \| `POSTED` \| `CANCELLED` |
| `journal_entry_id` | FK nullable | |
| `description` | str | |

### جدول جديد: `voucher_lines`
| العمود | النوع | ملاحظة |
|---|---|---|
| `id` | PK | |
| `voucher_id` | FK | |
| `account_id` | FK→accounts | **أي حساب** — عميل/مورد/مصروف/إيراد/آخر (لا قيد على `account_type`) |
| `amount_foreign` | numeric | بعملة **السند** (لا الفاتورة — الفرق جوهري، راجع §4 أدناه) |
| `amount_base` | numeric | `amount_foreign × voucher.exchange_rate` |
| `description` | str | |

**القيد المحاسبي**: `_jline`/`_jline_base` الحاليتان بالضبط (لا تعديل
على محرك الترحيل الأساسي) — سطر لكل `voucher_line` + سطر مقابل واحد
للصندوق/البنك بمجموع كل الأسطر.

**Settlement Dialog يظهر فقط عندما**:
```python
account.subtype in (AccountSubtype.CUSTOMER, AccountSubtype.SUPPLIER)
and account.allow_reconciliation
```
نفس الشرطين المُثبَتين فعلياً بـ§57 — لا فحص إضافي، لا تكرار منطق.
لأي حساب آخر (Expense/Income/General/Cash/Bank): يُرحَّل السطر مباشرة
بلا أي واجهة تسوية، بالضبط كسند القيد اليدوي الحالي.

---

## 2. Settlement Ledger هو مصدر الحقيقة — لا `paid_amount` مخزَّن

**قرار محسوم (طلب Bilal الصريح)**: لا يُضاف أي عمود `paid_amount` أو
`balance_due` على `Invoice`. **لا شيء يتغيّر هنا فعلياً** — هذا يطابق
تماماً ما بُني بالفعل في §42.1: `get_invoice_balance_due()` تحسب
ديناميكياً من `Settlement` في كل استدعاء، ولا تخزين مطلقاً. Phase 3
تُوسِّع نفس المبدأ فقط:

### جدول جديد: `settlement_allocations`
بدل ربط `Settlement` بفاتورة واحدة مباشرة (كما هو الآن)، تسوية واحدة
من سند واحد يمكن أن تُوزَّع على عدة فواتير:

| العمود | النوع |
|---|---|
| `id` | PK |
| `voucher_id` | FK→vouchers |
| `invoice_id` | FK→invoices |
| `amount_foreign` | numeric — بعملة **الفاتورة** (لا السند) |
| `amount_base` | numeric — بسعر صرف **الفاتورة الأصلي** (القيمة التاريخية، لا تُعاد كتابتها أبداً) |
| `settlement_rate` | numeric — سعر الصرف الفعلي وقت هذه التسوية تحديداً |
| `fx_amount` | numeric — الفرق المحقَّق لهذا التوزيع تحديداً |

`get_invoice_balance_due(invoice)` تُصبح:
```python
sum(alloc.amount_foreign for alloc in invoice.settlement_allocations)
```
بدل الاستعلام المباشر على `Settlement` كما الآن — **الجوهر لا يتغيّر
إطلاقاً**، فقط يدعم الآن "سند واحد → عدة فواتير" بدل "سند واحد ↔
فاتورة واحدة" الحالية.

**Cache للأداء (اختياري، مؤجَّل)**: لو استدعاء `get_invoice_balance_due`
أصبح بطيئاً فعلياً على فواتير بمئات التسويات (لم يُثبَت هذا بعد)، يمكن
إضافة عمود `_cached_balance_due` **يُعاد حسابه دائماً من
`settlement_allocations` عند كل قراءة أو يُبطَل صراحة عند أي تعديل**،
لا مصدر حقيقة بديل أبداً. غير مطلوب الآن — لا دليل على مشكلة أداء
فعلية بحجم بيانات Bilal الحالي.

---

## 3. Settlement Dialog — تصميم متعدد الفواتير

```
سند القبض/الدفع
     │ (سطر بحساب Customer/Supplier + allow_reconciliation=True)
     ▼
[تسوية الفواتير]
     ▼
┌─────────────────────────────────────────┐
│ الفواتير المفتوحة لهذا الحساب            │
│ رقم | تاريخ | العملة | الإجمالي | المتبقي │
│ ... (فلترة: POSTED فقط، غير CANCELLED،    │
│      balance_due > 0 فقط)                │
│                                           │
│ ○ FIFO          ○ توزيع يدوي              │
│                                           │
│ [لكل فاتورة، عمود "المخصَّص" قابل للتعديل │
│  فقط إن كان التوزيع يدوياً]               │
│                                           │
│ مبلغ السند: 1,000  |  المخصَّص: 800  |  غير مخصَّص: 200 │
└─────────────────────────────────────────┘
```

### قواعد المنع — بالخدمة حصراً، الواجهة تعرض النتيجة فقط
كل هذه تُفحَص داخل خدمة جديدة `app/services/allocations.py`، لا الـUI:

1. `sum(allocations) <= voucher.amount_foreign` — لا تجاوز مبلغ السند
2. لكل فاتورة: `allocation.amount <= invoice.balance_due` — لا تجاوز رصيدها
3. الفاتورة `status == POSTED` فقط — لا DRAFT، لا CANCELLED
4. `invoice.balance_due > 0` فقط — لا فاتورة مُسدَّدة بالكامل
5. الفاتورة يجب أن تخص **نفس حساب** سطر السند تحديداً (لا خلط بين
   حسابي عميلين لمجرد تشابه الاسم)
6. مبلغ سالب مرفوض (تحقق نوع بسيط، لا Business Rule معقّدة)

### FIFO — خوارزمية محددة
```python
def allocate_fifo(voucher_amount: Decimal, open_invoices: list[Invoice]) -> list[Allocation]:
    open_invoices.sort(key=lambda inv: inv.invoice_date)  # الأقدم أولاً
    remaining = voucher_amount
    allocations = []
    for inv in open_invoices:
        if remaining <= 0:
            break
        take = min(remaining, get_invoice_balance_due(inv))
        if take > 0:
            allocations.append(Allocation(inv, take))
            remaining -= take
    return allocations  # remaining المتبقي = غير مخصَّص (راجع §5)
```
الترتيب بـ`invoice_date` (لا `id`) — يطابق تعريف Bilal الحرفي
("فاتورة 1، فاتورة 2، فاتورة 3" بترتيب صدورها).

### توزيع يدوي
المستخدم يُدخل مبلغاً لكل فاتورة بالواجهة؛ عند التأكيد، القواعد
الستة أعلاه تُفحَص لكل سطر بالخدمة قبل أي ترحيل — رفض السطر الكامل لو
أي مخصَّص واحد غير صالح (لا ترحيل جزئي لتوزيع فيه خطأ واحد).

---

## 4. العملات — القرار المعتمد (Bilal): العملة الأساسية جسر التحويل الوحيد

**معتمد رسمياً — لا Cross Rate مصطنع أبداً.** لا يُخزَّن ولا يُحسَب أي
سعر تحويل مباشر بين عملتين أجنبيتين (مثل EUR→USD مباشرة) في أي مكان
بالنظام — كل تحويل يمر حصراً عبر `get_base_currency()` الفعلية لهذه
الشركة (وليست بالضرورة SYP — قد تكون SEK كما بمثال Bilal، أو أي عملة
أخرى، §58 يضمن هذا).

### الصيغة الدقيقة (قاعدة تنفيذية، لا اقتراح)
```
invoice_base_equivalent  = allocation.amount_foreign × invoice.exchange_rate   (تاريخي، ثابت للأبد)
voucher_base_equivalent  = المبلغ المكافئ من السند بعملته × voucher.exchange_rate

fx_amount (بالعملة الأساسية) = voucher_base_equivalent − invoice_base_equivalent
```
`allocation.amount_foreign` يُدخَل ويُخزَّن **بعملة الفاتورة دائماً**
(لا بعملة السند) — هذا ما يجعل الرصيد المتبقي للفاتورة (`balance_due`)
محسوباً بعملتها الأصلية دون أي تحويل إضافي. الجزء المقابل من مبلغ
السند (بعملته هو) يُشتَق حصراً بقسمة `invoice_base_equivalent` على
`voucher.exchange_rate` — أي أن الانتقال الوحيد بين عملة الفاتورة
وعملة السند هو: **عملة الفاتورة → العملة الأساسية → عملة السند**،
بخطوتين منفصلتين تماماً، لا خطوة واحدة مباشرة.

### الحالتان الرسميتان لقبول Bilal (اختبارا قبول إلزاميان، §10 أدناه)
1. **Base=USD، Invoice=EUR، Voucher=EUR** (نفس عملة الفاتورة والسند) —
   الحالة الأبسط: `voucher_base_equivalent` محسوبة مباشرة من مبلغ
   السند نفسه، الفرق ينتج فقط من اختلاف `exchange_rate` بين تاريخ
   الفاتورة وتاريخ التسوية (نفس ما أثبته `test_currency_lifecycle_final.py`
   أصلاً بالضبط — لا تغيير مطلوب هنا سوى استبدال "فاتورة واحدة↔تسوية
   واحدة" بـ"تخصيص من جدول `settlement_allocations`").
2. **Base=USD، Invoice=EUR، Voucher=GBP** (عملتان أجنبيتان مختلفتان
   تماماً) — الحالة الحرجة: يجب أن يمر الحساب بخطوتين منفصلتين
   (EUR→USD عبر `invoice.exchange_rate`، وGBP→USD عبر
   `voucher.exchange_rate` **بشكل مستقل تماماً**، لا معادلة واحدة
   تدمجهما). **هذه الحالة غير مدعومة إطلاقاً إلا إذا مرّت هذه الصيغة
   بالضبط** — لا نسخة مبسَّطة لها.

### حالات الربح/الخسارة (كلتاهما، لا حالة واحدة)
- **ربح محقَّق**: `voucher_base_equivalent > invoice_base_equivalent`
  (العملة الأساسية اشترت بالفاتورة أقل مما اشترت بالسند وقت التسوية) → `fx_gain`
- **خسارة محقَّقة**: العكس → `fx_loss`
- كلتا الحالتين ستُختبَران صراحة (يطابق ما أثبته
  `test_currency_lifecycle_final.py` أصلاً — كلا الاتجاهين ظهرا
  طبيعياً بأسعار صرف عشوائية أعلى/أقل).

---

## 5. الدفعة غير المخصَّصة (On-Account Payment)

من خوارزمية FIFO أعلاه، `remaining > 0` بعد تغطية كل الفواتير
المفتوحة = مبلغ غير مخصَّص. **لا يُجبَر المستخدم على اختراع تسوية
وهمية**. الحل:

`voucher_lines` كسطر عادي بحساب Customer/Supplier، لكن **بلا أي
`settlement_allocations` مرتبطة به** — القيد المحاسبي يُرحَّل عادياً
(مدين/دائن حساب العميل)، فيظهر بكشف حسابه كرصيد دائن (أو مدين للمورد)،
لكنه غير مرتبط بأي فاتورة محددة. يبقى **متاحاً للتخصيص لاحقاً** عبر
سند تسوية مستقبلي (Settlement Dialog يعرض أيضاً "دفعات غير مخصَّصة
سابقة لهذا الحساب" كخيار تخصيص، لا الفواتير المفتوحة فقط).

لا جدول جديد لهذا — هو ببساطة `voucher_line` بلا `allocations`، حالة
طبيعية للنموذج أعلاه، لا استثناء يحتاج بنية خاصة.

---

## 6. حساب التوازن الافتتاحي — مُعتمَد كإعداد شركة، لا اختراعاً تلقائياً

**قرار Bilal الصريح**: لا يخترع النظام اسماً أو حساباً افتراضياً من
تلقاء نفسه. حساب `Opening Balance Clearing` يُحدَّده المستخدم/النظام
ضمن **إعدادات الشركة** — مفتاح `Settings` جديد:
`opening_balance_clearing_account_id`، إلزامي (يُطلَب صراحة أول مرة
تُفتَح فيها شاشة الأرصدة الافتتاحية، لا افتراضي مخفي — نفس فلسفة
`base_currency` بالضبط). **يجب أن يكون نوعه `EQUITY`** (ليس Revenue
ولا Expense) — فصل مفاهيمي إلزامي بين رصيد الافتتاح وأرباح/مصاريف
الفترة الحالية، بالضبط كما شدَّد Bilal، حتى لا يُشوَّه قياس الربحية.

**حساب افتتاح المخزون منفصل مفاهيمياً لكنه نفس الآلية**: لا "حساب
مخزون افتتاحي" مستقل — القيمة تدخل مباشرة إلى **حساب المخزون الحقيقي
لكل مادة** (`item.inventory_account_id`، كما هو بالضبط بأي حركة شراء
عادية)، والطرف المقابل هو **نفس** `opening_balance_clearing_account_id`
أعلاه — لا حساب ثالث منفصل. هذا يحافظ على مبدأ Bilal ("لا حساب
افتراضي يخترعه النظام") بدون مضاعفة الإعدادات المطلوبة من المستخدم.

### جدول جديد: `opening_balance_entries`
| العمود | النوع |
|---|---|
| `id` | PK |
| `account_id` | FK→accounts |
| `currency_code` | str(3) |
| `debit_foreign` \| `credit_foreign` | numeric (أحدهما فقط >0) |
| `exchange_rate` | numeric |
| `base_equivalent` | numeric (`amount × rate`، مُخزَّن لأنه **تاريخي إلزامي** — لا يُعاد حسابه لاحقاً بسعر جديد، نفس مبدأ الفاتورة) |
| `opening_date` | date |

**القيد الافتتاحي المتوازن**: خدمة `post_opening_balances(session,
entries: list)` تبني **قيداً واحداً** (لا قيد منفصل لكل حساب) يحتوي
سطراً لكل `opening_balance_entries` + **سطر توازن تلقائي واحد** على
`opening_balance_clearing_account_id` المُعتمَد بإعدادات الشركة. الخدمة
تعرض الفرق **صراحة بالواجهة قبل الترحيل** ليتأكد المستخدم أنه منطقي
(فرق كبير غير متوقَّع = خطأ إدخال محتمل، لا خطأ برمجي).

---

## 7. الأرصدة الافتتاحية للعملاء/الموردين — فاتورة حقيقية بقيود صارمة

**قرار Bilal**: إعادة استخدام `Invoice` (لا جدول `OpeningInvoice`
منفصل)، بحقل `is_opening_balance: bool` — **لكن بشرط تحقيق كل هذه
القيود الستة معاً، لا الحقل وحده**:

| # | القيد | آلية الفرض |
|---|---|---|
| 1 | فاتورة حقيقية من ناحية الـsubledger | `Invoice` عادية، `status=POSTED` مباشرة عند الإدخال |
| 2 | مرتبطة بحساب العميل/المورد | `journal_entry.lines[0].account_id` = حساب الطرف الفعلي (كأي فاتورة عادية، عبر `get_or_create_party_account`) |
| 3 | قابلة للتسوية كأي فاتورة أخرى | **لا تعديل على `get_invoice_balance_due`/`post_receipt`/`post_payment`/FIFO** — تعمل عليها تلقائياً بلا أي كود إضافي، لأنها `Invoice` عادية بمنظور هذه الدوال بالكامل |
| 4 | لا تُنشئ إيراداً/مصروفاً جديداً | مسار ترحيل **منفصل تماماً**: `post_opening_invoice()` جديدة — **لا تستدعي `post_sales_invoice`/`post_purchase_invoice` إطلاقاً** |
| 5 | مرتبطة بقيد الافتتاح المقابل | القيد الناتج: `Dr/Cr Customer أو Supplier` مقابل `Dr/Cr opening_balance_clearing_account_id` مباشرة — سطران فقط، لا تعقيد إضافي |
| 6 | لا تدخل دورة posting العادية (لا تكرار إيراد/تكلفة) | `post_opening_invoice()` **لا تلمس** `InvoiceLine`/`InventoryMovement`/حسابات المبيعات أو COGS إطلاقاً — الفاتورة الافتتاحية تحمل مبلغاً إجمالياً واحداً فقط (لا بنود أصناف حقيقية)، بعكس الفاتورة العادية |

**الفرق الجوهري عن فاتورة مبيعات/شراء عادية**: فاتورة عادية تمر عبر
`post_sales_invoice()` (تُنشئ: قيد إيراد + قيد COGS + حركة مخزون).
فاتورة افتتاحية تمر عبر `post_opening_invoice()` منفصلة تماماً (تُنشئ:
قيد واحد بسيط `Dr Customer / Cr Opening Balance Clearing` فقط) — **لا
مسار مشترك بينهما إطلاقاً**، رغم كونهما نفس جدول `Invoice`. هذا يحقق
مطلب Bilal حرفياً: "قابلة للتسوية مثل أي فاتورة أخرى" (لأن `Invoice`
نفس البنية) **و** "لا تُنشئ إيراداً/لا تدخل دورة posting العادية"
(لأن دالة الترحيل منفصلة تماماً، لا استدعاء متبادل).

**لا خيار "رصيد إجمالي غير مفصَّل" منفصل بنية خاصة** — لو أراد
المستخدم عدم تتبع فواتير قديمة تفصيلياً، يستخدم آلية §5 (دفعة غير
مخصَّصة) بدلاً من فاتورة افتتاحية، بلا حاجة لمسار ثالث.

---

## 8. الأرصدة الافتتاحية للمخزون — لكل مستودع، تدخل محرك التكلفة فعلياً

### جدول جديد: `opening_inventory_entries`
| العمود | النوع |
|---|---|
| `item_id` | FK |
| `warehouse_id` | FK — **إلزامي، لا None** (يطابق §46: التكلفة منفصلة لكل مستودع) |
| `quantity` | numeric |
| `unit_cost_foreign` | numeric |
| `currency_code` | str(3) |
| `exchange_rate` | numeric |
| `unit_cost_base` | numeric محسوب ومُخزَّن (تاريخي) |

**الأهم (طلب Bilal الحرفي: "لا نريد إدخال الرصيد بجدول منفصل ثم جعل
`_average_cost()` يتجاهله")**: خدمة `post_opening_inventory()` تُنشئ
**`InventoryMovement` حقيقية** بنوع `source_type="opening_balance"،
`direction="IN"` بالضبط كحركة شراء عادية — **لا تعديل على
`_average_cost()` أو `get_item_stock_summary()` إطلاقاً**، لأن هذه
الدوال أصلاً تحسب من `InventoryMovement` مباشرة، وحركة الافتتاح
تصبح ببساطة أول حركة IN بتاريخها الطبيعي. البيع التالي يستخدم هذا
المتوسط تلقائياً بلا أي كود إضافي — يطابق تماماً سيناريو الاختبار
المطلوب (Opening→Sale→COGS→Purchase→New Average). الطرف المقابل
لهذه الحركة بالقيد المحاسبي: `Dr item.inventory_account_id / Cr
opening_balance_clearing_account_id` (راجع §6).

---

## 9. Idempotency — منع الترحيل المزدوج للأرصدة الافتتاحية

**متطلب صريح من Bilal، ليس تفصيلاً ثانوياً**: الضغط على "ترحيل
الأرصدة الافتتاحية" مرتين يجب ألا يُضاعِف أي رقم (100 وحدة لا تصبح
200، 10,000 AR لا تصبح 20,000).

**القرار**: **رفض صريح، لا idempotency صامتة عبر upsert**. السبب: لا
معنى تجاري لـ"إعادة ترحيل" رصيد افتتاحي — إما أنه صحيح فيبقى كما هو،
أو خاطئ فيحتاج **تصحيحاً** (قيد عكسي + قيد جديد صحيح)، لا "ترحيلاً
ثانياً فوق الأول". آلية الفرض:

- `Setting` جديد لكل نطاق: `opening_balances_accounts_posted_at`،
  `opening_inventory_posted_at`، `opening_customers_posted_at`،
  `opening_suppliers_posted_at` (تواريخ/طوابع زمنية، لا Boolean بسيط
  — تفيد لاحقاً بمعرفة متى تم الترحيل فعلياً).
- كل من دوال §6/7/8 تتحقق من الـSetting المقابل لها **أولاً**، وترفض
  فوراً برسالة واضحة ("الأرصدة الافتتاحية للحسابات مُرحَّلة مسبقاً
  بتاريخ كذا — للتصحيح استخدم Cancel/Reverse على القيد رقم كذا ثم
  أعد الإدخال") لو كانت موجودة مسبقاً — **لا صمت، لا استبدال تلقائي**.
- **مسار التصحيح**: نفس `cancel_invoice()`/عكس القيد اليدوي الموجودَين
  فعلاً بالمحرك (§44) — لا آلية جديدة. عكس القيد الافتتاحي يمسح
  الـSetting المقابل تلقائياً، فيسمح بإعادة الإدخال بعده مباشرة.

هذا يُطبَّق على الثلاثة (حسابات/مخزون/عملاء-موردين) بشكل مستقل — كل
واحد له Setting خاص به، يمكن ترحيل المخزون دون الحسابات مثلاً، لا
عملية "ترحيل شامل واحد" تُقفَل كلها معاً.

---

## 10. اختبارات القبول الإلزامية (Phase 3B) — بالأرقام الدقيقة

هذه هي معايير القبول الفعلية؛ أي تنفيذ لا يجتازها بالضبط بهذه الأرقام
غير مقبول، بصرف النظر عن نجاح أي اختبار آخر:

### أ) جسر العملة (§4) — حالتان إلزاميتان معاً
1. **Base=USD, Invoice=EUR@10, Voucher=EUR@12**: فاتورة 1,000 EUR
   (=10,000 USD تاريخياً)، تسوية 1,000 EUR كاملة بسعر 12
   (=12,000 USD) → `fx_gain = 2,000 USD` بالضبط، الرصيد = 0 EUR بالضبط.
2. **Base=USD, Invoice=EUR@10, Voucher=GBP@?**: فاتورة 1,000 EUR
   (=10,000 USD تاريخياً)، سند 800 GBP بسعر GBP→USD=12.5
   (=10,000 USD بالضبط) يُخصَّص بالكامل للفاتورة → `fx_amount = 0`
   بالضبط (المعادلان بالعملة الأساسية متطابقان تماماً رغم عدم وجود
   أي علاقة مباشرة EUR↔GBP بالنظام) — **هذا الاختبار تحديداً يُثبِت
   أن الجسر عبر العملة الأساسية يعمل بلا أي Cross Rate مصطنع**.

### ب) دورة العميل الافتتاحي الكاملة (§7)
`Opening Invoice (5,000 USD) → POSTED مباشرة → تظهر في get_account_statement()
لحساب AR → تظهر في get_invoice_balance_due() = 5,000 → Receipt 3,000
→ Settlement → balance_due = 2,000 بالضبط → Receipt 2,000 → balance_due
= 0 بالضبط`. تحقق إضافي إلزامي: **لا حساب إيراد واحد تأثر** (استعلام
مباشر على حساب `sales`/`4101` يثبت رصيده = 0 من هذه العملية بالكامل).

### ج) المخزون الافتتاحي (§8)
`Warehouse A: Opening Qty=100 @ 5 USD → Sale Qty=20 → COGS = 20×5 =
100 USD بالضبط (Oracle مستقل، لا "current purchase price" أي سعر
شراء لاحق لو وُجد)`. تحقق إضافي: نفس اختبار §55 السابق (شراء لاحق
بمستودع A لا يمسّ B) يُعاد بنفس الصيغة لكن بنقطة بداية من رصيد
افتتاحي، لا شراء عادي.

### د) Idempotency (§9)
محاولة ترحيل نفس دفعة الأرصدة الافتتاحية للحسابات مرتين → المحاولة
الثانية تُرفَض بوضوح، `100` وحدة تبقى `100` (لا `200`)، `10,000 AR`
يبقى `10,000` (لا `20,000`) — تحقق مباشر على `InventoryMovement.count()`
و`JournalLine` قبل وبعد المحاولة المرفوضة، لا افتراضاً.

---

## 11. ترتيب Phase 3B — مُعتمَد (Bilal)

```
3B-1  Opening Account Balances    (§6)
        ↓
3B-2  Opening Inventory           (§8)
        ↓
3B-3  Opening Customer/Supplier   (§7)
        ↓
3B-4  General Receipt/Payment Voucher Engine   (§1)
        ↓
3B-5  Settlement Allocation (FIFO + Manual)    (§2, §3)
        ↓
3B-6  UI (شاشات الأرصدة الافتتاحية + السند + Settlement Dialog)
        ↓
3B-7  Full regression + fuzz
```

كل خطوة تُبنى ثم تُختبَر فوراً (اختبارات §10 المطابقة لها) قبل
الانتقال للتالية — لا بناء الثلاث الأولى دفعة واحدة ثم اختبارها معاً.
3B-4/3B-5 (السندات والتخصيص) لا تبدأ قبل نجاح 3B-1/2/3 بالكامل، لأن
Settlement Dialog (3B-5) يحتاج فواتير افتتاحية حقيقية (3B-3) لاختبار
FIFO عليها بمعزل عن الفواتير العادية.

---

## القرارات المعتمدة نهائياً (Bilal) — لا أسئلة مفتوحة متبقية

1. **جسر العملة**: العملة الأساسية فقط، بخطوتين منفصلتين دائماً — لا Cross Rate مصطنع (§4).
2. **الفاتورة الافتتاحية**: `Invoice` عادية + `is_opening_balance` + 6 قيود صارمة، مسار ترحيل منفصل تماماً `post_opening_invoice()` (§7).
3. **حساب التوازن**: `Settings['opening_balance_clearing_account_id']` — إعداد شركة إلزامي، نوعه EQUITY، لا اختراع تلقائي (§6).
4. **ترتيب 3B**: حسابات → مخزون → عملاء/موردون → سندات → تخصيص → واجهة → E2E (§11).

**الخطوة التالية**: تنفيذ 3B-1 (Opening Account Balances) فقط —
النموذج + الهجرة + الخدمة + اختبارات §10-أ الجزء المتعلق بها، ثم
توقف وتقرير قبل 3B-2، مطابقةً تماماً لانضباط "ابنِ ثم اختبر ثم توقف"
الذي أثبت نجاحه بكل المراحل السابقة.
