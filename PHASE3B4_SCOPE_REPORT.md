# PHASE3B4_SCOPE_REPORT
**تعريف نطاق فقط، بناءً على PHASE3B4_SCOPE_DECISION. لا Architecture، لا Design Spec، لا كود، لا Migration، لا UI. `returns.py`/`invoice_no` لم يُلمَسا.**

---

## 1. Scope Included

- **Accounting Validation/Posting Boundary موحَّدة** تُغطي: إنشاء `JournalEntry` مركزياً، إنشاء/إضافة `JournalLine` مركزياً، التحقق من (حساب موجود، نشط، ليس Group، حصرية مدين/دائن، لا سطر فارغ، وجود أسطر، توازن)، قواعد `currency`/`rate`/`base amount` المشتركة.
- **دعم صريح لنمطين معاً بنفس النواة**: `DRAFT→POSTED` (يدوي) و`build→validate→POSTED` فوري (آلي) — بلا إجبار أي مسار على تبنّي نمط الآخر.
- **توحيد توليد `JournalEntry.ref_no`** للقيود التي تحتاجه — إزالة النسخ الست الحالية، مع فصل واضح بين عدّاد القيود العادية وعدّاد قيود العكس (منع `JV-REV-%` من التسرّب لعدّاد `JV-%`).
- **`is_group`/`is_active`**: تحقق إلزامي **داخل** الـBoundary نفسها (Python-level، لا UI-only)، يُطبَّق على أي مسار يمر عبرها — يدوياً كان أم آلياً.
- **General Reversal primitive** قابل لإعادة الاستخدام حيث ينطبق (يبقى `reverse_manual_entry` أو ما يعادلها بعد أي تنظيف تسموي — لا منطق جديد، فقط إتاحته لمسارات أخرى إن احتاجت عكساً عاماً غير مرتبط بقواعد فاتورة خاصة).
- **جعل المسارات الآلية تمر عبر هذه الـBoundary** بدل البناء المباشر لـ`entry.lines`/`JournalLine(...)` بمعزل عنها.
- **حسم تسموي واحد فقط**: هل يبقى `journal_edit.py` هو الملف (بعد توسيع محدود) أم يُعاد تسميته — إجابة أدناه (§3).

## 2. Scope Excluded (صراحة، بلا غموض)

- `returns.py` — لا لمس بهذه المرحلة، Technical Debt §69 كما هو.
- `Invoice.invoice_no` — Document Number، مؤجَّل لمرحلة Numbering مستقلة بالكامل (التقرير حسم أنه لا علاقة له بالهوية المحاسبية).
- `invoice_cancel.py`'s Business Rules الخاصة (حالة الفاتورة، فحص التسويات، عكس حركات المخزون) — تبقى مسؤوليتها بالكامل، لا تُنقَل للنواة.
- `Settlement`/`SettlementAllocation` semantics — Append-Only كما اعتمد بـ3B-3، **لا Reversal يُضاف لها بأي شكل ضمن 3B-4**.
- دمج `Cancellation` مع `Reversal` العام — يبقيان مفهومين منفصلين.
- زر Reverse بواجهة سند القيد — مؤجَّل لـ3B-6 UI.
- أي Business Domain logic (فواتير، مخزون، عملاء/موردون) — تبقى بالكامل خارج النواة؛ النواة تستقبل "نية محاسبية" (حسابات + مبالغ) جاهزة، لا تفهم مصدرها.
- إنشاء أي Model/جدول جديد (Sequence/Voucher/إلخ) — قرار Architecture لاحق، ليس Scope.

## 3. Existing Core to Reuse — الإجابة المطلوبة، بدليل من الكود

**التوصية المبنية على دليل**: **إعادة استخدام منطق `journal_edit.py` الحالي بالكامل (لا إعادة كتابة)، لكن مع إعادة تسمية الملف والدوال ذات البادئة `manual_` — الدليل يُحتِّم ذلك، لا مجرد تفضيل تسموي:**

1. **docstring الملف نفسه يُقيِّده صراحة بنطاق ضيق**: *"Journal Entry Edit Rules — سند القيد اليدوي... سند القيد اليدوي (بعكس قيود الفواتير الآلية) يُنشأ DRAFT ابتداءً"* — هذا النص **يفترض صراحة** أن كل ما يمر بهذا الملف هو DRAFT-first ويدوي، وهو بالضبط الافتراض الذي القرار رقم 1 يرفضه الآن (النواة يجب أن تخدم النمطين). إبقاء هذا النص/الاسم بعد أن يصبح الملف مسؤولاً أيضاً عن `posting.py`/`settlements.py`/إلخ **يُضلِّل أي قارئ مستقبلي للكود** — هذا دليل وظيفي (functional)، لا تجميلي.
2. **أسماء الدوال نفسها**: `add_manual_line`، `remove_manual_line`، `post_manual_entry`، `reverse_manual_entry` — كلمة "manual" **جزء من التوقيع العام (public API)**. لو استدعتها `posting.py` (مسار آلي بالكامل)، فالكود يقرأ حرفياً `add_manual_line(...)` داخل دالة تنشئ فاتورة آلية — تناقض تسموي مباشر، لا افتراض.
3. **الفجوة الوظيفية الحقيقية المؤكَّدة سابقاً (Post-Archaeology §B/§M)**: لا دالة "إنشاء `JournalEntry` فارغ" موجودة أصلاً بالملف — كل مُستدعٍ (بما فيها الواجهة اليدوية نفسها) يبني الكائن مباشرة. أي توسيع فعلي يحتاج **دالة جديدة كاملة** لهذا الغرض — هذه ليست "تنظيفاً محدوداً"، هي قدرة غائبة يجب إضافتها بغض النظر عن اسم الملف.
4. **بالمقابل، لا دليل يستدعي إعادة كتابة المنطق الداخلي**: `_validate_lines`، `ensure_editable`، `post_manual_entry` (الجوهر، بمعزل عن الاسم)، `reverse_manual_entry` — كلها منطق سليم، مُختبَر (ولو بشكل غير مباشر)، لا يحتاج تغيير خوارزمي، فقط: (أ) تعميم الاسم، (ب) إضافة تحقق `is_group`/`is_active`، (ج) إضافة دالة إنشاء `JournalEntry`، (د) توحيد الترقيم.

**الخلاصة**: **إعادة تسمية + توسيع محدود، لا استخراج بنية جديدة موازية ولا إعادة كتابة.** اسم الملف المقترَح (قرار تسموي فقط، ليس معمارياً): شيء يعكس الدور الجديد (النطاق الفعلي الآن: نواة الترحيل المحاسبي المشتركة) بدل الإيحاء بأنه خاص بالتعديل اليدوي وحده — **لا أحسم الاسم النهائي هنا** (خارج نطاق Scope تحديداً حسب توجيهك)، فقط أثبت أن **إبقاء الاسم/التسمية الحالية كما هي غير دقيق وظيفياً بعد التوسيع**، بدليل مباشر من نص الملف نفسه.

**بلاست ريديوس الإعادة تسمية (تأكيد فعلي، لا تقدير)**: 3 مستوردين فعليين فقط اليوم (`opening_balances.py`، `opening_party_balances.py`، `journal_voucher_form.py`) — تغيير مسار الاستيراد فيها ميكانيكي بحت، منخفض المخاطر.

## 4. Files/Services Affected (بالتوسيع، لا بإعادة الكتابة)

| الملف | نوع التأثير |
|---|---|
| `journal_edit.py` (أو اسمه الجديد لاحقاً) | توسيع: دالة إنشاء `JournalEntry`، تحقق `is_group`/`is_active`، توحيد الترقيم |
| `posting.py` | تعديل: استبدال بناء `entry.lines`/`_jline`/`_jline_base` المباشر باستدعاء النواة الموسَّعة، مع الإبقاء على منطق COGS/الفروق الخاص بالفواتير كما هو (يُمرَّر كـ"نية محاسبية" جاهزة للنواة) |
| `settlements.py` | نفس نمط `posting.py` — البناء المباشر بـ`_jline`/`_jline_base`/`_jline_party` يُستبدَل باستدعاء النواة، منطق التخصيص/FX الخاص بالتسوية يبقى بالكامل بـ`settlements.py` |
| `invoice_cancel.py` | تعديل محدود: يستفيد من "primitive العكس المحاسبي" إن كان آمناً، **بلا نقل Business Rules الخاصة بالإلغاء** |
| `opening_balances.py`/`opening_party_balances.py` | أقل الملفات تأثراً — تستخدم النواة أصلاً؛ قد يحتاج تحديث أسماء الاستيراد فقط لو أُعيدت التسمية |
| `journal_voucher_form.py` | تعديل محدود: إزالة `_next_ref_no` المحلية لصالح دالة الترقيم الموحَّدة الجديدة؛ إزالة إنشاء `JournalEntry` المباشر لصالح دالة الإنشاء الجديدة بالنواة |

## 5. Files Explicitly Untouched

`returns.py` (بالكامل، بما فيه أي بناء قيود بداخله رغم تشابهه الشديد بـ`posting.py`) · `app/models.py` (لا تعديل schema بهذه المرحلة) · `Invoice.invoice_no` وكل مسار يعتمد عليه للعرض/البحث · `Settlement`/`SettlementAllocation` (لا حقل جديد، لا Reversal) · كل شاشات UI الأخرى غير `journal_voucher_form.py` · `parties.py`/`item_queries.py`/محرك المخزون بالكامل.

## 6. Required Central Guarantees

بعد 3B-4، **أي** قيد محاسبي (بغض النظر عن المسار) يجب أن يمر عبر ضمانات مركزية واحدة: حساب موجود وفعّال وليس Group، حصرية مدين/دائن، لا سطر فارغ، وجود سطر واحد على الأقل، توازن كامل بالعملة الأساسية، رقم مرجعي (`ref_no`) من مصدر توليد واحد. **لا مسار يُعفى من هذه الضمانات بحجة كونه آلياً.**

## 7. Draft vs Immediate Posted Boundary

النواة تعرض قدرتين منفصلتين على نفس الأساس (بناء + تحقق + ترحيل)، لا lifecycle واحداً مفروضاً:
- **يدوي**: `create(DRAFT)` → `add_line`×N (كل استدعاء يُطبَّق فوراً، قابل للتراجع بحذف سطر) → `post()` (يتحقق ويُقفل).
- **آلي**: `build(lines جاهزة كاملة)` → `validate_and_post()` (خطوة واحدة، لا حالة وسيطة تُخزَّن DRAFT بقاعدة البيانات إطلاقاً — يطابق ما يفعله `posting.py` اليوم فعلياً، فقط عبر نقطة دخول موحَّدة بدل بناء يدوي مباشر).

**كلا المسارين ينتهيان لنفس تحقق `_validate_lines`+`is_balanced()`** — الفارق الوحيد هو **متى** يُستدعى (بعد كل سطر يدوياً، أو مرة واحدة بعد بناء كامل آلياً)، لا **ماذا** يُتحقَّق.

## 8. Numbering Boundary

**داخل 3B-4**: دالة توليد `ref_no` واحدة تخدم كل أنواع القيود المحاسبية (يدوي، عكس، آلي) — بادئات مختلفة حسب النوع (كما اليوم) لكن **مصدر منطق واحد**، مع فصل صريح لعدّاد قيود العكس عن عدّاد القيود العادية (منع التسرّب المُكتشَف بـ`journal_voucher_form.py`).
**خارج 3B-4**: `Invoice.invoice_no` بالكامل — لا لمس، لا حتى توحيد نمطها مع أرقام القيود.

## 9. Reversal Boundary

**primitive عام واحد** (منطق `reverse_manual_entry` الحالي، بمعزل عن اسمه) — متاح لأي مسار يحتاج "اعكس هذا القيد بدقة تاريخية" **بلا** أي Business Rule خاصة مُدمَجة بداخله. **خارج الـprimitive تماماً**: قرار "هل يُسمح بالعكس أصلاً" (مثال: فحص وجود تسويات مرتبطة بفاتورة قبل السماح بإلغائها) — هذا يبقى بمنطق `invoice_cancel.py` الخاص، يستدعي الـprimitive **بعد** أن يقرر بنفسه أن العكس مسموح، لا العكس.

## 10. Business-Domain Responsibilities That Stay Outside Core

حساب COGS ومتوسط التكلفة (فواتير/مخزون) · تحديد حساب العميل/المورد المناسب (`_invoice_receivable_or_payable_account_id`) · قواعد التخصيص متعدد الأهداف والـFX الخاصة بالتسوية (`_post_settlement_multi`) · فحص وجود تسويات قبل إلغاء فاتورة · كل قواعد صحة المستند التجاري نفسه (كمية، سعر، خصم، ضريبة). **النواة تستقبل "أسطر محاسبية جاهزة" (حساب+مبلغ+عملة) ولا تعرف مصدرها أو سبب قيمتها.**

## 11. Migration Impact

**None متوقَّع لضمانات `is_group`/`is_active`/الترقيم الموحَّد** — كلها منطق Python صرف فوق أعمدة موجودة أصلاً (`Account.is_group`/`is_active`، `JournalEntry.ref_no`)، لا عمود جديد مطلوب لتحقيقها.
**Unknown لسؤال واحد مؤجَّل لمرحلة Architecture**: هل يبقى الترقيم COUNT-based (كما اليوم، لا Migration) أم يتحوّل لجدول Sequence مستقل ذري (Migration مطلوبة)؟ **قرار لم يُتَّخذ بعد، خارج نطاق هذا التقرير.**
**None لأي شيء آخر مذكور بالنطاق** — لا عمود/جدول جديد آخر يظهر ضرورياً من تعريف النطاق وحده.

## 12. Tests Impact (تقدير فقط، لا كتابة)

- **يجب أن يبقى 30/30 Regression + 200/200 Fuzz أخضرين بلا تعديل قيم** — أي تغيير بمخرجات الأرقام المحاسبية الحالية بعد التوحيد يُعتبَر Regression حقيقياً، لا تحسيناً.
- **فجوة تغطية مؤكَّدة سابقاً (Post-Archaeology §K/§11 بالتقرير الأول) تحتاج سدّاً**: لا يوجد اليوم ملف اختبار مخصَّص للنواة المحاسبية نفسها (`journal_edit.py`) بمعزل — كل تغطيتها الحالية غير مباشرة. 3B-4 على الأرجح يحتاج ملف اختبار مخصَّص جديد يغطي: رفض Group/Inactive account (جديد كلياً، غير مُختبَر إطلاقاً اليوم بأي مكان)، تطابق نتائج الترقيم الموحَّد مع النمط القديم رقمياً، كلا مساري DRAFT/Immediate-POSTED على نفس النواة.
- **اختبارات الترقيم القديمة الست** (المتناثرة ضمنياً بالمشروع) تحتاج مراجعة فردية للتأكد أن توحيد المصدر لا يغيّر الأرقام الفعلية الناتجة بأي اختبار قائم يعتمد على نمط `ref_no` معيَّن.

## 13. Open Architectural Questions (تُترَك لمرحلة Architecture، لا يُبَتّ فيها هنا)

1. الشكل الدقيق لواجهة النواة الموحَّدة (توقيعات الدوال الجديدة تحديداً) — Scope حدَّد **ماذا** تضمن النواة، لا **كيف** تُستدعى برمجياً.
2. الاسم النهائي للملف/الدوال بعد إعادة التسمية (تأكَّد أن إعادة التسمية مطلوبة بدليل — الاسم نفسه قرار Architecture).
3. هل تُستبدَل `_jline`/`_jline_base`/`_jline_party` (أدوات بناء سطر بالعملة) بواجهة موحَّدة داخل النواة نفسها، أم تبقى أدوات مساعدة منفصلة تُغذّي النواة بأسطر جاهزة؟ (الفرق: هل حساب `base` من `raw`×`rate` مسؤولية النواة أم مسؤولية الطبقة الآلية قبل التسليم لها؟)
4. آلية "بناء دفعة كاملة من الأسطر ثم تحقق واحد" (نمط Immediate-POSTED) — هل تُمرَّر كقائمة جاهزة لدالة واحدة، أم عبر نفس دالة "إضافة سطر" مكرَّرة N مرة ثم "ترحيل"؟ قرار تصميم API، لا Scope.
5. شكل عدّاد الترقيم بعد التوحيد (COUNT بمعزل لكل بادئة كما اليوم، أم جدول Sequence مستقل) — مرتبط مباشرة بـ§11 (Migration Impact) أعلاه.

---

**لا Architecture، لا Design Spec، لا كود، لا Migration بهذا التسليم. Scope فقط — بانتظار مراجعتك قبل الانتقال لـBusiness Rules.**
