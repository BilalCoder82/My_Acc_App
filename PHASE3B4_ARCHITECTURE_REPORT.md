# PHASE3B4_ARCHITECTURE_REPORT
**تصميم معماري — النسخة المُعدَّلة بعد 8 تصحيحات من Bilal (§8 صياغة أدق لحدود الحماية، §4/§5-ج توضيح صريح لعدم اشتقاق/فرض علاقة `base=raw×rate`، §5-أ/ب اعتماد Sequence Table + سياسة حجز الترقيم، §7 توضيح حد أدنى زمني للعكس بلا نظام فترات، §9 قلق Master Data منفصل لتحويل حساب لـGroup، §12 توسيع Characterization لكل دومين لا FX فقط). لا كود، لا Migration تنفيذية، لا UI، لا فتح لـ3B-1/2/3.**

---

## 0. تعريف Posting — الأساس الذي يُبنى عليه كل شيء أدناه

**Posting ليس تغيير `status`.** هو: **اعتماد أثر محاسبي بعد اجتياز كل قواعد الصحة، ثم تثبيته POSTED غير قابل للتعديل، ليدخل الدفاتر كمصدر معتمد.**

```
Business Domain (Invoice/Settlement/Opening/Inventory/Refund)
        ↓  يقرر: ما الأثر المحاسبي المطلوب؟ (لا يعرف "كيف يُعتمَد")
   Accounting Intent
        ↓
Accounting Posting Boundary  ← الحد الوحيد الذي يعرف "كيف يُعتمَد"
        ↓
   Validate Accounts (exists / active / not-group)
        ↓
   Validate Lines (exclusivity, no empty, rate>0 عند الاستخدام)
        ↓
   Validate Balance (base currency)
        ↓
   Assign ref_no
        ↓
   POSTED (غير قابل للتعديل من هذه النقطة)
```

**Business Domain لا يصبح جزءاً من الـCore أبداً** — يبقى مسؤولاً عن "ماذا يجب أن يكون القيد"، والـBoundary وحده مسؤول عن "هل هذا القيد صحيح ومُعتمَد".

---

## 1. حدود Accounting Posting Boundary — بدقة

**داخل الحد**: كل شيء بـ§0 أعلاه (إنشاء `JournalEntry`، بناء/قبول `JournalLine`، كل التحققات المركزية §Business Rules §1-§5، `ref_no`، Immutability، Reversal العام).

**خارج الحد صراحة (Domain، لا Core، بلا استثناء)**: حساب COGS/متوسط التكلفة · تحديد حساب العميل/المورد المناسب · تخصيص التسوية متعدد الأهداف وFX الخاص بها (`_jline_party`, `carrying_rate`) · أهلية إلغاء فاتورة (فحص تسويات مرتبطة) · أهلية استرداد · دلالة Append-Only للتسويات · كل قواعد المستند التجاري نفسه (كمية، سعر، ضريبة، حالة `InvoiceStatus`).

**اختبار الحد العملي لأي قاعدة جديدة تُقترَح لاحقاً**: *"هل هذه القاعدة صحيحة لأي قيد محاسبي بغض النظر عن مصدره؟"* — إن كانت الإجابة نعم فهي Core؛ إن احتاجت معرفة "هل هذا فاتورة أم تسوية أم رصيد افتتاحي" فهي Domain بالتعريف، ولا تدخل الـBoundary مهما بدت "محاسبية الطابع".

---

## 2. مسؤولية كل طبقة

| الطبقة | تملك | لا تملك |
|---|---|---|
| Business Domain (`posting.py`/`returns.py`/`settlements.py`/`invoice_cancel.py`/`opening_*.py`) | حساب المبالغ، اختيار الحسابات المناسبة لمنطقها، قرار "متى" يُطلَب الترحيل، القيم الأجنبية/الأساسية الناتجة عن منطقها الخاص (بما فيها الأنماط الثلاثة للـFX) | صلاحية بناء `JournalEntry`/`JournalLine` مباشرة، صلاحية تجاوز أي تحقق مركزي |
| Accounting Posting Boundary | بناء/تحقق/اعتماد القيد، الترقيم، منع التعديل بعد POSTED، عملية العكس العامة | معرفة سبب وجود أي مبلغ، معرفة نوع المستند المصدر (تتعامل معه كنص `source_type` فقط، بلا تفسير) |
| Persistence (`JournalEntry`/`JournalLine` models) | التخزين، القيود البنيوية (FK، `UNIQUE`، `CHECK`) | أي منطق عمل |

---

## 3. واجهة الـCore المقترَحة (مفاهيمياً — لا كود، لا توقيعات Python نهائية)

نمطان يُخدَمان بنفس دوال التحقق الداخلية المشتركة (لا نسختان من منطق التحقق):

**نمط أ — يدوي (DRAFT قابل للتعديل)**، للاستخدام حيث الإدخال تدريجي بشرياً:
```
begin(entry_date, currency, rate, source_type, description) → مرجع قيد DRAFT
add_line(مرجع القيد, account_id, raw_debit, raw_credit, base_debit, base_credit, line_currency?, line_rate?)
   → يتحقق فوراً: الحساب (موجود/نشط/ليس Group)، حصرية مدين/دائن، لا سطر فارغ، rate>0 إن استُخدم
remove_line(مرجع القيد, line)
post(مرجع القيد) → يتحقق: توازن + وجود سطر واحد على الأقل + يُعيّن ref_no إن لم يكن مُعيَّناً → POSTED
```

**نمط ب — فوري (Build → Validate → POSTED)**، للمسارات الآلية:
```
post_immediate(entry_date, currency, rate, source_type, description, lines[]) → POSTED مباشرة
   حيث كل عنصر بـ lines[] = (account_id, raw_debit, raw_credit, base_debit, base_credit, line_currency?, line_rate?)
   يُطبَّق عليه **نفس** تحقق كل سطر بنمط (أ)، ثم توازن القيد الكامل، ثم ref_no، ثم POSTED — عملية ذرّية واحدة بالذاكرة، لا صفوف DRAFT وسيطة تُخزَّن.
```

**العكس العام** (متاح للنمطين، وأي Domain يحتاج عكساً بلا قواعد خاصة):
```
reverse(original_entry, reversal_date, description?) → POSTED جديد
   يتحقق: original POSTED، original ليس عكساً لقيد آخر، لا يوجد عكس سابق له، reversal_date >= original.entry_date (قرار §14.1)
   ينسخ كل سطر معكوساً (raw وbase معاً، حرفياً، بلا إعادة حساب) — نفس سلوك reverse_manual_entry اليوم تماماً
```

**ملاحظة حاسمة على التوقيع**: `add_line`/`lines[]` تقبل `(raw_debit, raw_credit, base_debit, base_credit)` **الأربعة صراحة ومستقلة عن بعضها** — لا `rate` واحداً يُشتَق منه `base` داخل الـBoundary افتراضياً. هذا يحل §4 التالية.

---

## 4. استيعاب الأنماط الثلاثة للعملة/base **بلا دمج خاطئ**

**القرار المعماري الجوهري**: **الـBoundary لا تحسب `base` من `raw×rate` أبداً بنفسها كسلوك افتراضي.** هي تستقبل **الزوج `(raw, base)` جاهزاً** من طبقة الـDomain، وتتحقق فقط من صحته (لا قيم سالبة، لا حصرية مدين/دائن، توازن الإجمالي). الحساب "كيف نصل لهذا الزوج" **يبقى بالكامل مسؤولية الـDomain**، عبر أدوات مساعدة **خارج** الـBoundary (لا داخلها):

| النمط | كيف يُحسَب الزوج (خارج الـBoundary) | ماذا تستقبل الـBoundary |
|---|---|---|
| 1. `raw×rate=base` | `base = raw × rate` (دالة مساعدة بسيطة، مكافئة لـ`_jline` اليوم) | الزوج الناتج، كأي زوج آخر |
| 2. `raw=base` | `base = raw` مباشرة (مكافئ `_jline_base`) | نفس الشيء — حالة خاصة من نفس الواجهة العامة، لا فرع كود خاص بها |
| 3. مُدمَج (`_jline_party`) | حساب Domain الخاص (مُدمَج من عدة أسعار) — **بلا تغيير على منطقه إطلاقاً** | نفس الشيء تماماً |

**النتيجة**: الـBoundary **لا "تعرف" أصلاً** أنها تتعامل مع 3 أنماط — ترى دائماً "زوج مبلغ خام/أساسي جاهز"، والفرق بين الأنماط الثلاثة يختفي تماماً من منظورها. **هذا يحقق ما طلبتَه حرفياً**: FX الخاص بـ3B-3 لا يُنقَل للـCore ولا يُمَس، لأن الـCore أصلاً لا تحسب أي فرق صرف — فقط تستقبل الأرقام النهائية وتتحقق من توازنها.

**توضيح حاسم (تصحيح Bilal — يجب أن يظهر حرفياً بأي Design Spec لاحق)**: "التحقق من صحة الزوج" **لا يعني ولا يشمل بأي شكل** التحقق من العلاقة `base == raw × rate` كقاعدة عامة على كل سطر. **الـBoundary تتحقق فقط من الخصائص التالية على كل سطر، ولا شيء غيرها**:
- عدم السلبية (`raw`/`base` كلاهما).
- حصرية مدين/دائن (Debit XOR Credit).
- عدم `(0,0)`.
- صحة الحساب (موجود، نشط، ليس Group).
- `rate > 0` **فقط عندما يكون `rate` جزءاً فعلياً من بيانات ذلك السطر/القيد** (راجع التوضيح الدقيق بـ§5-ب أدناه لثلاث الحالات).
- توازن `Σdebit_base == Σcredit_base` على مستوى القيد ككل (لا على مستوى السطر الواحد).

**`base_debit`/`base_credit` تدخل الـBoundary كقيمة محاسبية نهائية موثوقة قادمة من الـDomain — الـBoundary لا تعيد اشتقاقها، ولا تفرض أي علاقة حسابية عامة بينها وبين `raw`.** فرض `base=raw×rate` كقاعدة عامة كان سيُبطِل النمطين 2 و3 فوراً (`_jline_base`: `raw=base` بلا `rate` ذي معنى؛ `_jline_party`: `raw`/`base` مستقلان تماماً بلا معدّل واحد يربطهما) — **هذا محسوم نهائياً، لا يُعاد فتحه.**

**الدوال المساعدة الحالية (`_jline`/`_jline_base`/`_jline_party`) تبقى موجودة بمنطقها الحالي حرفياً**، لكنها تصبح "طبقة تحضير" تُستدعى من كود الـDomain **قبل** تسليم النتيجة للـBoundary، لا جزءاً من الـBoundary نفسها.

---

## 5. استراتيجية `ref_no` — القرار (Sequence Table، معتمَد بشروط) + سياسة الحجز + معنى `rate`

### 5-أ. القرار: Sequence Table معتمَد

بعد المقارنة (COUNT الحالي مقابل جدول Sequence ذري)، **القرار معتمَد: Sequence Table**. السبب المعتمَد رسمياً **ليس** خطر تزامن كارثي مؤكَّد اليوم (السياق الحالي — تطبيق سطح مكتب، على الأرجح مستخدم واحد لكل قاعدة SQLite في نفس اللحظة — يُخفِّفه فعلياً)، بل: (1) نظام محاسبي قابل للنمو لا سبب وجيهاً لإبقائه على `COUNT`، (2) يحل مشكلة تداخل `JV-%`/`JV-REV-%` **بالبنية نفسها**، لا بشرط استعلام (`NOT LIKE`) يمكن نسيانه بمسار جديد لاحقاً. **الأثر**: Migration واحدة صغيرة (جدول Sequence جديد — لا تغيير على `JournalEntry` نفسها). **مصدر توليد واحد فقط** يستدعيه `begin()`/`post_immediate()`/`reverse()` معاً — لا نسخ متعددة كاليوم (6 نسخ).

**فصل namespaces حقيقي بالبنية، لا بالاستعلام**: كل بادئة (`JV`، `JV-REV`، `JE-SAL`، `JE-PUR`، `JE-RCV`، إلخ) = **مفتاح صف منفصل تماماً بجدول الـSequence** — لا اعتماد على `LIKE`/`NOT LIKE` لفصل عدّاد عن آخر بأي شكل.

**`ref_no` ليس هوية محاسبية — تأكيد صريح، لا تغيير**: `JournalEntry.id` يبقى الهوية العلائقية الوحيدة (لا FK يعتمد على `ref_no` بأي مكان، كما أثبت تقرير الأعمال). `ref_no` = Reference/Document Number للعرض/البحث فقط، تماماً كـ`Invoice.invoice_no`.

### 5-ب. سياسة حجز الرقم للـDRAFT (قرار Bilal الصريح)

- **الرقم يُحجَز فوراً عند إنشاء الـDRAFT** — يعكس السلوك الحالي، ويبقى كذلك.
- **لا إعادة استخدام للأرقام المحجوزة أبداً** — حتى لو حُذفت المسودة قبل الترحيل.
- **الفجوات (Gaps) الناتجة عن مسودات محذوفة مقبولة صراحة** — لأن `ref_no` رقم مرجعي (Reference)، لا رقم مستند ضريبي متسلسل بلا فجوات. **إن فرضت جهة تنظيمية مستقبلاً قاعدة تسلسل متصل بلا فجوات، هذا قرار Domain/سياسة منفصل تماماً عن 3B-4، لا يُفتَرض الآن.**

### 5-ج. معنى `rate` بدقة — ثلاث حالات، لا حالة واحدة (تصحيح Bilal — يجب أن يظهر حرفياً بأي Design Spec)

**لا تفترض الـBoundary أن كل قيد متعدد العملات يحتاج نفس `rate`، ولا أن "استقلال `base` عن `raw`" يعني أن `rate` "غير مهم" — القاعدة الدقيقة: الـBoundary لا تستخدم `rate` لاشتقاق `base` (محسوم بـ§4)، لكنها لا تسمح بقيمة `rate` غير صالحة عندما يكون `rate` جزءاً فعلياً من بيانات ذلك السطر/القيد**:

| الحالة | السطر/القيد | قاعدة `rate` |
|---|---|---|
| 1. العملة = العملة الأساسية | لا تحويل فعلي مطلوب | `rate` يمكن أن يكون `1` أو القيمة المناسبة التي يفرضها النموذج الحالي — **لا `rate<=0`** |
| 2. عملة أجنبية + سعر تحويل مُستخدَم فعلياً (`_jline` بالنمط 1) | `base` مُشتَقة من `raw×rate` **بواسطة الـDomain قبل التسليم** | **`rate > 0` إلزامي** — قيمة غير صالحة هنا تعني قيمة `base` غير موثوقة أصلاً قادمة للـBoundary |
| 3. `base` مستقلة عن `raw` (`_jline_base`/`_jline_party`) | لا علاقة حسابية `raw×rate=base` مقصودة أصلاً | **لا نقول "`rate` غير مهم إذن نتجاهله"** — إن كان حقل `rate` موجوداً بالسطر/القيد (حتى لو لم يُستخدَم لاشتقاق `base`)، **يجب أن يحمل قيمة صالحة (`>0`) إن وُجد إطلاقاً**، لأنه لا يزال جزءاً من بيانات القيد التاريخية (يُستخدَم مثلاً بعرض القيد أو بعمليات أخرى لاحقاً) — الـBoundary ترفض قيمة `rate<=0` **حيثما وُجد الحقل أصلاً بالبيانات المُسلَّمة إليها**، بصرف النظر عن كونه استُخدِم لاشتقاق `base` أم لا |

**الفرق الجوهري المطلوب توضيحه بالتصميم لاحقاً**: "الـBoundary لا تشتق `base` من `rate`" **لا يعني** "الـBoundary لا تتحقق من صلاحية `rate` كحقل بيانات مستقل" — هذان قراران منفصلان تماماً، لا يجوز خلطهما.

---

## 6. DRAFT مقابل Immediate POSTED

مُغطاة بالكامل بـ§3 (نمط أ/ب) — **كلا النمطين يستدعيان نفس دوال التحقق الداخلية للسطر وللقيد ككل**؛ الفارق الوحيد هو **توقيت** الاستدعاء (بعد كل سطر يدوياً بنمط أ، أو دفعة واحدة بنمط ب) — لا اختلاف فيما يُتحقَّق منه. **سياسة ترقيم الـDRAFT محدَّدة بالكامل بـ§5-ب أعلاه.**

---

## 7. General Reversal — التصميم النهائي

`reverse()` بـ§3 أعلاه، مع القاعدة الجديدة المعتمَدة (§14.1): **`reversal_date >= original_entry.entry_date` إلزامي** — أي محاولة بتاريخ أسبق تُرفَض بالـBoundary نفسها (لا اعتماداً على انضباط المُستدعي).

**توضيح صريح (تصحيح Bilal)**: هذا **حد أدنى زمني بحت، لا نظام فترات محاسبية (Accounting Periods)**:
- `original=2026-09-10`، `reversal=2026-09-09` → **مرفوض** (أسبق من الأصل).
- `original=2026-09-10`، `reversal=2026-09-10` → **مسموح** (نفس اليوم).
- `original=2026-09-10`، `reversal=2026-10-01` → **مسموح من ناحية الـCore** (مستقبلي، بلا حد أعلى).
- **"هل سبتمبر مُغلَق محاسبياً؟" ليس من اختصاص 3B-4إطلاقاً** — مفهوم إغلاق الفترات المحاسبية (Period Closing) قرار Domain/سياسة مستقلة تماماً، غير موجود اليوم بالكود أصلاً، ولا يُفتَرض أو يُبنى ضمنياً هنا.

**قواعد المنع تبقى كما هي حرفياً** (لا عكس عكس، لا عكس مزدوج لنفس الأصل) — منقولة بلا تغيير من `reverse_manual_entry` الحالية.

**`invoice_cancel.py` تستفيد من `reverse()` بعد أن تُنهي فحوصها الخاصة** (وجود تسويات مرتبطة، حالة الفاتورة) — **الفحص نفسه يبقى بالكامل بـ`invoice_cancel.py`**، الـBoundary لا "تعرف" أنها تُستدعى من سياق إلغاء فاتورة، فقط تنفّذ عكساً عاماً بعد أن يُقرِّر المُستدعي أنه مسموح.

**التسويات (3B-3)**: **لا `reverse()` لها إطلاقاً** — القرار الحالي (Append-Only) يبقى كما هو تماماً، لا فتح لـ3B-3 بأي شكل.

---

## 8. حماية POSTED — من "لا كود يحاول" إلى "لا مسار إنتاجي معتمد" (تصحيح Bilal — لا ادّعاء استحالة تقنية مطلقة)

**الصياغة الدقيقة (بعد تصحيح Bilal)**: بعد الهجرة الكاملة للمسارات (§11 أدناه) للمرور عبر `begin`/`post_immediate`/`reverse` حصراً — **كل Accounting Posting داخل Application Domain يجب أن يمر عبر الـBoundary، ولا يوجد مسار إنتاجي معتمد ينشئ أو يعدّل `JournalEntry`/`JournalLine` خارجها.** هذا ضمان **معماري على مستوى كود التطبيق (Application-level)**، لا ضماناً مطلقاً على مستوى قاعدة البيانات نفسها — SQLAlchemy/SQLite لا يمنعان تقنياً مبرمجاً مستقبلاً من كتابة `JournalEntry(...)` مباشرة أو تنفيذ SQL خام يتجاوز الـBoundary بالكامل. **حماية قاعدة البيانات نفسها (لو احتيجت لاحقاً) طبقة منفصلة تماماً (مثال: صلاحيات DB-level، أو Triggers) — خارج نطاق 3B-4، ولا يجوز الادّعاء بأن الـBoundary وحدها تجعل التجاوز مستحيلاً تقنياً.**

بهذه الصياغة الدقيقة: **الفرق الحقيقي المُحقَّق بعد 3B-4** هو انتقال الحماية من "لا كود موجود اليوم يحاول ذلك" (حالة عرضية) إلى "لا مسار مُعتمَد/مُوثَّق بالمشروع يفعل ذلك" (حالة مفروضة بانضباط معماري صريح وموثَّق) — لا أكثر من ذلك، ولا أقل.

---

## 9. أين يقع Account Validation

**داخل الـBoundary حصراً، بدالة تحقق واحدة مشتركة** تُستدعى من نقطتي الدخول (نمط أ ونمط ب معاً) — لا نسخة بالـUI (`list_postable_accounts` تبقى فلترة UI **مريحة** لتحسين تجربة الاختيار، لكن **لا تُعتبَر خط الدفاع** بعد الآن؛ الدفاع الحقيقي الوحيد ينتقل للـBoundary). هذا يسدّ الفجوة المؤكَّدة بتقرير Business Rules (لا تحقق `is_active`/`is_group` بمستوى خدمة اليوم). **النطاق هنا: منع استخدام حساب Group/Inactive بقيد جديد فقط — لا فحص استعادي على القيود القائمة (§14.4 بتقرير Business Rules، محسوم).**

**قلق Master Data منفصل (تسجيل فقط — خارج 3B-4 صراحة، تصحيح Bilal)**: هناك فرق جوهري بين حساب يصبح `Inactive` لاحقاً (لا يفسد القيود القديمة، القاعدة أعلاه تكفيه) وبين **تحويل حساب مُستخدَم فعلياً بقيود POSTED إلى `is_group=True`** — الأخير أخطر بنيوياً لأن "حساب Group" له دلالة مختلفة تماماً بشجرة الحسابات (لا يُفتَرض أن يحمل أرصدة/حركات مباشرة أصلاً). **القرار المُسجَّل هنا كـTechnical/Master-Data concern منفصل، لا كجزء من 3B-4**: *"لا يجوز تحويل حساب مُستخدَم فعلياً بقيود POSTED إلى Group بطريقة تكسر دلالة شجرة الحسابات أو تجعله غير قابل للترحيل بأثر رجعي."* — هذا يحتاج حماية بمستوى إدارة دليل الحسابات نفسه (`account_edit.py`)، **لا علاقة له بـAccounting Posting Boundary**، ويُترَك كملاحظة لمرحلة مستقلة إن احتيج تنفيذها لاحقاً.

---

## 10. (0,0) — مقارنة DB CHECK مقابل Python فقط، مع تحليل أثر البيانات التاريخية

| | خيار Python فقط (توسيع التحقق الحالي داخل الـBoundary، بلا لمس Schema) | خيار تعديل الـDB CHECK (`>=` → `>` بكلا الشرطين) |
|---|---|---|
| Migration مطلوبة؟ | لا | **نعم** |
| الحماية بعد اكتمال 3B-4 | **قوية فعلياً** — بما أن الـBoundary تصبح الممر الوحيد (§8)، فلا مسار آخر يستطيع إدراج `(0,0)` أصلاً | قوية بمستوى إضافي (تحمي حتى من استعلام SQL خام مستقبلي خارج أي طبقة Python) |
| **خطر البيانات التاريخية**: | لا خطر — لا تُلمَس بيانات قائمة إطلاقاً | **يحتاج Data Audit إلزامي أولاً** (استعلام قراءة فقط: `SELECT COUNT(*) FROM journal_lines WHERE debit=0 AND credit=0`) — لو وُجد أي صف قائم بهذه الحالة، `ALTER`/`batch_alter_table` على SQLite (يُعيد بناء الجدول كاملاً) **سيفشل فوراً** عند محاولة فرض الـCHECK الأشد على بيانات موجودة تخالفه. يجب تشغيل هذا الـAudit (قراءة فقط، بلا أي تعديل) **قبل** اتخاذ القرار النهائي، لا بعده. |
| يتوافق مع قرار Forward-only (§14.3) | نعم مباشرة | نعم **بشرط** أن الـAudit يؤكد عدم وجود بيانات قائمة تخالف — وإلا يحتاج التعديل تأجيلاً لحين معالجة تلك الصفوف (خارج نطاق 3B-4 حينها، Data Audit منفصل كما نصصتَ) |

**لا توصية حاسمة من هذا التقرير — قرارك بعد تشغيل الـAudit المذكور** (وهو استعلام قراءة بحت، يمكن تشغيله بأي وقت بلا أي مخاطرة، ويُوصى بتشغيله بالفعل كخطوة أولى في مرحلة التنفيذ بغض النظر عن أي القرارين يُعتمَد لاحقاً).

---

## 11. كل المسارات الحالية التي ستُعاد توجيهها عبر الـBoundary (الخريطة الكاملة المطلوبة)

| المسار الحالي | النمط المُستهدَف | ملاحظة |
|---|---|---|
| `journal_voucher_form.py` (يدوي) | نمط أ (`begin`/`add_line`/`post`) | يستبدل `add_manual_line`/`post_manual_entry` باستدعاءات الـBoundary الجديدة مباشرة — أقرب الملفات لأي تغيير فعلي لاحقاً |
| `opening_balances.py` (3B-1/3B-2) | نمط ب على الأرجح (`post_immediate`) بدل استخدامها الحالي لـ`add_manual_line` بنمط أ مُصغَّر | أقل الملفات خطورة — تستخدم الجوهر الصحيح أصلاً اليوم |
| `opening_party_balances.py` (3B-3) | نفس الملاحظة أعلاه | نفس مستوى الخطورة المنخفض |
| `posting.py` (فواتير بيع/شراء) | نمط ب | يحتاج تمرير أزواج `(raw, base)` جاهزة بدل بناء `entry.lines` مباشرة |
| `settlements.py` (3B-3، قبض/دفع/استرداد) | نمط ب | **حساس محاسبياً جداً** — يحتاج التحقق الأكثر دقة من تطابق الأرقام حرفياً (§12) بسبب الأنماط الثلاثة المُستخدَمة فيه تحديداً |
| `invoice_cancel.py` | نمط ب لبناء قيد الإلغاء نفسه + استدعاء `reverse()` العام حيث ينطبق | فحوص الأهلية الخاصة به تبقى خارج الـBoundary بالكامل |
| `returns.py` | **مذكور هنا فقط لاكتمال الخريطة المطلوبة صراحة بهذا البند — لا يعني إدراجه بأول دفعة تنفيذ، ولا لمسه الآن بأي شكل** | يطابق نمط `posting.py` تماماً بالبنية، نفس الملاحظات تنطبق نظرياً متى قُرِّر لمسه مستقبلاً |

---

## 12. إثبات أن 3B-1/2/3 ستنتج نفس الأرقام حرفياً — استراتيجية التحقق (مُوسَّعة، تصحيح Bilal: Characterization لكل دومين، لا FX فقط)

1. **الأرقام الذهبية (Golden Numbers) موجودة أصلاً**: 30/30 Regression + 200/200 Fuzz الحاليان **هما** خط الأساس المرجعي — أي تشغيل لاحق يجب أن يُنتِج **تطابقاً حرفياً**، لا "لا أخطاء جديدة" فقط.

2. **Characterization Baseline كامل لكل دومين على حدة قبل لمسه — ليس فقط للأنماط ذات FX** (تصحيح Bilal، أهم إضافة بهذه الجولة): لكل من: **3B-1 (Opening Account Balances)، 3B-2 (Opening Inventory)، 3B-3 (Opening Party/Settlement/Refund)، Sales، Purchase، Receipt، Payment، Refund، Invoice Cancellation** — يُسجَّل **قبل** أي تعديل، لعيّنة تمثيلية من معاملات حقيقية (تغطي الحالات الحدّية المُختبَرة أصلاً بكل مرحلة)، **كل الحقول التالية حرفياً**:
   - على `JournalEntry`: `ref_no`، `currency_code`، `exchange_rate`، `status`، `source_type`.
   - على كل `JournalLine` تابع له: `account_id`، `debit`، `credit`، `debit_base`، `credit_base`، `line_currency_code`، `line_exchange_rate`.

   يُقارَن **`BEFORE == AFTER` على مستوى البيانات نفسها حقلاً بحقل** (لا `assert` عام بالاختبار فقط، ولا الاكتفاء بأن الاختبار "لا يزال أخضر") — الهدف: **إثبات أن المخرجات المحاسبية نفسها لم تتغيّر حرفاً واحداً**، لا أن الاختبارات تمر فقط.

3. **هجرة دومين واحد في كل مرة، لا Big-Bang**: تُرتَّب المسارات بـ§11 من الأقل خطورة للأعلى (`opening_*` أولاً، `journal_voucher_form.py` ثانياً، `posting.py`/`invoice_cancel.py` ثالثاً، `settlements.py` أخيراً وبحذر إضافي). **Full Regression + Fuzz + Characterization Diff (البند 2) يُشغَّلان ويُطابَقان رقمياً بعد كل دومين بمفرده** قبل الانتقال للتالي — أي Regression يُنسَب فوراً لآخر دومين هُوجِر، لا يُبحَث عنه بين عدة تغييرات دفعة واحدة.

4. **اهتمام إضافي (لا استبدال) لأنماط FX تحديداً**: لأي دومين يستخدم النمط 2 أو 3 (`_jline_base`/`_jline_party`، وتحديداً `settlements.py`)، الـCharacterization بالبند 2 **تشمل بالفعل** قيم `(raw, base)` الناتجة — لا خطوة منفصلة إضافية، فقط تأكيد أن هذه الحقول تحديداً تُفحَص بعناية خاصة ضمن نفس عملية المقارنة الشاملة.

5. **معيار الإغلاق لكل دومين مُهاجَر**: Regression الكامل + Fuzz + Characterization Diff (تطابق حرفي، لا تقريبي) + اختبارات ذلك الدومين المخصَّصة (`test_phase3b3_*.py` للاستيطان، إلخ) — **كلها بنفس الأرقام تماماً** — لا يُعتبَر الدومين "مُهاجَراً" قبل تحقق الأربعة معاً.

---

## 13. تأكيدات صريحة على ما لا يتغيّر (Anti-Scope-Creep)

**لا God Service**: الـBoundary لا "تفهم" فاتورة أو تسوية أو مخزوناً — تتعامل مع أرقام وحسابات مجرَّدة فقط، `source_type` نص واصف بلا تفسير داخلي. **لا Repository/UoW/DDD**: لا طبقة تجريد جديدة فوق SQLAlchemy Session الحالية — نفس نمط الوصول المباشر للـSession المُتَّبع بكل المشروع حتى الآن. **لا تغيير على نتائج 3B-1/2/3** — مضمون بمنهجية §12. **لا فتح على `Settlement` Append-Only أو `Invoice Cancellation` Business Rules** — كلاهما يستدعيان الـBoundary، لا يندمجان بها.

---

**لا كود، لا Migration تنفيذية، لا UI بهذا التسليم. تصميم معماري فقط — بانتظار مراجعتك وإغلاقها قبل أي انتقال لـDesign Spec/Code.**
