# 3B-5-F — Audit Findings Register

يجمع هذا السجل نتائج جولة المراجعة الخارجية لإغلاق 3B-5-F (FD-001 REV1)
قبل الانتقال لتصميم G، مصنَّفة CONFIRMED / UNCONFIRMED / DESIGN QUESTION /
OUT OF SCOPE — بلا إدخال أي إصلاح أو قرار تصميمي جديد ضمن هذا السجل نفسه؛
هذا تصنيف لما حدث فعلاً، لا مرحلة تصحيح إضافية.

## 1. FD-001 REV1 — إزالة الإغلاق الكمي
**التصنيف: CONFIRMED**
تحقّقت منه ثلاث مراجعات مستقلة عبر قراءة `correction_scope.py` مباشرة
وتشغيل الاختبار المستهدف فعلياً: لا `total_qty` بأي صياغة (`<=0`,`==0`,`<0`)
تُستخدَم كإغلاق؛ `walk_warehouse_branch()` يستمر حتى نفاد البيانات
المعروفة؛ `scope_completeness=COMPLETE` لا يعتمد على حالة كمية؛ لا
استدعاء لـ`accumulate_average_cost()`/`get_item_stock_summary()`. متطابق
مع القرار المُعتمَد.

## 2. اختبار 1.4 — لم يكن يختبر عبوراً سالباً فعلياً
**التصنيف: CONFIRMED (فجوة تغطية اختبار، لا خلل تنفيذ) — تم الإصلاح**
النسخة الأصلية: `1 + 9 - 10 = 0` (صفر تام، مطابق فعلياً لـ1.3/1.3b) لا
عبوراً سالباً كما وصفه اسم الاختبار. صُحِّح إلى `1 + 5 - 10 = -4` (عبور
سالب حقيقي)، مع تأكيد على حركة العبور نفسها والحركة اللاحقة (1.4 + 1.4b).
تحقَّقت منه مراجعتان مستقلتان لاحقتان من نفس الحزمة المصحَّحة.

## 3. اختبار 1.5 — لم يكن يتحقق فعلياً من دخول W2 للنطاق
**التصنيف: CONFIRMED (فجوة تغطية اختبار) — تم الإصلاح**
تعليق الاختبار الأصلي ادّعى "W2 المستقلة تُبنى أيضاً" دون أي assertion
يثبت ذلك. إضافة assertion صريح (1.5b) كشفت أن الافتراض كان غير دقيق —
راجع البند التالي.

## 4. دلالات النطاق عبر المستودعات (W2 المستقلة) — كان مقترحاً كـFD-007
**التصنيف: OUT OF SCOPE (ليس خللاً، ليس قراراً جديداً مطلوباً)**
اقتُرِح مبدئياً فتح قرار تصميم جديد لتحديد ما إذا كان يجب أن يمتد النطاق
لمستودع آخر يحمل نفس الصنف بلا تحويل. تبيّن بالفحص أن هذا محسوم مسبقاً:
`WORKFLOW.md §46` ("التكلفة منفصلة لكل مستودع، لا موحّدة على مستوى
الشركة") + `test_warehouse_cost_isolation.py` (12/12 PASS فعلياً) يثبتان
أن عزل المستودعات هو التصميم المعتمد أصلاً، وأن العبور بين المستودعات
في Scope يحدث حصراً عبر `StockTransfer` صريح (FD-002)، لا لمجرد اشتراك
الصنف. **اقتراح FD-007 مسحوب بالكامل** — لا حاجة له.

## 5. تعليق متقادم في 1.5b (بعد إصلاح البند 3، قبل تصحيح هذه الجولة)
**التصنيف: CONFIRMED (توثيق فقط) — تم التنظيف**
تعليق مؤقت أشار إلى "يحتاج قرار تصميم صريح (FD إضافي)" بقي في الكود بعد
أن حسم البند 4 المسألة فعلياً. صُحِّح ليشير مباشرة إلى `§46 WORKFLOW.md`
بدل الإيحاء بسؤال مفتوح.

## 6. عدد ملفات UI في `run_gate.py`
**التصنيف: CONFIRMED (تصحيح رقم مُبلَّغ سابقاً)**
تقرير تنفيذ سابق ذكر "4 ملفات UI"؛ العدّ الفعلي من القائمة = **5 ملفات**
(`test_qt_editor_lifecycle`, `test_ui_warehouse_integration`,
`test_ui_settlement_and_cancel`, `test_ui_full_sales_lifecycle`,
`test_ui_journal_voucher_boundary`) — تعتمد جميعها على `PySide6`.

## 7. عدد ملفات Regression المُعلَن ("43/43")
**التصنيف: CONFIRMED (تصحيح رقم مُبلَّغ سابقاً)**
العدّ البرمجي المباشر لقائمة `REGRESSION_FILES` في `run_gate.py` = **44
ملفاً بالضبط**، لا 43. لا توجد وثيقة تشرح كيف حُسِب الرقم السابق (43)،
فلا يُعتمَد. الرقم الصحيح الموثَّق هنا: 44/44، exit=0 لكل الملفات في
بيئة تحتوي `PySide6`.

## 8. قابلية إعادة إنتاج Full Gate عبر بيئات المراجعين
**التصنيف: UNCONFIRMED (فجوة بيئة/أدوات، لا خلل كود)**
من أصل ثلاث جولات مراجعة مستقلة بهذا السجل، جولتان لم تستطيعا إعادة
إنتاج `44/44 Regression + 200/200 Fuzz + gate PASS` محلياً بسبب غياب
`PySide6` في بيئتيهما (إحداهما بلا اتصال شبكة للتثبيت). جولة واحدة فقط
(هذه) ثبّتت `PySide6` فعلياً وشغّلت الـGate كاملاً وأعادت النتيجة أعلاه.
هذا لا يناقض النتيجة، لكنه يعني أنها لم تُصادَق عليها بأغلبية مراجعين
مستقلين فعلياً حتى الآن — فجوة تستحق تسجيلها، لا تجاهلها.
**توصية عملية (خارج نطاق هذا السجل، لا قرار تصميم)**: تثبيت نسخة
`PySide6` صراحة في `requirements.txt` وتوثيق بيئة تشغيل مرجعية واحدة،
لتفادي هذا الاعتماد على بيئة كل مراجع على حدة مستقبلاً.

## 9. خطر أداء traversal غير محدود — موثَّق ذاتياً في الكود، لم يناقشه أي مراجع بعد
**التصنيف: DESIGN QUESTION (مؤجَّل عمداً من التصميم نفسه — يخص G لاحقاً، لا F)**
`correction_scope.py` نفسه يوثِّق صراحة (قسم "قيد أداء معروف، غير مُعالَج
بهذا التصميم عمداً"): بإلغاء الإغلاق الكمي، لم يعد لطول أي سلسلة
traversal حد أقصى — حدث تصحيح قريب من بداية تاريخ صنف طويل قد يعبر كل
الحركات المتبقية لهذا الصنف/المستودع. هذا لم يُحسَم بعد بأي قرار FD،
وهذا مقصود بحسب توثيق F نفسه (ليس نطاقه). يُسجَّل هنا فقط لضمان أن G
(الذي سيستهلك هذا الـScope فعلياً لإعادة حساب مالية) يأخذه بالحسبان
صراحة عند تصميمه، لا أن يُنسى بين إغلاق F وبدء G.

---

## الخلاصة
- **CONFIRMED وتم إصلاحه**: بنود 2، 3، 5، 6، 7.
- **CONFIRMED بلا حاجة لإصلاح إضافي**: بند 1 (التنفيذ الأساسي).
- **OUT OF SCOPE (لا حاجة لقرار جديد)**: بند 4 — FD-007 مسحوب نهائياً.
- **UNCONFIRMED (فجوة بيئة، تحتاج متابعة عملية لا قرار تصميم)**: بند 8.
- **DESIGN QUESTION مؤجَّلة لِG عمداً**: بند 9.

**3B-5-F: CLOSED.**
التسلسل التالي: Audit Findings Register (هذا الملف) → 3B-5-G Design.
