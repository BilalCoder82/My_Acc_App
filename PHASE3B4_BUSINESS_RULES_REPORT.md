# PHASE3B4_BUSINESS_RULES_REPORT
**قواعد العمل فقط — مُستخرَجة من الكود والقرارات المعتمدة سابقاً، لا من الافتراض. لا Architecture، لا API، لا أسماء ملفات، لا كود. أي قاعدة غير محسومة تُوضَع صراحة تحت §14 (Ambiguous/Open) ولا تُخمَّن.**

---

## 1. Core JournalEntry Rules

- **متى يجوز إنشاؤه؟** لا قيد عمل يمنع الإنشاء نفسه — أي كود يستطيع بناء `JournalEntry` بأي حالة ابتدائية.
- **متى يكون DRAFT؟** فقط بمسار سند القيد اليدوي اليوم (`journal_voucher_form.py`) — يُنشأ `DRAFT` صراحة، بلا تحقق توازن أو وجود أسطر عند الإنشاء نفسه.
- **متى يصبح POSTED؟** إما فوراً عند الإنشاء (كل المسارات الآلية: فواتير/تسويات/أرصدة افتتاحية/عكس)، أو صراحة عبر انتقال `DRAFT→POSTED` (سند القيد اليدوي فقط، عبر `post_manual_entry`).
- **هل يمكن إنشاء POSTED مباشرة؟** **نعم — هذا هو السلوك الفعلي الغالب اليوم** (كل قيد آلي POSTED منذ لحظة إنشائه، بلا مرحلة DRAFT وسيطة مخزَّنة بقاعدة البيانات إطلاقاً).
- **هل يمكن تعديل DRAFT؟** نعم بالكامل — إضافة سطر (`add_manual_line`)، حذف سطر (`remove_manual_line`)، أو حذف كل الأسطر وإعادة بنائها (نمط `_save_draft()` الحالي فعلياً: يحذف كل الأسطر القديمة عند كل حفظ، يعيد بناءها من الصفر).
- **هل يمكن تعديل POSTED؟** **لا، مطلقاً** — `ensure_editable()` يرفض أي استدعاء لـ`add_manual_line`/`remove_manual_line`/`post_manual_entry` على قيد `status != DRAFT`. هذا مُطبَّق فقط على المسار اليدوي؛ **المسارات الآلية لا "تعدّل" أصلاً بعد الإنشاء (لا دالة إنتاجية تحاول ذلك) — القاعدة موجودة فعلياً بالسلوك، لا بحارس صريح يفحصها على تلك المسارات.**
- **هل يمكن حذف POSTED؟** **لا دليل على وجود أي مسار حذف مباشر لقيد POSTED بأي مكان بالكود** — التصحيح دائماً بقيد إضافي (عكس)، لا حذف.
- **علاقة status بـposting**: `status` هو المصدر الوحيد لتحديد قابلية التعديل (`DRAFT`=قابل، غير ذلك=غير قابل). لا حقل آخر يؤثر على هذا القرار.

## 2. JournalLine Rules

- **الحساب يجب أن يكون موجوداً**: مُتحقَّق منه جزئياً فقط — `add_manual_line`/`_validate_lines` يرفضان `account_id` الفارغ (`None`/`0`)، لكن **لا تحقق أن الـ`id` الممرَّر يقابل صفاً فعلياً بجدول `accounts`** (لا `session.get(Account, account_id)` قبل البناء) — الاعتماد ضمنياً على FK بقاعدة البيانات لرفض معرّف غير موجود عند الـ`flush`.
- **الحساب Active**: **[غير محقَّق بأي مكان بمستوى الخدمة]** — فقط فلترة اختيار بالواجهة (`list_postable_accounts`).
- **الحساب ليس Group**: **[غير محقَّق بأي مكان بمستوى الخدمة]** — نفس الملاحظة، UI-only.
- **مدين أو دائن، لا كلاهما**: مُحقَّق مركزياً بمكانين مستقلين متفقين: Python (`add_manual_line`/`_validate_lines`: `d>0 and c>0 → رفض`) و**DB** (`CHECK ck_debit_xor_credit`: `(debit=0 AND credit>=0) OR (credit=0 AND debit>=0)` — يرفض أي صف بقيمتين موجبتين معاً).
- **لا يجوز debit=0 وcredit=0 معاً**: **مُحقَّق بـPython فقط** (`add_manual_line`/`_validate_lines`) — **الـDB CHECK الحالي يسمح بهذه الحالة صراحة** (تحقَّق حسابياً من نص القيد: `debit=0 AND credit>=0` صحيح عند `debit=0, credit=0` أيضاً) — **هذه فجوة حماية حقيقية على المسارات الآلية الأربعة التي لا تستدعي `_validate_lines`/`add_manual_line` إطلاقاً.**
- **القيم يجب ألا تكون سالبة**: **مُحقَّق بالـDB فقط** (نفس الـCHECK: `debit=-5, credit=0` → الشرط الثاني يفشل لأن `debit>=0` كاذبة، والأول يفشل لأن `debit=0` كاذبة → رفض DB). **لا تحقق سلبي صريح بـPython بأي من `add_manual_line`/`_validate_lines`** — الاعتماد الكامل على الـDB لهذه الحالة تحديداً.
- **zero base amount مع foreign amount موجب**: لا قاعدة صريحة تمنع هذا اليوم — تقنياً ممكن حسابياً لو `rate=0` (لا تحقق `rate>0` بـ`add_manual_line` نفسها؛ `Settlement`/`OpeningPartyEntry` فقط لديهما `CHECK rate>0` بمستوى تلك الجداول تحديداً من 3B-3، **لا `JournalLine`/`JournalEntry` أنفسهما**).
- **currency/rate/base**: راجع §3 أدناه بالتفصيل — لا قاعدة واحدة، بل معانٍ متعددة موثَّقة.

## 3. Currency/FX Business Rules

- **`line_currency_code`**: يعني "هذا السطر بعملة مختلفة عن عملة القيد الافتراضية". `None` = يرث عملة القيد.
- **`line_exchange_rate`**: سعر الصرف الخاص بهذا السطر تحديداً، مستقل عن سعر القيد العام. `None` = يرث `exchange_rate` الممرَّر لـ`add_manual_line` (وهو بدوره افتراضياً `exchange_rate` القيد نفسه بالاستخدام النموذجي، لكن التقنية تسمح بتمرير قيمة مختلفة صراحة).
- **متى يرث السطر من القيد؟** فقط حين `line_currency_code`/`line_exchange_rate` كلاهما `None` — وهذا هو **الاستخدام الشائع بكل قيد أحادي العملة (الأغلبية الساحقة اليوم)**.
- **متى تُحسَب base amount؟** **دائماً فوراً وقت إنشاء السطر** (لا حساب مؤجَّل، لا إعادة حساب لاحقة) — قيمة محفوظة صراحة بعمودين (`debit_base`/`credit_base`)، لا مُشتقَّة وقت العرض.
- **هل base amount هي Accounting Source of Truth؟** **نعم، صراحة وحصرياً** — `JournalEntry.is_balanced()` يجمع `debit_base`/`credit_base` فقط، **لا** `debit`/`credit` الخام إطلاقاً. هذا قرار مُثبَّت ومُختبَر عبر 3B-1/2/3 بالكامل، **غير قابل لإعادة النظر ضمن 3B-4**.
- **هل يُسمح لسطر أن يحمل base amount مستقلاً عن raw×rate؟** **نعم — ثلاثة أنماط حقيقية موجودة فعلياً اليوم، لا نمط واحد**:
  1. **`raw × rate = base`** (نمط `_jline`/`add_manual_line` القياسي) — العلاقة الحسابية مباشرة وقابلة للتحقق.
  2. **`raw = base` حرفياً** (نمط `_jline_base` — لخطوط لا معنى محاسبياً لـ"قيمة أجنبية" منفصلة لها، مثل COGS/فروق الصرف نفسها).
  3. **`raw` و`base` مستقلان تماماً، بلا معدّل تحويل واحد يربطهما** (نمط `_jline_party` — سطر "مُدمَج" من عدة أجزاء بأسعار مختلفة، اكتُشف ضرورياً بـ3B-3 لتفادي Bug حقيقي مؤكَّد بـIndependent Oracle).
  **هذا التمايز الثلاثي قرار محاسبي مغلق من 3B-3، لا يُعاد فتحه أو "تبسيطه" ضمن 3B-4** — أي محاولة توحيد هذه الأنماط الثلاثة بمعدّل تحويل واحد ستكرر بالضبط الـBug الذي أصلحه 3B-3.
- **الحفاظ على القيم التاريخية بعد POSTED**: مضمون بحكم Immutability (§5) — `debit_base`/`credit_base` مُخزَّنة، لا تُعاد حسابها أبداً بعد الترحيل (نفس مبدأ "Snapshot Exchange Rate" الموثَّق صراحة بتعليق `reverse_manual_entry`).
- **قاعدة العكس العامة لـFX**: **عكس حرفي دقيق للقيم التاريخية المحفوظة نفسها** — لا إعادة حساب بسعر صرف اليوم إطلاقاً؛ `debit↔credit` يتبادلان، و`debit_base↔credit_base` يتبادلان أيضاً بنفس القيم المحفوظة حرفياً (لا ضرب بسعر جديد).
- **صراحة، لا تغيير على FX الخاص بـ3B-3**: قواعد `_post_settlement_multi`/`_post_refund` (تخصيص متعدد الأهداف، `carrying_rate` بالاسترداد، اتجاه الربح/الخسارة حسب نوع الطرف) **تبقى Domain Rules بالكامل خارج أي Core** — لم يثبت هذا الفحص أن أي جزء منها قاعدة Core عامة.

## 4. Posting Rules

- **شروط الانتقال DRAFT→POSTED**: عبور `_validate_lines()` (كل سطر صالح فردياً) **ثم** `entry.is_balanced()` (المجموع صفر بالـbase). كلا الفحصين إلزاميان معاً، بالترتيب هذا تحديداً بالكود الحالي.
- **validation قبل flush أم بعده؟** `_validate_lines`/`is_balanced()` بـ`post_manual_entry` تُستدعيان **على الأسطر الموجودة أصلاً بالذاكرة/مُتزامنة مسبقاً** (كل سطر سبق أن `flush`ت لحظة إضافته عبر `add_manual_line`) — التحقق النهائي إذن **بعد** كل `flush` فردي، **قبل** تغيير `status` نفسه.
- **هل Immediate POSTED يمر بنفس الـvalidation؟** **جزئياً فقط بالسلوك الحالي** — كل مسار آلي يطبّق `entry.is_balanced()` بنفسه (نسخة مطابقة السلوك، لا استدعاء مشترك)، لكن **لا يستدعي `_validate_lines()` إطلاقاً** (فجوة مؤكَّدة سابقاً بالفحوص الأولى، مُعاد تأكيدها هنا).
- **Atomic بين الإنشاء والترحيل؟** نعم بالسلوك الفعلي للمسارات الآلية (كل الأسطر تُبنى بالذاكرة أولاً، `entry.lines=[...]` دفعة واحدة، ثم `session.add(entry)` مرة واحدة) — **لا حالة وسيطة مكشوفة لأي كود آخر بين البناء والترحيل**. بالمسار اليدوي، العملية **ليست atomic بالتعريف نفسه** — DRAFT قابل للحفظ الجزئي بمراحل متعددة عمداً (هذا هو الغرض من DRAFT أصلاً).
- **من يملك commit/rollback؟** **المُستدعي دائماً، لا الخدمة** — لا استدعاء واحد لـ`session.commit()`/`session.rollback()` بأي دالة بـ`journal_edit.py` أو أي مسار آلي مفحوص. هذا مبدأ مُثبَّت وموثَّق صراحة بكل مرحلة سابقة (3B-1/2/3): **"نجاح الخدمة لا يعني Commit"**.
- **القاعدة الحالية تُحافَظ عليها بلا استثناء**: أي نواة موحَّدة يجب أن تستمر بعدم استدعاء `commit`/`rollback` داخلها.

## 5. Immutability Rules

- **ما الذي يمنع تعديل POSTED؟** `ensure_editable()` (فحص `status` فقط) — على المسار اليدوي. **لا حارس مماثل صريح على المسارات الآلية لأنها لا تحاول التعديل أصلاً بعد الإنشاء (لا كود يفعل ذلك، لا لأن شيئاً يمنعه فعلياً لو حاول).**
- **هل يمكن تغيير `ref_no` بعد POSTED؟** لا دليل على أي مسار يحاول ذلك؛ `UNIQUE` على العمود يمنع تكراره فقط، لا يمنع تعديل قيمته تقنياً لو استُدعي `UPDATE` مباشر (لا حارس عمل صريح ضد ذلك بمعزل عن `ensure_editable` العام).
- **هل يمكن تغيير `date` بعد POSTED؟** نفس الملاحظة — يقع تحت نفس حماية `ensure_editable` العامة بالمسار اليدوي فقط، لا حارس خاص بحقل التاريخ تحديداً.
- **هل يمكن إضافة/حذف `JournalLine` بعد POSTED؟** **لا** — `add_manual_line`/`remove_manual_line` كلاهما يستدعيان `ensure_editable()` أولاً، يرفضان فوراً إن `status != DRAFT`.
- **هل التصحيح دائماً بقيد جديد؟** **نعم، بلا استثناء واحد بكل الكود المفحوص** — لا مسار حذف أو تعديل مباشر لقيد/سطر POSTED بأي شكل. القيد الجديد إما "عكس" (`reverse_manual_entry`/`invoice_cancel.py`) أو "تصحيح مستقل" (تسوية 3B-3 الجديدة لتصحيح تخصيص خاطئ سابق).
- **لا تفترض قاعدة عكس واحدة لكل مستند**: مؤكَّد — 3 آليات منفصلة فعلياً (§9 بالتقرير السابق: عام، فاتورة، ولا شيء للتسويات) — كل نوع مستند له قاعدة عكس/تصحيح خاصة به، لا قاعدة واحدة تُفرَض على الجميع.

## 6. General Reversal Rules (العام فقط — لا إلغاء فواتير، لا تسويات)

- **الأصل يجب أن يكون POSTED**: نعم، `reverse_manual_entry` ترفض صراحة إن لم يكن كذلك.
- **الأصل لا يُعدَّل**: نعم — لا تغيير على `original_entry` نفسه بأي حقل، فقط قراءة.
- **يُنشأ قيد جديد**: نعم دائماً.
- **debit/credit تنعكس**: نعم، لكل سطر بمعزل — `debit↔credit` و`debit_base↔credit_base` يتبادلان قيمهما حرفياً (سطر بسطر، لا مجموع).
- **`currency`/`rate` التاريخية تُحافَظ عليها**: نعم — `line_currency_code`/`line_exchange_rate` يُنسَخان حرفياً بلا تعديل لكل سطر.
- **هل `exchange_rate` يُنسَخ حرفياً؟** نعم — قيد العكس نفسه يرث `currency_code`/`exchange_rate` من الأصل حرفياً (`original_entry.currency_code`/`original_entry.exchange_rate`)، بلا أي إعادة حساب بسعر اليوم.
- **هل reversal نفسه POSTED مباشرة؟** **نعم** — لا مرحلة DRAFT وسيطة لقيد العكس، يُبنى `POSTED` من لحظة الإنشاء.
- **هل يمكن عكس reversal؟** **لا، ممنوع صراحة** — فحص `original_entry.is_reversal_of is not None` يرفض فوراً.
- **هل يمكن reversal مرتين لنفس القيد؟** **لا** — فحص `existing_reversal` (بحث عن أي قيد آخر بـ`is_reversal_of=original.id` مسبقاً) يرفض المحاولة الثانية.
- **قواعد تاريخ العكس**: **لا قاعدة عمل موجودة إطلاقاً** — `reversal_date` معامل حر بلا أي تحقق (لا رفض تاريخ سابق للأصل، لا رفض تاريخ مستقبلي). **[يحتاج قراراً — لا افتراض]**.
- **هل reversal يحتاج `ref_no` مستقل؟** نعم — بادئة مخصَّصة (`"JV-REV-"`) منفصلة شكلياً عن قيود سند القيد العادية (`"JV-"`)، رغم التداخل الفعلي بالعدّاد المُكتشَف سابقاً (فحص `LIKE 'JV-%'` يحتسب `"JV-REV-%"` ضمنه أيضاً — هذه مشكلة تنفيذية موثَّقة سابقاً، لا قاعدة عمل بحد ذاتها).

## 7. Numbering Business Rules (بلا تحديد آلية تنفيذ)

- **هل كل `JournalEntry` يحتاج `ref_no`؟** نعم، بلا استثناء واحد بكل مسار مفحوص — العمود `String(30)` بلا `nullable=True`، وكل دالة إنشاء تملأه صراحة.
- **هل DRAFT يحتاج رقماً؟** **نعم، فوراً عند الإنشاء** — حتى قبل أي سطر أو أي محتوى فعلي (`_save_draft` يولّد الرقم لحظة إنشاء الكائن الفارغ). لا "حجز لاحق عند الترحيل".
- **هل POSTED يحتاج رقماً؟** نعم بالطبع — يرث نفس رقم الـDRAFT (لا رقم ثانٍ يُستبدَل به عند الترحيل بالمسار اليدوي؛ المسارات الآلية تولّد رقمها مرة واحدة فقط أصلاً لأنها POSTED من البداية).
- **هل reversal يحتاج رقماً؟** نعم، مستقلاً شكلياً (بادئة مختلفة).
- **هل manual/automatic/reversal تحتاج sequences متمايزة من منظور Business؟** **نعم — هذه قاعدة عمل واضحة من السلوك الحالي**: كل نوع مستند له بادئة (namespace) مرئية مختلفة يتوقعها المستخدم بالتقارير/البحث (`JV-` لليدوي، `JE-SAL`/`JE-RCV`/إلخ للآلي، `JV-REV-` للعكس) — **التمايز الشكلي مطلوب كقاعدة عمل**، حتى لو آلية التوليد الداخلية تُوحَّد تقنياً لاحقاً (قرار Architecture، لا يُمس هنا).
- **هل يجب أن يكون `ref_no` UNIQUE؟** **نعم، قاعدة عمل قائمة فعلياً ومفروضة بالـDB** (`unique=True`).
- **هل يمكن أن يتغيّر بعد POSTED؟** لا دليل على قاعدة عمل تسمح بذلك أو تمنعه صراحة — لم يُختبَر أي مسار يحاول هذا. **[يحتاج قراراً]**.
- **هل الرقم جزء من الهوية المحاسبية أم reference/document number؟** **Reference/Document number فقط بالنسبة لـ`JournalEntry.ref_no` أيضاً** — بنفس منطق `invoice_no` تماماً (§J بالتقرير السابق): لا FK واحد بكل المشروع يستخدم `ref_no` كمرجع علائقي؛ كل الروابط (`Settlement.journal_entry_id`، `OpeningPartyEntry.journal_entry_id`، `is_reversal_of`) تستخدم `JournalEntry.id` الرقمي حصراً.

## 8. Direct Accounting Bypass Rule

**القاعدة المطلوبة صراحة (اعتماداً على تعليمات Bilal، لا استنتاجاً من الكود وحده — الكود الحالي لا يفرضها بعد)**: **لا يجوز لأي خدمة Business إنشاء `JournalEntry`/`JournalLine` بطريقة تتجاوز حدود التحقق المحاسبي المركزية.** الخدمة تبني "المبالغ والحسابات" وفق قواعدها الخاصة، ثم تُسلِّمها لحدود الترحيل المركزية للبناء والتحقق والإقفال. **هذه قاعدة توجيهية لِما يجب أن يكون صحيحاً بعد 3B-4، لا وصفاً لواقع اليوم** (اليوم 4 من 6 مسارات تتجاوز هذا الحد فعلياً، كما وثَّق التقرير الأول).

## 9. Domain Rules That MUST Stay Outside Core

مؤكَّدة صراحة بلا استثناء واحد يظهر من الفحص: قواعد صحة فاتورة البيع/الشراء (كمية، سعر، خصم، ضريبة) · COGS · متوسط التكلفة (Average Cost) · حركة المخزون واستهلاكه · قواعد المستودعات · تصنيف عميل/مورد (`AccountSubtype`) وتحقق `allow_reconciliation` · تخصيص التسوية متعدد الأهداف (`SettlementAllocation`) وقواعد الدفعة الزائدة/الاسترداد · قواعد الرصيد الافتتاحي للعميل/المورد (توافق `kind` مع `subtype`) · أهلية الاسترداد (`carrying_rate`, فحص الإشارة) · أهلية إلغاء الفاتورة (وجود تسويات، حالة الفاتورة) · قيود العكس الخاصة بالفاتورة (تختلف عن العكس العام) · دلالة Append-Only الخاصة بالتسويات · حالة المستند التجاري نفسه (`InvoiceStatus` منفصل تماماً عن `JournalEntryStatus`).

**لا استثناء حقيقي وُجد** يستحق نقله للـCore بناءً على هذا الفحص وحده.

## 10. Compatibility with 3B-1/3B-2/3B-3 (قاعدة غير قابلة للتفاوض)

**أي اختلاف بعد 3B-4 فيما يلي يُعتبَر Regression وجوباً، لا تحسيناً**: قيم `debit`/`credit` الخام · `debit_base`/`credit_base` · فرق الصرف (`fx_amount` بكل صوره الثلاثة §3) · تقييم المخزون (`InventoryMovement.unit_cost`، `average_cost`) · رصيد الطرف (`get_party_currency_balance`) · نتائج `carrying_rate`/الاسترداد · كل رقم مُثبَّت اليوم بـ**30/30 Regression + 200/200 Fuzz** الحاليين. **المعيار العملي**: تشغيل الحزمة الكاملة بعد أي تغيير يجب أن يُنتِج **نفس الأرقام تماماً**، لا فقط "لا أخطاء".

## 11. Python vs DB Rules — تصنيف دقيق (مطلوب صراحة، لا نظري)

| القاعدة | Python | DB | كلاهما | Domain |
|---|---|---|---|---|
| حساب موجود (FK صالح) | لا تحقق صريح | نعم (FK constraint ضمني) | — | — |
| حساب Active | **لا يوجد بأي مستوى خدمة** | لا | — | UI فقط اليوم |
| حساب ليس Group | **لا يوجد بأي مستوى خدمة** | لا | — | UI فقط اليوم |
| لا مدين ودائن معاً | نعم (`add_manual_line`/`_validate_lines`) | نعم (`ck_debit_xor_credit`) | **كلاهما، متفقان** | — |
| **لا سطر (0,0)** | نعم (Python فقط) | **لا — الـCHECK الحالي يسمح به صراحة** | — | — |
| لا قيم سالبة | **لا تحقق Python صريح** | نعم (ضمن نفس `ck_debit_xor_credit`) | — | — |
| توازن القيد (base) | نعم (`is_balanced()`) | لا (لا CHECK على مستوى القيد كوحدة) | — | — |
| POSTED غير قابل للتعديل | نعم (`ensure_editable`) — يدوي فقط | لا | — | — |
| `ref_no` فريد | — | نعم (`UNIQUE`) | — | — |
| منع عكس مزدوج/عكس-العكس | نعم (استعلام صريح) | لا | — | — |
| Idempotency/تخصيص/FX الخاص بالتسوية | — | — | — | **Domain بالكامل** |

**أهم صفين بالجدول (طُلب فحصهما صراحة)**: **(0,0) مسموح بالـDB، ممنوع بـPython فقط** — تحقق **حقيقي وليس نظرياً** يُثبت أن حماية المسارات الآلية الأربعة (التي لا تمر بـPython هنا) **غير كاملة اليوم فعلياً**، لا افتراضاً محتملاً. والقيم السالبة: العكس تماماً — الـDB هو الحارس الوحيد الفعلي هنا، Python لا يفعل شيئاً.

## 12. Atomicity and Transaction Boundary (Business Rule فقط)

- **ما الذي يجب أن يكون Atomic؟** بناء قيد + كل أسطره + تحديث حالته لـ`POSTED` — يجب أن تنجح معاً أو لا شيء منها يُثبَّت (`commit`).
- **عند فشل التحقق**: لا شيء يُثبَّت بقاعدة البيانات بشكل دائم — لكن **الخدمة نفسها لا تضمن هذا وحدها** (لا `rollback` داخلها)؛ الضمان الفعلي يعتمد على أن **المُستدعي** يستدعي `session.rollback()` عند التقاط الاستثناء (نمط مُتَّبع بكل الاختبارات/الواجهة المفحوصة، **لكنه اتفاقية لا قاعدة مفروضة بالكود نفسه**).
- **هل يجوز أن يبقى `JournalEntry`/`JournalLine` جزئياً بالذاكرة (session) دون commit؟** نعم مؤقتاً — `flush()` (لا `commit()`) يُستخدَم بكل مكان لأخذ `id`؛ الصفوف **موجودة بالـtransaction الحالية فقط**، تختفي بـ`rollback`.
- **من يملك `transaction`/`commit`؟** **المُستدعي دائماً** — قاعدة مُثبَّتة ومُكرَّرة صراحة بكل مرحلة سابقة (3B-1/2/3)، **تُحافَظ عليها حرفياً بلا استثناء**.

## 13. Required Regression Invariants

`Σdebit_base == Σcredit_base` لكل قيد POSTED · لا قيد `POSTED` غير متوازن بأي حال · لا `JournalLine` بقيمتين موجبتين معاً (مفروض DB) · **حالياً: `JournalLine` بقيمتين صفريتين ممكن فعلياً بالمسار الآلي — إن أُضيف حارس جديد لهذا لاحقاً، يجب ألا يرفض أي سطر (0,0) كان موجوداً مسبقاً بقيود POSTED تاريخية (إن وُجدت) بأثر رجعي** · لا `debit`/`credit` سالبة (مفروض DB) · لا حساب Group/Inactive بقيد جديد (**قاعدة جديدة تُضاف بـ3B-4، غير موجودة اليوم — ليست invariant قائماً بل هدفاً جديداً**، يجب عدم الخلط بينها وبين الـinvariants القائمة فعلياً) · `POSTED` غير قابل للتعديل (يدوياً مُثبَّت، آلياً بالسلوك فقط) · العكس لا يغيّر الأصل مطلقاً · العكس يحافظ على القيم التاريخية حرفياً (لا إعادة حساب) · نتائج 3B-1/3B-2/3B-3 المحاسبية (§10 أعلاه) · **لا `commit`/`rollback` داخل أي دالة خدمة محاسبية**.

## 14. Ambiguous/Open Business Decisions (لا تخمين — قرارك مطلوب)

1. **قاعدة تاريخ العكس (`reversal_date`)**: لا قاعدة عمل موجودة اليوم تمنع تاريخاً سابقاً لتاريخ الأصل أو تاريخاً مستقبلياً. هل يُفرَض قيد جديد ضمن 3B-4، أم يبقى حراً كما اليوم؟
2. **تغيير `ref_no`/`entry_date` بعد POSTED**: لا قاعدة صريحة تمنع أو تسمح (خارج الحماية العامة لـ`ensure_editable` بالمسار اليدوي فقط) — هل تحتاج قاعدة صريحة مستقلة، أم تكفي حماية `ensure_editable` العامة الحالية؟
3. **حالة `JournalLine(debit=0, credit=0)` القائمة فعلياً لو وُجدت ببيانات سابقة**: هل نمنعها فقط للقيود الجديدة (Forward-only rule)، أم نحتاج فحص/تنظيف للبيانات القائمة أولاً (خارج نطاق الأثر الرجعي هنا لكن يستحق طرحه)؟
4. **هل "منع Group/Inactive account" يُطبَّق فقط على القيود الجديدة بعد 3B-4، أم يحتاج فحصاً استعادياً على أي قيود POSTED قائمة قد تحتوي حسابات أصبحت Group/Inactive لاحقاً (تغيّرت حالتها بعد الترحيل)؟** الكود الحالي لا يمنع تغيير حالة حساب (`is_active`/`is_group`) بعد استخدامه بقيود POSTED قديمة — هل هذا سلوك مقبول يبقى، أم قاعدة جديدة مطلوبة هنا أيضاً؟
5. **`rate=0` أو `rate` سالب على مستوى `JournalLine`/`JournalEntry` نفسيهما**: لا `CHECK`/تحقق Python يمنع هذا اليوم على مستوى `JournalLine`/`JournalEntry` (بخلاف `Settlement`/`OpeningPartyEntry` اللذين يملكان `CHECK rate>0` من 3B-3) — هل هذه فجوة يجب سدّها بـCore Rule جديدة ضمن 3B-4، أم تُترَك (باعتبار كل مسار مسؤولاً عن تمرير سعر صحيح ضمنياً)؟
6. **هل "منع (0,0)" يجب أن يصبح Core Rule مفروضة أيضاً بمستوى الـDB (تعديل الـCHECK)، أم يكفي فرضها Python-side ضمن الحدود المركزية الجديدة فقط؟** هذا قرار Architecture أصلاً (تعديل Schema أم لا)، لكن Business Rule الأساسية ("يجب أن تُمنَع دائماً، بلا استثناء لأي مسار") محسومة هنا — آلية الفرض تفصيل لاحق.

---

**لا Architecture، لا Design Spec، لا كود بهذا التسليم. قواعد عمل فقط، بعضها مؤكَّد من الكود صراحة، وبعضها مطروح صراحة كقرار مفتوح بـ§14 — بانتظار حسمك قبل الانتقال لـArchitecture.**
