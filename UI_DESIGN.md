# UI_DESIGN.md — مرجع تصميم الواجهة

> هذا الملف يحفظ **رؤية كاملة طويلة الأمد** للواجهة (من نقاش خارجي مفصّل)،
> بالإضافة إلى **نطاق v1 المصغّر المعتمد فعلياً للتنفيذ الآن**. لا تُنفَّذ
> أي عناصر خارج قسم "نطاق v1" بدون قرار صريح جديد.

---

## نطاق v1 — المعتمد للتنفيذ الآن (لا أكثر)

- عربي فقط، RTL ثابت. **بدون** نظام ترجمة (`tr()`) أو تبديل لغة/اتجاه
- تخطيط مرن بسيط عبر Qt layouts، بدون اختبار رسمي على دقات شاشة متعددة
- قائمة تنقل رئيسية واحدة (بدون "خريطة قسم" كطبقة منفصلة)
- تبويبات عمل (Workspace Tabs) لفتح أكثر من شاشة بنفس الوقت — عنصر مفيد فعلياً، معتمد
- الفواتير وسند القيد: **Grid-first** — جدول بنود حقيقي، سطر أخير دائماً جاهز للإدخال
- تنقل لوحة مفاتيح أساسي: `Enter` (سطر تالي), `Tab` (خلية تالية), `Insert` (سطر جديد), `Delete` (حذف سطر), `Esc` (إلغاء)
- Search-select سريع للمادة/الحساب/العميل (كتابة + قائمة منسدلة، بدون dialog منفصل)
- **كل عملية إنشاء/تعديل تستدعي `app/services/` مباشرة — صفر منطق محاسبي بالواجهة** (القاعدة الثابتة الموجودة أصلاً)
- منع تعديل مستند POSTED **من الواجهة نفسها** (تعطيل حقول الإدخال)، بالإضافة للحاجز الموجود أصلاً بالـservice

## مؤجَّل عمداً — ليس رفضاً نهائياً، فقط ليس الآن

- نظام ثنائي اللغة (عربي/إنجليزي) + RTL/LTR ديناميكي — **يُبنى فقط لو ظهر احتياج فعلي مؤكد** (نفس معيار تعدد المستودعات)
- Top Navigation + Section Map كطبقتين منفصلتين — نبدأ بطبقة تنقل واحدة، نضيف الطبقة الثانية لو الواجهة فعلاً صارت مزدحمة
- اختبار رسمي على دقات شاشة متعددة
- Dashboard مخصص بالصفحة الرئيسية (نبدأ بشاشة رئيسية بسيطة)

---

## المرجع الكامل (كما وصل من النقاش الخارجي، بدون تعديل)

راجع محضر المحادثة الأصلي للتفاصيل الكاملة: فلسفة الواجهة، الشريط العلوي
والقوائم المنسدلة، خريطة الأقسام، Workspace Tabs، تصميم شاشة الفاتورة
وسند القيد بنمط Grid، تفاصيل التنقل بلوحة المفاتيح (Enter/Tab/Insert/
Delete/Esc/Ctrl+Enter للترحيل)، دليل الحسابات كـTree View، دليل المواد
وبطاقة المادة، شاشة المستودعات والتحويل المخزني، شاشة التقارير الموحّدة،
الإعدادات بتابات فرعية، ونظام الترجمة والـRTL/LTR الكامل.

**هذا القسم مرجع تخطيطي فقط — التنفيذ الفعلي يلتزم بقسم "نطاق v1" أعلاه حصراً.**

---

## §45 — تدقيق واقعي: الواجهة الحالية موجودة فعلاً، ليست بدءاً من الصفر

اكتشاف مهم أثناء تحضير "مواصفة UI الفاتورة": **الواجهة موجودة ومبنية
فعلياً** (`app/ui/common/document_form.py`، 623 سطر) — هذا الملف نفسه
يوثّق بالتعليق الأول: *"فاتورة البيع هي المرجع البصري والسلوكي لكل
مستند آخر (طلب المستخدم صراحة)"*. قسم "نطاق v1" أعلاه بهذا الملف هو
المواصفة المعتمدة أصلاً. اختبرت `SalesInvoiceFormView` فعلياً (إنشاء
حقيقي عبر PySide6 offscreen) — يعمل بلا استثناء مع كل تعديلات هذه
الجلسة على المحرك.

### تدقيق النقاط الاثنتي عشرة مقابل الكود الفعلي

| # | المطلوب | الحالة | الدليل |
|---|---|---|---|
| 1 | فاتورة البيع كقالب أساسي | ✅ موجود فعلاً | `BaseDocumentFormView` + تعليق الملف صراحة |
| 2 | الحقول وترتيب Tab/Enter | ✅ موجود | `_move_to_next_cell()`، `eventFilter` |
| 3 | بحث العميل/المادة | ✅ موجود (نص حر + مطابقة SKU) | `party_edit`، `_on_cell_changed` |
| 4 | العملة/سعر الصرف/المعادل الأساسي | ✅ موجود | `currency_combo`+`exchange_rate_spin`+`_recalculate_totals` |
| 5 | الأصناف/الكميات/الحسم/الضريبة | ✅ موجود | `GRID_COLUMNS`، `_build_transient_invoice` |
| 6 | الإنشاء والترحيل + منع تعديل POSTED | ✅ موجود | `_save_draft`/`_post`/`_refresh_editability` |
| 7 | المرتجع المرتبط + إدخال يدوي | ✅ موجود | `is_return`، `original_ref_edit`، `_load_from_original` |
| **8** | **الرصيد المستحق والتسويات** | ❌ **غير موجود** | Settlement بُنيت هذه الجلسة، بعد هذه الواجهة |
| 9 | Validation قبل الترحيل | ✅ موجود | `try/except (EditNotAllowedError, InvoiceValidationError)` |
| 10 | اختصارات لوحة المفاتيح | ⚠️ **جزئي** | Enter/Tab/Insert/Delete/Ctrl+S/Ctrl+Enter موجودة، **Esc غير مُطبَّق** |
| **-** | **Cancel/Reverse** | ❌ **غير موجود إطلاقاً** | `cancel_invoice()` بُنيت هذه الجلسة أيضاً |
| 12 | Mockup/مواصفة سلوكية | ✅ موجود (هذا الملف نفسه) | — |

**الخلاصة**: 9 من 12 نقطة مُغطّاة فعلاً بعمل حقيقي سابق. الفجوات
الحقيقية الوحيدة هي بالضبط ما بُني حديثاً بهذه الجلسة ولم تكن الواجهة
تعرفه أصلاً: **عرض الرصيد المستحق/التسويات (نقطة 8)**، **Cancel/Reverse**
(ضروري بعد §44 رغم عدم وجوده برقم بالقائمة الأصلية)، و**زر Esc**.

### إضافة للمواصفة (v1.1) — الفجوات الثلاث فقط، لا إعادة تصميم لما يعمل

**أ) Esc — إلغاء التعديل الحالي دون حفظ**، يعيد الخلية لقيمتها السابقة
(`closePersistentEditor` بلا commit)، متسق مع `_close_current_editor`
الموجودة أصلاً.

**ب) عرض الرصيد المستحق (نقطة 8)** — بطاقة صغيرة أسفل رأس المستند، تظهر
**فقط للفواتير الآجلة المرحّلة POSTED** (لا معنى لها لمسودة أو فاتورة
نقدية):
```
┌─────────────────────────────────────────────┐
│ الرصيد المستحق: 600.00 USD                    │
│ (من إجمالي 1,000.00 USD — 3 تسويات سابقة)     │
│ [عرض التسويات]  [تسجيل قبض جديد]              │
└─────────────────────────────────────────────┘
```
`get_invoice_balance_due()` مباشرة — صفر منطق حسابي بالواجهة. **زر
"تسجيل قبض/دفع جديد" يفتح حواراً منفصلاً بسيطاً** (لا شاشة كاملة):
مبلغ (`amount_foreign`) + تاريخ + سعر صرف التسوية + حساب الصندوق/البنك
→ يستدعي `post_receipt()`/`post_payment()` مباشرة. **D-2 (تفصيل
العملات) لا يظهر هنا كما اتُّفق — رقم واحد بعملة الفاتورة فقط.**

**ج) زر Cancel/Reverse — منفصل تماماً عن زر المرتجع** (كما شدَّد صديق
المبرمج: لا زر واحد "إلغاء/مرتجع"):
```
[ترحيل]  [مرتجع]  [إلغاء الفاتورة]  ← ثلاثة أزرار منفصلة، لا اثنان
```
يظهر فقط لفاتورة POSTED بلا تسويات. عند النقر: تأكيد صريح ("سيُعكس هذا
المستند بالكامل بقيمه الأصلية — لا يمكن التراجع")، يستدعي
`cancel_invoice()`. إن رُفض (تسوية موجودة/ملغاة أصلاً) → رسالة الخطأ من
`CancelNotAllowedError` تُعرض حرفياً (نفس نمط `_post` الحالي تماماً).

### القرار
لا حاجة لإعادة بناء شاشة الفاتورة. الخطوة التالية: إضافة الفجوات
الثلاث فقط فوق `BaseDocumentFormView` الموجودة، ثم اختبار الشاشة الكاملة
مع الخدمات الحقيقية (نفس نمط `test_qt_editor_lifecycle.py` الموجود
أصلاً)، ثم Regression كامل.

---

## §51 — تنفيذ الفجوات الثلاث (v1.1 → واقع مُختبَر)

نُفِّذت الفجوات الثلاث فوق `BaseDocumentFormView` الموجودة دون أي إعادة
بناء، بقرارين صريحين من Bilal يخالفان تفصيلاً بسيطاً في مسودة v1.1 أعلاه
(مُسجَّلان هنا بدل ترك الوثيقة القديمة موحية بسلوك غير الفعلي):

**Settlement**: نافذة `SettlementDialog` منبثقة منفصلة تماماً (لا تبويب
داخل نموذج الفاتورة كما اقتُرح ضمناً أعلاه)، تُفتح من زر "تسوية
(قبض/دفع)". تعرض الفاتورة/الرصيد/العملة/المبلغ/حساب القبض أو الدفع/سعر
الصرف، تستدعي `post_receipt`/`post_payment` مباشرة، وتُحدِّث الرصيد
المعروض بنموذج الفاتورة فور النجاح.

**Cancel**: خلافاً لمسودة v1.1 ("يظهر فقط لفاتورة POSTED **بلا
تسويات**") — القرار النهائي المعتمد: الزر يظهر ويبقى مفعّلاً على أي
فاتورة POSTED **بصرف النظر عن وجود تسويات**؛ الرفض (لو وُجدت تسوية)
يأتي حصراً من `cancel_invoice()` عبر `CancelNotAllowedError`، لا من
إخفاء الزر بمنطق UI. السبب: قرار السماح/المنع يجب أن يبقى في Business
Logic فقط.

**Esc**: نُفِّذ فعلياً بمعالجة صريحة داخل `NumericGridDelegate.eventFilter`
(`RevertModelCache`) بدل الاعتماد الضمني على سلوك كيوت الافتراضي.

اختبار تكاملي حقيقي عبر الواجهة الفعلية (`tests/test_ui_settlement_and_cancel.py`)
يغطي: فتح Settlement Dialog، رفض تسوية DRAFT، تسوية جزئية، تسوية كاملة،
تحديث الرصيد الفوري، رفض Cancel مع تسوية مرتبطة، نجاح Cancel بلا تسويات،
وعزل الفواتير الأخرى — 21/21 تحققاً. بالإضافة لبق حقيقي اكتُشف أثناء
البناء: `get_invoice_balance_due()` لا تتحقق من حالة الفاتورة (DRAFT)
أصلاً، فحُلَّ بفحص صريح داخل `SettlementDialog` قبل استدعائها (لا تعديل
على الخدمة نفسها — الفحص يخص عرض الواجهة فقط).

gate() PASS — 19/19 regression + 200/200 fuzz.
