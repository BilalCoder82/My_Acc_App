# تقرير المراجعة النهائي — قبل مواصلة واجهات الفواتير
تحديث: بعد §46-§48 (عزل المستودعات، ربط UI، المراجعة الشاملة)
Baseline المرجعي: `v2-accounting-engine-with-alembic-and-inventory-integrity`

مبدأ هذا التقرير: كل تصنيف مبني على فحص فعلي للكود والاختبارات الحالية،
لا على نسخة سابقة من هذا الملف. **هذا الملف استبدال كامل**، لا إضافة —
النسخة السابقة كانت منحرفة عن الواقع (توثيق Settlement/Cancel كفئة C
رغم تنفيذهما لاحقًا بالكامل)؛ لا يجوز الوثوق بنسخة أقدم من هذا الملف.

---

## A — مكتمل ومثبت بالاختبار

| البند | الدليل |
|---|---|
| ترحيل شراء/بيع بعملات متعددة، تمييز سعر الوحدة عن سعر الصرف | `test_aggressive_currency_inventory.py`، `test_full_inventory_lifecycle.py` |
| Weighted Average عبر مشتريات متعددة | Oracle مستقل، 200 سيناريو fuzz |
| مرتجع بيع/شراء بالتكلفة التاريخية، لا المتوسط الحالي | §39، `test_inventory_unit_cost_source_of_truth.py` |
| `InventoryMovement.unit_cost` = مصدر الحقيقة الوحيد لحركات الخروج | إصلاح §39، 4 مصادر مستقلة |
| فاتورة متعددة المواد بحسابات منفصلة كليًا | `test_aggressive_currency_inventory.py` مجموعة 4 |
| منع تعديل/حذف مستند POSTED | `invoice_edit.py`/`journal_edit.py` |
| **خدمة قبض/دفع مخصصة (`post_receipt`/`post_payment`)** | §42-43، **26 تحققًا Oracle مستقل**، 12 حالة مطلوبة + AP |
| **رابط بنيوي Invoice→Settlement→Receipt/Payment (لا `balance_due` مخزَّن)** | `get_invoice_balance_due()` ديناميكي دائمًا، §42.1 |
| **فرق الصرف: حسابان منفصلان (ربح/خسارة)، لا يُحسب مرتين** | §42.3، مُختبَر صراحة (`test_settlement_fx.py` حالة 12) |
| **منع تجاوز الرصيد المستحق / تسوية فاتورة ملغاة أو غير مرحّلة** | §42.4، مُختبَر |
| **تسلسل تسويات متعددة (قبض/دفع جزئي متكرر بأسعار مختلفة)** | §48، Oracle تراكمي، `test_comprehensive_review.py` |
| **Cancel/Reverse منفصل تمامًا عن Return** | §44، `cancel_invoice()`، 15 تحققًا: عكس حرفي، رفض مع تسوية مرتبطة، رفض الإلغاء المزدوج |
| **التكلفة/COGS/قيمة المخزون منفصلة لكل مستودع (لا موحّدة على الشركة)** | §46، 12+ تحققًا Oracle مستقل، نقل A→B وB→A كلاهما |
| **`_average_cost()` تفرض `warehouse_id` إلزاميًا — لا سقوط صامت** | §46.3 |
| **ComboBox المستودع بالواجهة، لا اختيار صامت، مرتجع مرتبط يرث ويُقفَل** | §47، اختبار UI فعلي (لا Mock)، 9 تحققًا |
| Alembic مدمج بمسار التطبيق الحقيقي | §33 |
| تطابق ميزان المراجعة مع محرك المخزون (warehouse-aware) | §48، `test_comprehensive_review.py` |
| Regression + Fuzz/Oracle + Gate آلي | **17/17 + 200/200 حاليًا** |
| Baseline قابل للاستعادة من بيئة نظيفة | v1 وv2 |

## B — موجود لكن يحتاج اختبارًا إضافيًا

| البند | الفجوة تحديدًا |
|---|---|
| قائمة الدخل/الميزانية العمومية | موجودتان، مُختبرتان لسيناريو E2E واحد + تطابق أساسي بـ§48؛ لا Oracle مستقل مخصص شامل لحالات عملات معقدة |
| حساب واحد بخطوط عملات مختلطة (عملاء/موردين تحديدًا) | الآلية سليمة تقنيًا (`debit_base`/`credit_base` حصرًا) لكن غير مُختبَرة بكثافة لكل نوع حساب |
| منع التلاعب بتسوية موجودة (تعديل/حذف) | لا توجد دالة `update_settlement`/`delete_settlement` أصلًا — الحماية الحالية هي **غياب أي مسار خدمة شرعي للتعديل** (نفس قوة حماية `ensure_editable` للمستندات الأخرى)، لكن غير مُختبَرة بحالة صريحة تحاول التلاعب المباشر |

## C — غير مكتمل (ضمن نطاق النظام، لم يُنفَّذ بعد)

| البند | التفصيل |
|---|---|
| حذف `app/migrations/runner.py` نهائيًا | مؤجَّل عمدًا (§34)، يحتاج فترة مراقبة + موافقة موثَّقة |
| **`base_currency` داخل قاعدة الشركة نفسها** | موجود فقط بـ`registry.db` المنفصلة (`CompanyRecord.base_currency`) — راجع القسم أدناه، **اكتشاف جديد بـ§48** |

## D — قرار محاسبي يحتاج حسمًا (محدَّثة — 3 نقاط فقط بقيت مفتوحة)

1. **عملة الحساب الوصفية** (`Account.currency_code`): لا تزال غير مفروضة
   برمجيًا (وصفية بحتة). هل تُفرض مستقبلًا؟
2. **عرض رصيد حساب متعدد العملات تفصيليًا لكل عملة**: مؤجَّل، موثَّق.
3. **IAS 21 — إعادة التقييم الدورية للأرصدة الأجنبية غير المسواة**:
   مؤجَّلة صراحة، موثَّقة، منفصلة عن فرق الصرف المحقَّق عند التسوية
   (المُنفَّذ فعليًا).

~~D-3 (متى تُبنى خدمة القبض/الدفع)~~ — **محسومة**: بُنيت (§42-43).
~~D-4 (نموذج التسوية الجزئية)~~ — **محسومة**: `Invoice→Settlement→Receipt/Payment` (§42.1).
~~D-6 (Cancel مقابل Return)~~ — **محسومة**: منفصلان تمامًا (§44).

---

## اكتشاف جديد (§48): لا `base_currency` داخل قاعدة الشركة نفسها

بحثت في الكود مباشرة: `base_currency` موجود فقط كعمود بـ`CompanyRecord`
داخل **`registry.db`** (قاعدة منفصلة تمامًا عن قاعدة كل شركة). **لا يوجد
أي حقل أو إعداد مكافئ داخل قاعدة بيانات الشركة نفسها** — أي أن خدمات
الترحيل والحسابات لا تملك طريقة رسمية لمعرفة عملتها الأساسية الحقيقية
من داخل قاعدتها هي.

**التصنيف حسب الخطورة (بحث فعلي بالكود، لا افتراض)**:

- **خطر حالي (منخفض الأثر، لكن حقيقي)**: `create_default_chart_of_accounts()`
  تُنشئ **كل** حسابات الشركة الافتراضية بـ`currency_code="SYP"` دائمًا
  (لا تستقبل عملة الشركة الحقيقية من أي مكان) — بصرف النظر عن
  `base_currency` الفعلي المُسجَّل بـ`registry.db` وقت إنشاء الشركة.
  الأثر محدود حاليًا لأن `Account.currency_code` وصفي فقط (D-1 أعلاه،
  غير مفروض بأي منطق فعلي) — فهو خطأ في **التسمية**، لا في **الحساب**.
- **Technical Debt يمكن تأجيله بأمان**: `journal_voucher_form.py:602`
  (`base_currency = "SYP"` لتنسيق عرض إجمالي القيد اليدوي فقط — التوازن
  الفعلي يُحسَب من `debit_base`/`credit_base` بصرف النظر عن هذا المتغير،
  فالخلل عرضي/تجميلي بحت، لا محاسبي).
- Combo boxes بالواجهات تضع SYP كأول خيار افتراضي — تفصيل UX، لا خطر.

**لم أنفّذ أي إصلاح لهذا** — يحتاج قرارًا: هل يُضاف `Setting` لعملة
الشركة الأساسية داخل قاعدتها هي (مُتزامن مع `registry.db` وقت الإنشاء)،
أم يبقى الوضع كما هو لأن كل عملاء المشروع الحاليين والمُتوقَّعين قريبًا
يستخدمون SYP فعليًا؟

---

## الخلاصة

لا نقطة في فئة C تمنع الاستمرار بواجهات الفواتير. فئة D تقلّصت من 6
نقاط إلى 3 (النصف محسوم فعليًا). الاكتشاف الوحيد الجديد بهذه الجلسة
(غياب `base_currency` داخل قاعدة الشركة) خطره حالي منخفض ومحدود التأثير
(تسمية لا حساب)، ومُوثَّق لا مُصلَح — قرار مستقبلي منفصل.
