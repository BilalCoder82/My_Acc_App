# تقرير المراجعة النهائي قبل UI الفواتير
تاريخ: بعد Baseline v2 (`v2-accounting-engine-with-alembic-and-inventory-integrity`)

مبدأ هذا التقرير: كل تصنيف مبني على فحص فعلي للكود والاختبارات، لا على
الذاكرة أو الافتراض. حيث وُجد تناقض مع ما قلته سابقًا في هذه المحادثة،
أُصحِّحه صراحة أدناه بدل تجاهله.

---

## تصحيح مهم أولاً

قلت سابقًا: *"لا توجد أي وحدة receipts/payments/settlement/fx_gain_loss
بالمشروع حالياً إطلاقاً"*. هذا **غير دقيق**. الصحيح:

> **المحرك المحاسبي (الأستاذ العام) يدعم تمثيل القبض والدفع وتسوية
> العملات وفروقات الصرف بشكل صحيح فعلاً** — مُثبَت رياضيًا ومُختبَر في
> `tests/test_e2e_scenario.py` (المرحلتان 9 و10: قبض من عميل، دفع جزئي
> لمورد بعملة الفاتورة نفسها، تحويل USD↔EUR وUSD↔SYP، وتسوية التزام
> بالدولار بسعر صرف مختلف عن سعر التسجيل مع حساب خسارة صرف حقيقية
> 60,000 SYP ورصيد الالتزام يُطفأ بالضبط لصفر).
>
> **ما هو غير موجود فعلاً**: طبقة خدمة مخصصة (مثل `post_purchase_invoice`
> و`post_sales_invoice`) تبني هذا القيد **تلقائيًا** انطلاقًا من "فاتورة +
> مبلغ تسوية + عملة/سعر صرف التسوية". حاليًا، كل قبض/دفع بعملة مختلفة
> يتطلب من الذي يُنشئ القيد (مطوّر الاختبار هنا، أو مستخدم واجهة مستقبلية)
> حساب فرق الصرف يدويًا بنفسه — لا يوجد `calculate_fx_difference()` ولا
> `post_receipt()` ولا `post_payment()` ولا رابط بنيوي بين قيد التسوية
> والفاتورة الأصلية (لا `invoice.paid_amount`، لا `invoice.balance_due`).

هذا فرق جوهري بين "الآلة المحاسبية تعمل بشكل صحيح" (نعم) و"يوجد نظام
قبض/دفع جاهز لبناء واجهة عليه" (لا). التصنيف التفصيلي أدناه.

---

## A — مكتمل ومثبت بالاختبار

| البند | الدليل |
|---|---|
| ترحيل شراء/بيع بعملات متعددة (SYP/USD/EUR)، مع تمييز سعر الوحدة عن سعر الصرف | `test_aggressive_currency_inventory.py` مجموعة 1-2، `test_full_inventory_lifecycle.py` |
| المتوسط المرجّح (Weighted Average) عبر مشتريات متعددة | Oracle مستقل، `test_accounting_fuzz_oracle.py` (200 سيناريو) + الاختبارات أعلاه |
| مرتجع بيع يستخدم التكلفة التاريخية للبيع الأصلي، لا المتوسط الحالي | `test_aggressive_currency_inventory.py` مجموعة 3 |
| مرتجع شراء مرتبط يستخدم تكلفة الشراء الأصلي، حتى بسعر صرف مختلف عن المرتجع | `test_aggressive_currency_inventory.py` + `test_inventory_unit_cost_source_of_truth.py` |
| **`InventoryMovement.unit_cost` = مصدر الحقيقة الوحيد لحركات الخروج (لا إعادة تسعير بأثر رجعي)** | إصلاح §39، مُختبَر بـ4 مصادر مستقلة (24+40 تحققًا) |
| فاتورة متعددة المواد بحسابات Inventory/COGS/Sales منفصلة كليًا | `test_aggressive_currency_inventory.py` مجموعة 4 |
| منع تعديل/حذف فاتورة POSTED (`ensure_editable`) | `invoice_edit.py`، مُختبَر مباشرة |
| منع تعديل قيد يدوي POSTED، وعكسه (`reverse_manual_entry`)، ومنع عكسه مرتين | `journal_edit.py`، مُختبَر مباشرة |
| قبض/دفع/تسوية عملات مختلفة/فرق صرف حقيقي — **كآلية GL يدوية، سيناريو واحد شامل** | `test_e2e_scenario.py` مراحل 9-10 (راجع "تصحيح مهم" أعلاه لحدود هذا التصنيف) |
| دمج Alembic في مسار التطبيق الحقيقي (عميل جديد/قديم/مستقبلي/تالف/متزامن) | §33، 5 حالات + مسار تطبيق كامل |
| Regression + Fuzz/Oracle مستقل + Gate آلي | §31-§39، 10/10 + 200/200 حاليًا |
| Baseline قابل للاستعادة من بيئة نظيفة (مُتحقَّق فعليًا، لا بافتراض وجود tag) | v1 وv2 كلاهما |

## B — موجود لكن يحتاج اختبارًا إضافيًا

| البند | الفجوة الاختبارية تحديدًا |
|---|---|
| قائمة الدخل (`get_income_statement`) والميزانية العمومية (`get_balance_sheet`) | موجودتان ومُختبرتان في E2E لسيناريو واحد فقط؛ لا Oracle مستقل مخصص لهما، ولا اختبار لحالات عملات متعددة معقدة تصل لهما |
| حساب واحد بخطوط بعملات مختلطة (USD+EUR+SYP على نفس الحساب) | الآلية سليمة تقنيًا (`get_account_statement` يستخدم `debit_base`/`credit_base` حصرًا، لا جمعًا خام) — لكن غير مُختبَر بكثافة لكل أنواع الحسابات (عملاء تحديدًا، موردين، بنوك) بتنويعات كثيرة |
| القبض/الدفع/فروقات الصرف عبر قيود يدوية | مُثبَتة العمل في سيناريو واحد مصمَّم بعناية؛ غير مُختبَرة بـFuzz أو Oracle مستقل عبر تنويعات (تسوية جزئية، عملات متعددة متتالية، إلخ) |
| تسوية بسعر صرف مختلف عن الفاتورة الأصلية (بدون FX gain/loss منفصل، فقط اختلاف تحويل) | مغطاة جزئيًا ضمن أمثلة القبض/الدفع بـE2E، غير مُعمَّمة |

## C — غير مكتمل (موجود ضمن نطاق النظام، لم يُنفَّذ بعد)

| البند | التفصيل |
|---|---|
| **خدمة قبض/دفع مخصصة** (`post_receipt`/`post_payment` أو ما يعادلهما) | لا وجود لها؛ القيد يُبنى يدويًا بالكامل حاليًا (راجع "تصحيح مهم") |
| **رابط بنيوي بين فاتورة ومقبوضاتها/مدفوعاتها** | لا `invoice.paid_amount` ولا `invoice.balance_due` ولا أي حقل/جدول يربط قيد تسوية بفاتورة معيّنة (بعكس المرتجعات التي تملك `original_invoice_id` فعليًا) |
| **آلية تسوية جزئية (Partial Settlement) متعددة الدفعات لفاتورة واحدة** | لا تتبّع "المتبقي" على الفاتورة إطلاقًا |
| **حساب فرق الصرف تلقائيًا** (`calculate_fx_difference` أو ما يعادلها) | يُحسَب يدويًا حاليًا من قِبل من يبني القيد (كما بـE2E)؛ لا دالة خدمة تفعل هذا |
| **إلغاء/عكس فاتورة كاملة بمعزل عن آلية المرتجع** (`cancel_invoice`) | الآلية الوحيدة الموثَّقة رسميًا هي `post_return()` (مرتجع جزئي أو كلي)؛ لا مسار "إلغاء صريح" منفصل بمنطق مختلف |
| حذف `app/migrations/runner.py` نهائيًا | مؤجَّل عمدًا (§34)، يحتاج فترة مراقبة + موافقة موثَّقة لاحقًا |

## D — قرار محاسبي يحتاج حسمًا (لم أفترض أي إجابة — أسئلة مفتوحة فقط)

1. **عملة الحساب الوصفية** (`Account.currency_code`): موثَّقة بالكود صراحة
   كحقل **غير مفروض برمجيًا** — أي قيد بأي عملة يُقبَل على أي حساب حاليًا.
   هل يُفرض هذا مستقبلًا (رفض قيد USD على حساب مُعلَن كـSYP مثلًا)، أم
   يبقى الحقل وصفيًا بحتًا للعرض فقط؟

2. **عرض رصيد حساب متعدد العملات**: حاليًا يُعرض بالعملة الأساسية فقط
   (`debit_base`/`credit_base`) عبر `get_account_statement`. هل يُطلب
   مستقبلًا عرض تفصيلي لكل عملة على حدة أيضًا (مثلًا: "هذا العميل مدين
   لنا بـ200 USD و150,000 SYP" منفصلين، لا معادلًا أساسيًا واحدًا فقط)؟

3. **متى تُبنى خدمة القبض/الدفع**: قبل UI الفواتير (كجزء من هذه المرحلة
   نفسها)، أم تُؤجَّل بالكامل وتُبنى شاشات الفواتير أولًا بمعزل عنها
   (فواتير آجلة بلا تتبّع سداد حتى إشعار لاحق)؟

4. **نموذج التسوية الجزئية**: هل يدعم النظام دفعات جزئية متعددة لفاتورة
   واحدة (يتطلب تصميم `balance_due` وربطًا بنيويًا)، أم كل فاتورة تُسوَّى
   دفعة واحدة كاملة فقط في هذه المرحلة؟

5. **توقيت ربح/خسارة الصرف**: فقط عند التسوية الفعلية (كما في مثال
   E2E — "realized FX gain/loss")، أم أيضًا كإعادة تقييم دورية لأرصدة
   أجنبية غير مسواة بعد بتاريخ التقرير (unrealized FX gain/loss، مبدأ
   IAS 21 مُشار إليه فعلًا بتعليق الكود لكن غير مُنفَّذ كآلية دورية)؟

6. **إلغاء الفاتورة الكاملة**: هل يبقى المرتجع الكامل (`post_return`
   بكامل الكمية) هو الآلية الرسمية الوحيدة للإلغاء بعد الترحيل، أم يُبنى
   مسار "Cancel" منفصل بمنطق مختلف (مثلًا: بلا التأثير على متوسط تكلفة
   المخزون كما يفعل المرتجع الحالي)؟

---

## الخلاصة

لا توجد نقطة في فئة C تمنع بناء **واجهات الفواتير والمرتجعات نفسها**
(الشراء، البيع، المرتجعات، تعدد المواد، تعدد العملات) — كل هذا في فئة A،
مُختبَر فعليًا وبعمق.

**ما يمنع** بناء واجهة قبض/دفع الآن هو غياب طبقة الخدمة (فئة C أعلاه) —
وهذا متوقَّع ومفهوم، ليس خطأً. الأفضلية العملية المقترحة (بانتظار قراركم
بنقطة D-3): البدء بواجهات الفواتير أولًا (الجزء الجاهز والمُختبَر بالكامل)،
وتأجيل واجهة القبض/الدفع حتى تُحسَم نقاط D 3-5 وتُبنى الخدمة المخصصة.
