# PHASE3B4_POST_ARCHAEOLOGY_REPORT
**جولة ثانية مركَّزة — Manual Voucher Core مقابل المسارات الآلية. لا كود، لا Migration، لا تعديل. `returns.py` لم يُلمَس، Technical Debt §69 كما هو.**

---

## A. Manual Voucher الحالي (`journal_voucher_form.py` + `journal_voucher_list.py`)

**ما تمثله الشاشة فعلياً**: نعم، Journal Voucher فقط — سند قيد يدوي عام، لا مفهوم آخر (لا Receipt/Payment/Contra منفصل بهذه الشاشة).

- **Numbering**: نعم، لكن عبر **دالة ثالثة مستقلة داخل الـUI نفسها**: `JournalVoucherFormView._next_ref_no()` (سطر 626-630) — `SELECT COUNT WHERE ref_no LIKE 'JV-%'` ثم `f"JV-{count+1:06d}"`. **هذه دالة سادسة منفصلة عن كل ما وُرِد بالتقرير الأول** (كانت 5 هناك؛ هذه بالواجهة لا بالخدمة). البادئة `"JV-"` **لا تتقاطع فعلياً مع** بادئات `journal_edit.py::reverse_manual_entry` (`"JV-REV-"`) و`opening_balances.py` (`"JV-OPEN"`/`"JV-OPNINV"`) لأن الفلترة بـ`LIKE 'JV-%'` — لكن لاحظ: `"JV-REV-000001"` **يطابق أيضاً** نمط `LIKE 'JV-%'`! يعني عدّاد سند القيد اليدوي **يُحتسَب ضمنه أي قيد عكسي أيضاً** (تضخّم عدّاد غير مقصود على الأرجح — لم يُختبَر تأثيره الفعلي).
- **Status**: نعم، DRAFT/POSTED (من `JournalEntryStatus` مباشرة، لا enum خاص بالشاشة).
- **Save/Post منفصلان**: نعم — `_save_draft()` و`_post()` دالتان منفصلتان صراحة، بلا خلط.
- **Edit**: نعم، طالما DRAFT — `_save_draft()` تُستدعى مجدداً فتحذف كل الأسطر القديمة (`session.delete` لكل `entry.lines`) وتُعيد بناءها من الشبكة (grid) الحالية.
- **Reverse**: **[غير موجود بالواجهة إطلاقاً]** — لا زر، لا استدعاء لـ`reverse_manual_entry()` بأي مكان بهذين الملفين. الدالة موجودة بالخدمة (`journal_edit.py`) لكن **بلا أي نقطة دخول من الواجهة الحالية** — رسالة التأكيد بـ`_post()` تقول "لا يمكن التراجع إلا بعكسه لاحقاً" لكن لا آلية فعلية بالشاشة لفعل ذلك.
- **Draft يُحفَظ فعلياً**: نعم، `session.commit()` صريح بـ`_save_draft()`.
- **تعتمد مباشرة على journal_edit.py**: نعم — `add_manual_line`/`post_manual_entry`/`JournalEditError` مُستوردة ومُستخدَمة مباشرة (لا طبقة وسيطة).
- **Business logic داخل الـUI [موجود فعلياً — يحتاج قراراً لاحقاً هل يبقى]**:
  1. **إنشاء `JournalEntry` نفسه** (`self.entry = JournalEntry(...)`) — لا دالة خدمة لهذا، الـUI ينشئ الكائن مباشرة بكامل حقوله (`entry_date`, `ref_no`, `source_type="manual"`, `currency_code`, `exchange_rate`, `status=DRAFT`).
  2. **توليد الترقيم** (`_next_ref_no`) — كما بالأعلى.
  3. **مطابقة الحساب من كود مكتوب يدوياً** بشبكة الإدخال (`match = next(a for a in self._accounts_cache if a.code == code)`) — منطق بحث/تحقق نصي بالـUI، لا بالخدمة.
  4. **قائمة الحسابات القابلة للاختيار** (`list_postable_accounts`) — هذه من الخدمة فعلياً (`account_queries.py`)، **ليست** منطق UI (تصحيح لملاحظة محتملة: الفلترة `is_group=False, is_active=True` مصدرها خدمة، لا الشاشة).

**ما يمكن إعادة استخدامه مباشرة**: `add_manual_line`/`post_manual_entry`/`reverse_manual_entry`/`_validate_lines` (كلها بالخدمة أصلاً، جاهزة).
**ما يجب أن يبقى UI فقط**: عرض الشبكة، الألوان الإرشادية (مدين/دائن)، التنقل بالكيبورد.
**ما هو حالياً UI لكنه Business Logic ينبغي أن يُراجَع مكانه [يحتاج قراراً منك]**: إنشاء `JournalEntry` الفارغ نفسه + توليد رقمه — لا يوجد اليوم أي دالة خدمة اسمها شيء مثل `create_draft_journal_entry()`؛ هذا أقرب فجوة فعلية بنواة `journal_edit.py` (راجع B أدناه).

---

## B. Journal Accounting Core الحالي (`journal_edit.py`) — إجابات مباشرة على الأسئلة الـ15

| السؤال | الإجابة الفعلية |
|---|---|
| ما الدوال التي يستخدمها [يقصد يُستخدَم منها]؟ | `ensure_editable`، `_validate_lines` (خاصة)، `add_manual_line`، `remove_manual_line`، `post_manual_entry`، `reverse_manual_entry` — 6 دوال، 73 سطراً بالمجمل |
| كيف تنشئ JournalEntry؟ | **لا تنشئه إطلاقاً** — كل الدوال تأخذ `entry: JournalEntry` **موجوداً مسبقاً** كمعامل. إنشاء الكائن نفسه مسؤولية المُستدعي دائماً (اليوم: `journal_voucher_form.py` فقط) |
| كيف تنشئ JournalLine؟ | `add_manual_line` تبنيه مباشرة (`JournalLine(...)`) وتُلحقه بـ`session.add`+`flush` فوراً (سطراً بسطر، لا دفعة واحدة) |
| كيف تتعامل مع currency؟ | `line_currency_code`/`line_exchange_rate` اختياريان لكل سطر؛ `None` = يرث من معامل `exchange_rate` الممرَّر لـ`add_manual_line` نفسها (لا من `entry.exchange_rate` مباشرة — القيمة تُمرَّر صراحة من المُستدعي في كل استدعاء) |
| كيف تتعامل مع base amount؟ | `debit_base = money(d × effective_rate)` محسوبة فوراً وقت `add_manual_line`، لا لاحقاً |
| كيف تتعامل مع exchange rate؟ | `effective_rate = line_exchange_rate إن وُجد وإلا exchange_rate` — أولوية واضحة، موثَّقة بالتعليق |
| كيف تتعامل مع source_type؟ | **لا تتعامل معه إطلاقاً** — حقل على `JournalEntry` يُملأ بالكامل خارج هذا الملف (بالمُستدعي) |
| كيف تتعامل مع reference؟ | **لا تتعامل معه** — `ref_no` يُملأ خارجياً بالكامل قبل استدعاء أي دالة هنا |
| كيف تتعامل مع dates؟ | `entry_date` لا تُلمَس هنا أبداً؛ `reversal_date` معامل صريح لـ`reverse_manual_entry` فقط، بلا أي تحقق (لا رفض تاريخ سابق لتاريخ الأصل مثلاً) |
| كيف تمنع imbalance؟ | `post_manual_entry`: `entry.is_balanced()` (بالعملة الأساسية) بعد `_validate_lines()`؛ `reverse_manual_entry`: نفس الفحص على القيد المعكوس المُبنى تلقائياً |
| هل تمنع posting إلى group account؟ | **لا — [غير موجود]**. `add_manual_line` لا تتحقق من `account.is_group` إطلاقاً. الحماية الوحيدة اليوم UI-only (`list_postable_accounts` يستبعد `is_group=True` من قائمة الاختيار بالشاشة) |
| هل تمنع inactive accounts؟ | **لا — [غير موجود]**، نفس الملاحظة أعلاه تماماً (`is_active=True` فلترة UI فقط) |
| هل تمنع تعديل POSTED؟ | نعم — `ensure_editable()` تُستدعى أول كل من `add_manual_line`/`remove_manual_line`/`post_manual_entry`، ترفض إن `status != DRAFT` |
| كيف يتم reversal؟ | `reverse_manual_entry`: يبني `JournalEntry` **جديداً كاملاً** (`POSTED` مباشرة، لا DRAFT)، بأسطر معكوسة (`debit↔credit` متبادلان لكل سطر أصلي)، **بنفس** `line_currency_code`/`line_exchange_rate`/`cost_center` الأصليين حرفياً (نسخ دقيق، لا إعادة حساب) |
| ما الفرق بين draft وposted؟ | DRAFT: قابل للتعديل/الحذف بالكامل عبر `add_manual_line`/`remove_manual_line`. POSTED: نهائي، `ensure_editable` يرفض أي محاولة تعديل، التصحيح الوحيد عبر `reverse_manual_entry` (قيد جديد مقابل) |
| هل UI تقوم بـvalidation لا ينبغي أن تكون فيها؟ | **نعم — واحدة محدَّدة**: مطابقة "كود الحساب المكتوب يدوياً" لكائن `Account` (`match = next(...)`) — هذا بحث/تحقق وجود الحساب، منطقياً أقرب لطبقة خدمة (`account_queries.py` يملك دوال مشابهة أصلاً) لكنه مكتوب inline بالـUI اليوم |

**اكتشاف إضافي مهم [موجود فعلياً]**: `reverse_manual_entry` تحتوي **توليد `ref_no` inline خاصاً بها** (`f"JV-REV-{count+1:06d}"`, سطر 74) — **لا تستخدم أي دالة ترقيم مشتركة، ولا حتى `_next_ref_no` من `posting.py`** رغم أن كليهما بنفس الشكل تقريباً. هذا يعني: **`journal_edit.py` نفسها تحتوي منطق ترقيم مكرَّراً داخلياً بمعزل عن باقي المشروع.**

---

## C. Automated Posting Paths — جدول مقارنة كامل

| Service | ينشئ JE مباشرة؟ | ينشئ Lines مباشرة؟ | يستخدم validation مركزي (`journal_edit.py`)؟ | FX | Reversal |
|---|---|---|---|---|---|
| `posting.py` (`post_sales_invoice`/`post_purchase_invoice`) | نعم (`JournalEntry(...)`, `status=POSTED` مباشرة) | نعم (`entry.lines = [...]` قائمة كاملة دفعة واحدة، عبر `_jline`/`_jline_base`) | **لا** — `entry.is_balanced()` ذاتي فقط، لا `_validate_lines()` | نعم، ضمن حساب COGS/الفروق يدوياً | لا تنشئه هي — العكس عبر `invoice_cancel.py` منفصل |
| `returns.py` (`post_sales_return`/`post_purchase_return`) | نعم، نفس نمط `posting.py` تماماً | نعم، نفس النمط | **لا** | نعم يدوياً | لا — يُلغى عبر `invoice_cancel.py` أيضاً على الأرجح (لم يُتحقَّق بعمق، `returns.py` لم يُفتَح أبعد من اللازم بهذه الجولة تحديداً كما طُلب) |
| `settlements.py` (`_post_settlement_multi`/`_post_refund`) | نعم | نعم | **لا** | نعم، يدوياً (`_jline_party` الجديدة بـ3B-3) | **لا reversal إطلاقاً بالتصميم** — Append-Only، تصحيح فقط بقيد جديد منفصل (قرار 3B-3 المتعمَّد، ليس نقصاً) |
| `invoice_cancel.py` (`cancel_invoice`) | نعم (قيد عكسي) | نعم | **لا** | لا (عكس دقيق، لا تحويل) | هي نفسها آلية العكس لكل من `posting.py`، بمنطق مستقل عن `journal_edit.py::reverse_manual_entry` (لا تشترك بأي كود) |
| `opening_balances.py` (3B-1/3B-2) | لا — تستخدم `add_manual_line` | لا | **نعم** | نعم، عبر `add_manual_line`'s `exchange_rate` | نعم، عبر `reverse_manual_entry` مباشرة (نفس دالة `journal_edit.py`، لا نسخة خاصة) |
| `opening_party_balances.py` (3B-3) | لا | لا | **نعم** | نعم | نعم، نفس `reverse_manual_entry` المشتركة |

**ملاحظة حاسمة [موجود فعلياً]**: **المسارات الوحيدة التي تستخدم `journal_edit.py` فعلياً هي `opening_balances.py` و`opening_party_balances.py` — 2 من أصل 6 مسارات آلية.** الأربعة الباقية (`posting.py`/`returns.py`/`settlements.py`/`invoice_cancel.py`) — وهي **الأكثر استخداماً عملياً بالنظام** (كل فاتورة، كل تسوية، كل إلغاء) — تبني القيود مباشرة بمعزل تام عن `journal_edit.py`.

**لماذا هذا الانقسام موجود أصلاً (استنتاج من نمط الكود، لا افتراض)**: كل قيود المسار الآلي تُنشأ `POSTED` **فوراً** (لا حالة DRAFT وسيطة أبداً — الفاتورة إما POSTED بالكامل مع قيدها أو لا قيد لها إطلاقاً)، بينما `journal_edit.py` مبني حول فرضية DRAFT←→POSTED قابلة للتنقل ذهاباً وإياباً (تعديل الأسطر، حذفها، إلخ) — وهي فرضية **لا تحتاجها** المسارات الآلية إطلاقاً (الفاتورة تُبنى بالكامل بذاكرة Python أولاً، ثم تُرحَّل دفعة واحدة). **هذا يفسّر الانفصال معمارياً، لا يبرره بالضرورة** — الأمر متروك لقرارك.

---

## D. Validation Rules الحالية (مُجمَّعة)

**بـ`journal_edit.py` (2 مسارين فقط)**: حساب محدَّد لكل سطر، لا مدين+دائن معاً بنفس السطر، لا سطر فارغ، لا قيد بلا أسطر، توازن إلزامي (base)، لا تعديل POSTED، لا عكس عكس، لا عكس مزدوج لنفس القيد.

**بالمسارات الآلية الأربعة**: توازن (`is_balanced()`) فقط — **لا فحص فردي لكل سطر** (نظرياً، سطر بمدين وصفر دائن معاً ممكن تقنياً لو `_jline`/`_jline_base` استُدعيت بخطأ برمجي، لأن هذه الدوال نفسها لا تتحقق من ذلك — `JournalLine.__table_args__` بـ`models.py` **يحتوي `CHECK` على مستوى قاعدة البيانات** (`(debit=0 AND credit>=0) OR (credit=0 AND debit>=0)`) وهذا **هو الحارس الفعلي الوحيد** لهذه الحالة بالمسار الآلي — حماية DB-level، لا Python-level).

**تصنيف الحساب (subtype/allow_reconciliation)**: مُتحقَّق منه فقط بـ`settlements.py`/`opening_party_balances.py` (مسارات 3B-3) — **غير مُتحقَّق منه إطلاقاً** بـ`journal_edit.py` نفسها ولا بالمسارات الآلية الأخرى (`posting.py` يستخدم حسابات ثابتة من الإعدادات، لا اختياراً حراً، فالمخاطرة عملياً منخفضة هناك لكن غير مفروضة بالكود صراحة أيضاً).

---

## E. FX Rules الحالية (مُجمَّعة)

3 أنماط منفصلة، بلا دالة واحدة مشتركة:
1. **سعر واحد بسيط** (`_jline`/`add_manual_line`'s `effective_rate`): `base = raw × rate`.
2. **مُحوَّل مسبقاً** (`_jline_base`): `raw = base` (لخطوط COGS/فروق صرف لا معنى لـ"خام" منفصل لها).
3. **مُدمَج من أسعار متعددة** (`_jline_party`, جديدة بـ3B-3 فقط): `raw` و`base` مستقلان تماماً، لا علاقة سعر واحد بينهما.

**لا معالج FX مركزي واحد** يُستدعى من أي من المسارات الأربعة الآلية أو من `journal_edit.py` — كل حساب فرق صرف (بـ`settlements.py` تحديداً) مكتوب inline بكل دالة بمفردها.

---

## F. Posting/Reverse Rules (مُلخَّص موحَّد)

| النمط | من يملكه | كيف |
|---|---|---|
| Post قيد يدوي DRAFT→POSTED | `journal_edit.py::post_manual_entry` | تحقق شامل ثم `status=POSTED` |
| Post قيد آلي (فاتورة/تسوية/رصيد افتتاحي) | كل خدمة بمفردها | `status=POSTED` مباشرة وقت الإنشاء، لا DRAFT وسيط (باستثناء `opening_*` التي تمر فعلياً بـ`post_manual_entry` رغم DRAFT ابتدائي قصير العمر) |
| Reverse قيد يدوي | `journal_edit.py::reverse_manual_entry` | قيد جديد معاكس، منع عكس مزدوج ومنع عكس-العكس |
| Reverse فاتورة | `invoice_cancel.py::cancel_invoice` | منطق **مستقل تماماً**، لا يشارك كوداً مع `reverse_manual_entry` |
| Reverse تسوية (3B-3) | **لا يوجد — بالتصميم** | Append-Only، تصحيح بقيد جديد منفصل فقط، قرار متعمَّد لا نقص |
| Reverse رصيد افتتاحي (3B-1/2/3) | `journal_edit.py::reverse_manual_entry` مباشرة | نفس الدالة المشتركة، بلا نسخة خاصة |

**إذن يوجد فعلياً 3 آليات "عكس" منفصلة كودياً**: `reverse_manual_entry` (عام)، `cancel_invoice` (خاص بالفواتير)، ولا شيء للتسويات (بالتصميم). هذا تكرار حقيقي إضافي لم يُذكَر بالتقرير الأول بهذا الوضوح.

---

## G. Draft/Posted Lifecycle

`JournalEntryStatus`: `DRAFT`/`POSTED`/`CANCELLED` (الأخيرة **قيمة ميتة تماماً لـJournalEntry** — تأكيد إضافي هذه الجولة: صفر إسناد بأي كود مفحوص، بما فيه إعادة الفحص الحالية). `InvoiceStatus` منفصل تماماً (`DRAFT`/`POSTED`/`CANCELLED`، هذه الأخيرة **مُستخدَمة فعلياً** على `Invoice`، لا `JournalEntry`). لا enum ثالث لـ`Settlement`/`SettlementAllocation`/`OpeningPartyEntry` (بلا status، Append-Only بالتصميم).

---

## H. Source Type Architecture — هل `source_type` كافٍ؟

**نعم، بحدود الاستخدام الحالي — لا حاجة لـabstraction جديد لمجرد التسمية.** `source_type` (نص حر، `String(30)`) يميّز المصدر بدقة كافية اليوم لكل حالة موجودة (§4 بالتقرير الأول). **لكن** له 3 قيود فعلية يجب معرفتها قبل البناء عليه لأي Voucher Engine:
1. **لا enum يحصر القيم المسموحة** — نص حر بالكامل، عرضة لخطأ إملائي مستقبلي بلا تحذير من النظام (مثال حقيقي موجود فعلاً: `"purchase_return"` بـ`InventoryMovement` مقابل `"return_invoice"` بمكان آخر — تسميتان مختلفتان لمفهوم قريب، رغم عدم كونهما بنفس الجدول بالضرورة، يستحق فحصاً إن وُحِّد لاحقاً).
2. **`source_id` غير مُستخدَم باتساق** — مُملَّأ للفواتير/الأرصدة الافتتاحية، **دائماً `None`** للتسويات (§4 بالتقرير الأول).
3. **لا فهرس (index) مؤكَّد على `source_type`** بالفحص الحالي — أداء الاستعلام بحجم بيانات كبير لم يُفحَص، خارج نطاق هذه الجولة.

---

## I. Voucher vs JournalEntry vs Business Document

بناءً على الكود الفعلي فقط (لا اقتراح):
- **`JournalEntry`**: التمثيل المحاسبي الوحيد الفعلي — كل شيء ينتهي هنا مهما كان مصدره.
- **"Voucher" بالمشروع اليوم**: تسمية UI حصراً لسند القيد اليدوي (`journal_voucher_form.py`) — **لا يوجد أي جدول أو كائن Python اسمه `Voucher`**. مفهومياً، "Voucher" اليوم = "الشكل اليدوي لإدخال JournalEntry"، لا أكثر.
- **"Business Document"**: `Invoice`/`Settlement`/`OpeningPartyEntry`/`StockTransfer` — كل منها **جدول مستقل بحقول ونمط حياة (lifecycle) خاص به تماماً**، يُنتِج (أو لا يُنتِج، حالة `StockTransfer`) `JournalEntry` بطريقته الخاصة.
- **لا تسلسل هرمي مشترك بينها اليوم** (لا Interface/Base class مشترك بمعنى OOP، ولا جدول أب بمعنى DB) — كل واحد مستقل تماماً، الرابط الوحيد بينها هو `JournalEntry.source_type`/`source_id` (نصياً، لا FK حقيقياً لـ`source_id` — عمود `Integer` عادي، **بلا** `ForeignKey` constraint فعلي بالنموذج، تأكيد إضافي: يعتمد بالكامل على انضباط التطبيق، لا قاعدة البيانات).

---

## J. Invoice Numbering Findings — إجابة السؤال المحوري بدقة

**السؤال المطروح**: هل `invoice_no` هوية محاسبية/مرجعية حقيقية، أم مجرد رقم عرض؟

**الإجابة القاطعة من الكود، لا افتراض**: **مجرد رقم عرض/بحث للمستخدم — `Invoice.id` (المفتاح الأساسي الرقمي) هو الهوية الحقيقية المُستخدَمة بكل مكان فعلياً.**

الدليل الكامل (بحث شامل، كل نتيجة فُحصت):
- **كل الحقول العلائقية (FK) تستخدم `invoice.id`**: `SettlementAllocation.invoice_id→invoices.id` (رقمي)، `InventoryMovement.source_id=invoice.id`، `Invoice.original_invoice_id→invoices.id` (لربط المرتجع بالأصل) — **لا FK واحد يستخدم `invoice_no` النصي بأي مكان**.
- **كل استخدامات `invoice_no` بالخدمات (17 موقعاً فُحصت بالكامل، §سابق أعلاه) هي**: نص رسائل خطأ/وصف (`f"الفاتورة {invoice.invoice_no}..."`)، أو دالة بحث صريحة واحدة (`invoice_queries.py::get_invoice_by_no`).
- **الاستخدام الوحيد بالـUI**: `document_form.py::_load_from_original` — يبحث بـ`invoice_no` (رقم يكتبه المستخدم)، **ثم فوراً يُحوِّله لـ`original.id` ويخزّن ذلك** (`self._original_invoice_id = original.id`) — تأكيد مباشر أن `invoice_no` بوابة بحث بشرية فقط، لا هوية مخزَّنة.
- **Uniqueness**: نعم، `unique=True` بمستوى قاعدة البيانات (`app/models.py:262`).
- **هل يمكن أن يتغيّر بعد POSTED دون كسر العلاقات؟**: **نعم تقنياً — لا علاقة واحدة ستنكسر**، لأن لا شيء يعتمد عليه علائقياً. القيد الوحيد هو الحفاظ على `unique=True` عند أي تغيير.
- **Cancellation/Reports**: `invoice_cancel.py` يستخدم `invoice.invoice_no` **بنص رسائل الخطأ فقط**، لا كمنطق. لا تقرير فُحص بهذه الجولة يعتمد عليه كمفتاح تجميع (خارج النطاق المطلوب هذه الجولة تحديداً).

**الخلاصة العملية**: **خلل الترقيم (`DRAFT-{id(self)}` يبقى بعد POSTED) هو Bug في جودة/احترافية المستند المعروض للمستخدم فقط — لا خطر محاسبي أو علائقي أو بنيوي واحد ناتج عنه.** يدعم بقوة فصله كـPhase/إصلاح مستقل عن Voucher Engine، تماماً كما رجَّحتَ.

---

## K. Duplicate Logic (تحديث بعد الجولة الثانية)

بالإضافة لكل ما ذُكر بالتقرير الأول (§14 هناك)، اكتُشف بهذه الجولة تحديداً:
- **دالة ترقيم سادسة**: `journal_voucher_form.py::_next_ref_no` (UI-level، منفصلة عن كل الخمس بالخدمات).
- **`reverse_manual_entry` تحتوي توليد ترقيم inline خاصاً بها** (`"JV-REV-"`)، منفصل حتى عن `_next_ref_no` العامة بـ`posting.py` رغم التشابه التام بالشكل.
- **3 آليات "عكس" منفصلة كودياً بلا مشاركة**: `reverse_manual_entry`، `cancel_invoice`، ولا شيء للتسويات (بالتصميم).
- **تحقق وجود الحساب بالكود ("account exists by code")** مكرَّر بين `journal_voucher_form.py` (بحث نصي بقائمة مُحمَّلة بالذاكرة) وما يعادله ضمنياً بمنطق الفواتير (اختيار من قائمة جاهزة، لا بحث نصي حر).

---

## L. Direct Accounting Bypasses (تأكيد نهائي)

لا "كسر توازن" فعلي بأي مكان (`is_balanced()` أو `CHECK` بقاعدة البيانات مفروضان دائماً). **البايباس الحقيقي الوحيد المؤكَّد**: تجاوز `_validate_lines()` (منع سطر فارغ/مدين+دائن معاً على مستوى Python) بـ4 من 6 مسارات آلية — **مخفَّف جزئياً** بـ`CHECK` قاعدة بيانات على `JournalLine` نفسها (`ck_debit_xor_credit`) الذي يمنع "مدين+دائن معاً" فعلياً بغض النظر عن المسار، **لكن لا يمنع "سطر فارغ تماماً" (0,0)** — الـCHECK الحالي يسمح بـ`debit=0 AND credit=0` (يحقق `credit=0 AND debit>=0` بصراحة) — **[اكتشاف جديد]**: سطر فارغ تماماً (0/0) **ممكن فعلياً بقاعدة البيانات نفسها بالمسار الآلي**، لا يُمنَع إلا بـ`journal_edit.py::_validate_lines()` (Python-only، غير مُطبَّق على 4 مسارات).

---

## M. Candidate Reusable Core — ما الذي يصلح أساساً فعلياً

**نعم — `journal_edit.py` نواة صالحة جزئياً للتوسيع، لا نواة كاملة**:
- **صالح كما هو**: `_validate_lines`، `ensure_editable`، `post_manual_entry` (التحقق + الإغلاق) — منطق سليم ومُختبَر (ولو بشكل غير مباشر).
- **يحتاج إضافة قبل أن يصلح أساساً عاماً لكل المسارات**:
  1. دالة **إنشاء** `JournalEntry` فارغ (غير موجودة اليوم — كل مُستدعٍ يبنيه يدوياً).
  2. **توليد ترقيم موحَّد** (بدل 6 نسخ متكررة).
  3. **تحقق `is_group`/`is_active` على مستوى الخدمة**، لا الـUI فقط.
  4. **حل مسألة `POSTED` فوري مقابل `DRAFT` وسيط** — المسارات الآلية تحتاج `POSTED` فوري بلا DRAFT وسيط (طبيعة الفاتورة/التسوية)؛ `post_manual_entry` تفترض DRAFT قائم مسبقاً دائماً. أي توحيد يحتاج معالجة هذا الفارق صراحة (دعم "أنشئ وارحّل فوراً" كمسار مختصر، بدل إجبار كل شيء على DRAFT وسيط اصطناعي).
  5. **معالج FX مركزي واحد** (بدل 3 أنماط متوازية).

---

## N. Things that must NOT change (بناءً على النطاق المحدَّد وحده، لا اجتهاداً)

- `returns.py` — لم يُلمَس هذه الجولة كما طُلب صراحة، ويبقى كذلك.
- كل ما أُغلق رسمياً بـ3B-1/3B-2/3B-3 (`WORKFLOW.md §§61-69`) — أي توحيد لاحق يجب أن يحافظ على نفس السلوك المحاسبي الناتج (الأرقام، لا بالضرورة نفس الكود الداخلي حرفياً).
- `Settlement`/`SettlementAllocation` Append-Only — قرار متعمَّد صريح، ليس نقصاً يحتاج "إصلاحاً" بمحرك عكس موحَّد.
- التمييز بين `Cancellation` (خاص بالفواتير، بقواعده الخاصة كوجود تسويات) و`Reversal` (عام) — مفهومان مختلفان فعلياً بالكود، لا يجب دمجهما بلا مبرر محاسبي صريح.

---

## O. Open Decisions Required from Bilal

1. **الفارق الجوهري بين "POSTED فوري" (المسارات الآلية) و"DRAFT قابل للتعديل" (اليدوي)** — هل أي نواة موحَّدة يجب أن تدعم كلا النمطين صراحة كخيارين معلَنين، أم يبقى لكل مسار حريته الكاملة فوق طبقة تحقق مشتركة فقط (بلا توحيد دورة الحياة نفسها)؟
2. **مصير الترقيم المكرَّر (6 نسخ)** — هل يدخل ضمن نطاق 3B-4 (توحيد الترقيم كجزء من "الحدود المحاسبية الصحيحة")، أم يبقى خارجه تماماً بصفته مسألة عرض/تجربة مستخدم بحتة كما اتضح من invoice_no (§J)؟
3. **الفجوة الأمنية الحقيقية الوحيدة المكتشَفة**: `add_manual_line`/المسارات الآلية لا تتحقق من `is_group`/`is_active` على مستوى الخدمة (§B/§L) — هل يدخل ضمن 3B-4 كـ"قاعدة تحقق مركزية يجب أن تُفرَض على الجميع"، أم يُعتبَر إصلاحاً منفصلاً أصغر؟
4. **آليات العكس الثلاث المنفصلة** (`reverse_manual_entry`/`cancel_invoice`/لا شيء للتسويات) — هل توحيدها ضمن 3B-4، أم يبقى كل نوع مستند بمنطق العكس الخاص به (بما يتماشى مع "كل Domain يحتفظ بمنطقه" الذي نصصت عليه)؟
5. **زر Reverse المفقود بواجهة سند القيد** — إصلاح UI بسيط (الدالة الخلفية موجودة أصلاً)، لكنه يقع تقنياً ضمن نفس الشاشة التي قد يمسّها 3B-4 — هل يُدرَج تلقائياً، أم قرار منفصل؟

---

**لا تصميم، لا Scope نهائي، لا كود. هذا فحص فقط — بانتظار قرارك على النقاط الخمس بـ(O) قبل الانتقال لأي مرحلة تالية.**
