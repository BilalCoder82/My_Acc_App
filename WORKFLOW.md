# WORKFLOW.md — المرجع الوحيد للمعمارية وحالة المشروع

> **قاعدة ثابتة**: هذا الملف يُحدَّث بنهاية كل جلسة عمل مع Claude، بغض النظر
> عن استمرار نفس المحادثة أو بدء محادثة جديدة. أي محادثة جديدة تبدأ بقراءة
> هذا الملف كاملاً قبل أي تعديل على الكود.

---

## 1. الهدف من المشروع

برنامج محاسبة سطح مكتب (PySide6) لمحاسب مستقل يخدم عملاء متعددين
(أقل من 10 حالياً)، بديل عن الدخول عبر AnyDesk لأجهزة العملاء. **استخدام
شخصي فقط، ليس منتجاً تجارياً.**

## 2. القرارات المعمارية المتّخذة (ولماذا)

| القرار | البديل المرفوض | السبب |
|---|---|---|
| ملف SQLite منفصل لكل عميل + registry.db مركزي | قاعدة بيانات واحدة متعددة المستأجرين (multi-tenant table) | عزل كامل لبيانات كل عميل، بساطة نسخ احتياطي لكل عميل على حدة |
| SQLAlchemy models مباشرة، بدون Repository/UoW/DDD | Clean Architecture بـ4 طبقات (domain/application/infrastructure/presentation) | مطوّر واحد، مشروع صغير — التجريد الإضافي كلفة إدراكية بدون فائدة فعلية حالياً (مبدأ YAGNI) |
| قيد مزدوج حقيقي (JournalEntry + JournalLine متوازن دائماً) | نظام قيد مبسّط بدون توازن مفروض | أساس صحيح لأي حسابات ختامية أو ميزان مراجعة لاحقاً — لا رجعة عن هذا القرار |
| الفاتورة تولّد قيداً آلياً، لا تُعامل كسجل محاسبي مستقل | حفظ نتائج الفاتورة مباشرة كأرقام بدون قيد | يضمن مصدر حقيقة واحد (دفتر الأستاذ) لكل التقارير |
| المرتجعات = عكس القيد الأصلي بالضبط | قيد مستقل جديد للمرتجع | يمنع انحراف الأرقام بمرور الوقت، ويحافظ على أثر التدقيق |
| لا يوجد جدول Customer/Supplier منفصل | جدول أطراف كامل بكشف حساب لكل طرف | **مُنفَّذ**: `app/services/parties.py` — حساب فرعي تلقائي تحت الذمم المدينة/الدائنة لكل طرف، بدون جدول Party منفصل. مُختبر: نفس العميل بفاتورتين = حساب واحد فقط، عملاء مختلفون = أكواد منفصلة |
| العملة الأساسية لكل شركة تُختار صراحة عند الإنشاء، بدون افتراضي مخفي | قيمة افتراضية ثابتة (مثلاً SYP) | قرار المستخدم: "أختارها أنا" — `create_company()` يرفض القيمة الفارغة |
| بنية ملفات مسطّحة حالياً (`app/models.py` واحد)، تُقسَّم لاحقاً حسب النطاق (accounting/, sales/, purchases/...) فقط عند الحاجة الفعلية | تقسيم مسبق لملفات منفصلة من الآن | راجعنا اقتراح تقسيم بديل من "مبرمج ثانٍ" — اتفقنا أنه تحسين صحيح **مستقبلاً**، بس تطبيقه الآن (ملف ٢٥٠ سطر فقط) هو over-engineering مبكر بنفس منطق رفضنا لـDDD |
| Migration بنمط `PRAGMA user_version` (يدوي، بدون Alembic) | Alembic | الـschema لسه بمرحلة تغيّر سريع؛ نمط user_version أثبت نجاحه بمشروع Restaurant Costing Pro سابقاً. **قاعدة صارمة: لا حذف أعمدة/جداول أبداً، فقط إضافة** |
| Repo عام (public) على GitHub | خاص | قرار المستخدم — **لكن بيانات العملاء الفعلية (`data/`, `*.db`) ممنوعة نهائياً من الرفع، مضمونة عبر `.gitignore`** |

## 3. بنية المجلدات الحالية

```
accounting_system/
├── app/
│   ├── models.py              # كل SQLAlchemy models (Account, Item, JournalEntry...)
│   ├── db.py                  # فتح registry.db وملفات العملاء + تطبيق migrations
│   ├── migrations/
│   │   └── runner.py          # نظام PRAGMA user_version
│   └── services/
│       └── posting.py         # محرك ترحيل الفواتير → قيود محاسبية
├── tests/
│   └── test_migration_safety.py
├── data/                       # (مستثنى من git) ملفات العملاء الفعلية
├── .gitignore
├── requirements.txt
└── WORKFLOW.md                 # هذا الملف
```

## 4. ما تم إنجازه (مُختبر فعلياً، ليس نظرياً)

- [x] `models.py`: Account (شجري)، Item، InventoryMovement (متوسط مرجّح)،
      JournalEntry/JournalLine (مع فرض توازن `is_balanced()`)، Invoice/InvoiceLine، Setting
- [x] `posting.py`: `post_sales_invoice()` — يولّد قيد مبيعات + قيد COGS متوازن، **مُختبر بنجاح**
- [x] `posting.py`: `post_purchase_invoice()` — يولّد قيد شراء (بدون COGS، البضاعة تدخل مخزون فقط)، **مُختبر بنجاح**
- [x] `posting.py`: `post_return()` — يعكس القيد الأصلي بالضبط، **مُختبر بنجاح: مدين الأصلي == دائن العكس تماماً**
- [x] `invoice_calc.py`: مصدر حقيقة وحيد لحساب الحسم (بند + فاتورة) والضريبة، **مُختبر بنجاح** (حسم بند 10% + حسم فاتورة 5% موزّع نسبياً + ضريبة 10% → قيد متوازن تماماً)
- [x] `invoice_edit.py`: منع تعديل/حذف أي فاتورة POSTED (على مستوى البند أو الفاتورة كاملة)، **مُختبر بنجاح**. سلوكنا مختلف عمداً عن QuickBooks Desktop الذي يسمح بتعديل مستند مرحّل مباشرة
- [x] `migrations/runner.py`: نظام ترقية آمن — **مُختبر: إضافة عمود لا تمسح بيانات قديمة**
- [x] `db.py`: registry.db + فتح ملف عميل مع تطبيق migrations تلقائياً
- [x] `.gitignore`: يمنع رفع أي بيانات عملاء فعلية
- [x] `rollup.py`: تجميع هرمي لرصيد أي حساب تجميعي = مجموع أبنائه
- [x] `income_statement.py`: قائمة الدخل لفترة محددة، **مُختبرة بنجاح**
- [x] `balance_sheet.py`: الميزانية العمومية مع صافي ربح غير مُقفل كبند مستقل — **مُختبرة بنجاح: الأصول = الالتزامات + حقوق الملكية بالضبط (679.33 = 86.00 + 593.33)**، عبر شراءين بسعرين مختلفين وبيعتين بفترتين مختلفتين
- [x] `app/ui/`: `MainWindow`، `SalesInvoiceListView`، `SalesInvoiceFormView`، `ChartOfAccountsView` — **مُختبرة end-to-end headless**: فتح فاتورة → حساب حي → حفظ → ترحيل → ميزان متوازن (55=55)

## 5. الناقص (بالترتيب المقترح للتنفيذ)

1. شاشات الواجهة المتبقية (فاتورة الشراء، المرتجعات، سند القيد، الإعدادات، اختيار عميل حقيقية) — تكرار نمط `SalesInvoiceFormView` الناجح، راجع `UI_DESIGN.md`
2. نسخ احتياطي تلقائي مشفّر (المستخدم اختار محلي فقط بقرص خارجي حالياً)
3. تقرير مخزون مُجمَّع (رصيد كل مادة/مستودع بجدول واحد) — البيانات جاهزة (`get_stock_balance`)، التقرير المُجمَّع لم يُبنَ
4. شاشة قائمة الدخل والميزانية العمومية بالواجهة — الـbackend جاهز ومختبر (`income_statement.py`, `balance_sheet.py`)، العرض بالواجهة لم يُبنَ

## 6. إضافات نُفِّذت تحت تفويض "كل ما هو ضروري مهنياً" (كل واحدة موثّقة ولماذا)

| الإضافة | لماذا كانت ضرورية فعلاً (لا مجرد ميزة إضافية) |
|---|---|
| `debit_base` / `credit_base` بكل JournalLine | القيمة بالعملة الأساسية يجب أن تُحفظ وقت القيد لا تُحسب لاحقاً — لأن سعر الصرف يتغير، والتقارير التاريخية يجب أن تعكس السعر الفعلي وقت العملية، لا السعر الحالي |
| `set_item_opening_balance()` | بدون رصيد افتتاحي للمواد، أي تقرير مخزون لعميل له تاريخ سابق يكون ناقصاً من أول يوم استخدام — نفس منطق رصيد الحسابات الافتتاحي بالضبط |
| `Warehouse` + `warehouse_id` (بدون واجهة تحويل مستودعات) | **حل وسط صريح**: أُضيف الحقل بالـschema فقط لتفادي هجرة بيانات مستقبلية إذا احتاج المستخدم تعدد مستودعات فعلياً — **لم تُبنَ أي واجهة أو منطق نقل بين مستودعات**، لأن هذا تطلّب فعلي غير مؤكد حالياً (عميل واحد، أقل من 10 عملاء) |
| `LedgerService` (`app/reports/ledger.py`) | كشف حساب مع رصيد افتتاحي واتجاه رصيد صحيح حسب نوع الحساب (مدين طبيعي مقابل دائن طبيعي) — طُلب صراحة بأول رسالة بالمحادثة |
| `TrialBalanceService` | يتحقق تلقائياً أن مجموع مدين = دائن على مستوى النظام كله — أول خط دفاع لاكتشاف أي خطأ ترحيل تسرّب |

## 7. القواعد الخمس الثابتة (مرجع لا يُنقض)

1. لا تحتوي PySide6 على منطق محاسبي — عرض وجمع مدخلات فقط
2. لا تُبعثر SQLAlchemy queries داخل كل نافذة — الوصول لقاعدة البيانات فقط عبر `app/services/`
3. كل عملية محاسبية تمر عبر service واضح ومختبر
4. المستندات المرحّلة (POSTED) غير قابلة للتعديل — **مُنفَّذة الآن على الفواتير (`invoice_edit.py`) وعلى القيود اليدوية (`journal_edit.py`) معاً**
5. المعلومات المصدرية (JournalLine, InventoryMovement) محفوظة، والنتائج المشتقة (كشف حساب، ميزان مراجعة) تُحسب منها دائماً، لا تُخزَّن

## 7. سياسة الدقة المالية (Decimal Policy) — إلزامية على كل كود جديد

- **كل قيمة مالية تُحوَّل لـ`Decimal` فور دخولها عبر `app/services/money.py` — ممنوع استخدام `float()` على أي مبلغ مالي بأي مكان بالنظام.** هذا كان خطأ فعلياً بالكود الأولي وتم تصحيحه بالكامل بجلسة لاحقة
- التقريب: `ROUND_HALF_UP` على منزلتين للمبالغ (`money()`), ثلاث منازل للكميات (`qty()`) — عند كل خطوة حساب، لا فقط بالعرض النهائي
- **تنويه SQLite**: التخزين الفعلي ليس Decimal حقيقياً (REAL/TEXT حسب SQLite)؛ الالتزام بالتقريب الموحّد بكل خطوة هو ما يمنع تراكم الخطأ، لا نوع العمود وحده
- **مُختبر فعلياً**: `0.1 + 0.2 + 0.3 = 0.60` بالضبط عبر النظام الكامل (فاتورة → قيد → ميزان مراجعة)، بدون خطأ float التراكمي الكلاسيكي

## 8. تعدد المستودعات — قرار مُراجَع ومُصحَّح

**تصحيح من الجلسة السابقة**: كان القرار المبدئي تأجيل أي منطق تحويل، بافتراض أنه "تطلّب غير مؤكد". **هذا افتراض خاطئ صححه المستخدم لاحقاً** — عنده فعلياً عملاء بمستودعات متعددة، فهذا مطلب حقيقي معروف مسبقاً لا افتراضي (ليس YAGNI). المُنفَّذ الآن:
- `Warehouse`, `InventoryMovement.warehouse_id`, `Invoice.warehouse_id` — كل الحركات مرتبطة بمستودع محدد
- `StockTransfer` + `transfer_stock()`: تحويل بين مستودعين، حركتين مرتبطتين (OUT/IN)، **بدون أي أثر محاسبي** (لا قيد، لا تغيّر بالربح)، **مُختبر بنجاح** (إجمالي الرصيد ثابت قبل/بعد التحويل)
- `get_stock_balance()`: رصيد مادة إجمالي أو لمستودع محدد
- **لم يُبنَ بعد**: واجهة PySide6 لاختيار مستودع أو تنفيذ تحويل (ينتظر مرحلة الواجهة عمداً — الفرق بين المنطق الجاهز والواجهة، لا بين ضروري وغير ضروري)

## 9. سجل الجلسات

**جلسة 1 (2026-08-21)**: تصميم schema أولي، محرك ترحيل الفواتير، نظام migration آمن،
بنية مجلدات، `.gitignore`. تقرر رفض معمارية DDD/Clean Architecture المقترحة
لصالح بنية مبسّطة مبررة بحجم المشروع الفعلي. راجعنا رأي مبرمج ثانٍ (اتفاق ٩٥٪) —
تبنّينا تصحيحه بخصوص فرق YAGNI عن إهمال التنظيم. نفّذنا الحسابات الفرعية التلقائية
للعملاء/الموردين (بديل عن جدول Party) وفرضنا اختيار العملة الأساسية صراحة لكل شركة.
أضفنا حسم البند/الفاتورة (`invoice_calc.py`) وقواعد منع تعديل POSTED (`invoice_edit.py`)،
مع ملاحظة أن سلوكنا يختلف عمداً عن QuickBooks Desktop بموضوع تعديل المستند المرحّل.
تحت تفويض "كل ما هو ضروري مهنياً"، أضفنا: base_amount للعملات، رصيد مواد افتتاحي،
وWarehouse كحقل schema فقط (بدون ميزة كاملة) — رُفض تعدد المستودعات الكامل كـover-engineering
غير مبرر بحجم المشروع الحالي. بُني `LedgerService` و`TrialBalanceService`، مُختبران بنجاح
(ميزان متوازن 100=100، كشف حساب برصيد جارٍ صحيح). migration v2 يدعم عميل جديد بالكامل
وعميل قديم بالترقية، الاثنان مُختبران.

**جلسة 2 (2026-08-22)**: راجعنا نقد مبرمج ثانٍ على 4 نقاط (تعدد مستودعات، Decimal بدل
float، أولوية قفل JournalEntry اليدوي قبل الواجهة، فهرسة بدل caching). اعتُمدت النقاط
الأربع بالكامل. نُفِّذ: `app/services/money.py` (سياسة Decimal موحّدة)، إعادة كتابة
`invoice_calc.py`/`posting.py`/`ledger.py`/`trial_balance.py` بالكامل بـDecimal بدل float
(كان خطأ فعلياً بالكود الأصلي)، `JournalEntryStatus` + `journal_edit.py` (قفل القيد اليدوي
بعد الترحيل)، `StockTransfer` + `transfer_stock()` (تحويل بين مستودعات بدون أثر محاسبي)،
فهارس على `journal_lines.account_id` و`journal_entries.entry_date` (migration v3).
migration v4 يضيف status للقيود القديمة (تُعتبر posted تلقائياً بالترقية). كل التغييرات
مُختبرة: دقة Decimal (0.1+0.2+0.3=0.60 بالضبط)، تحويل مستودعات (رصيد إجمالي ثابت)، قفل
القيد اليدوي بعد الترحيل. صُحِّح افتراضي `JournalEntry.status` من POSTED إلى DRAFT بعد
اكتشاف خطأ بالاختبار الأول (كل قيد جديد يفرض تحديد حالته صراحة الآن).

**جلسة 3 (2026-08-22)**: صديق المستخدم اقترح تصميم واجهة كامل ومفصّل (Top Nav +
Section Map + Workspace Tabs + Grid-first + ثنائية اللغة). حُفظ كاملاً بـ`UI_DESIGN.md`
كمرجع طويل الأمد، لكن اعتُمد نطاق v1 مصغّر فقط (بدون نظام ترجمة/RTL-LTR ديناميكي —
تضخّم غير مبرر لمستخدم عربي وحيد لم يطلب إنجليزية إطلاقاً). تقرر تأجيل أي عمل على
الواجهة والعودة للترتيب المتفق عليه: الحسابات الختامية أولاً.

نُفِّذ: `rollup.py` (تجميع هرمي)، `income_statement.py` (قائمة دخل لفترة)،
`balance_sheet.py` (ميزانية عمومية مع بند "صافي ربح غير مُقفل" لتعويض غياب قيد
الإقفال الفعلي). **أهم اختبار بالمشروع لغاية الآن**: الميزانية توازنت فعلياً
(679.33 = 86.00 + 593.33) عبر سيناريو واقعي (شراءين بسعرين مختلفين يغيّران المتوسط
المرجّح، بيعتان بفترتين مختلفتين). محرك المحاسبة الأساسي مكتمل الآن بالكامل —
الخطوة التالية PySide6 حصراً.

**جلسة 4 (2026-08-22)**: المستخدم أكّد أن دعم الإنجليزية مستقبلاً هدف حقيقي (نُسي
ذكره سابقاً) — القرار: عادة كتابة رخيصة الآن (نصوص منفصلة كثوابت `LABELS`، بدون
جمل مركّبة يصعب تفكيكها)، بدون بناء نظام ترجمة فعلي. راجعنا رأي مبرمج ثانٍ حول
تصميم الواجهة الكامل — اتفاق شبه كامل، اعتُمد نطاق v1 مصغّر (Navigation بسيطة +
Workspace Tabs، بدون Section Map منفصل أو اختبار دقات شاشة متعددة).

بُنيت 3 مكونات (بنفس ترتيب المبرمج الثاني المقترح): `MainWindow`، `SalesInvoiceListView`،
`SalesInvoiceFormView` (النموذج المرجعي: Header → Grid → Totals → Actions)، بالإضافة
لـ`ChartOfAccountsView`. **اختبار end-to-end حقيقي عبر الواجهة نفسها** (headless،
`QT_QPA_PLATFORM=offscreen`): فتح فاتورة جديدة، إدخال بند بالـgrid، حساب حي صحيح،
حفظ مسودة، ترحيل، والتحقق من ميزان مراجعة متوازن بعد الترحيل (55=55).

**بحثان حقيقيان اكتُشفا وصُحِّحا أثناء هذا الاختبار** (مو مجرد تفاصيل اختبار):
1. حقول اختيارية (`discount_amount` وغيرها) تبقى `None` على كائنات مؤقتة (transient)
   غير محفوظة، لأن الافتراضي بـSQLAlchemy (`default=0`) لا يُطبَّق إلا عند `flush()`
   الفعلي — أُصلح جذرياً بـ`invoice_calc.py` نفسه (دالة `_safe()`)، مو فقط بالواجهة،
   لأن أي كود مستقبلي غير الواجهة قد يقع بنفس الفخ
2. `itemChanged` بـQTableWidget يُطلَق فوراً أثناء التعبئة البرمجية الجماعية (وسط
   تعبئة صف لسه ما اكتمل)، يسبب استثناء عند محاولة إعادة حساب مبكرة — أُصلح بتعطيل
   الإشارات (`blockSignals`) أثناء التعبئة الجماعية + حراسة دفاعية إضافية بـ`_recalculate_totals`

كل الشاشات بنطاق v1 مبنية ومختبرة الآن. الناقص: فواتير الشراء/المرتجعات بالواجهة
(تكرار نمط `SalesInvoiceFormView` نفسه)، شاشة سند القيد اليدوي، شاشة الإعدادات،
شاشة اختيار/إنشاء عميل حقيقية (حالياً `main.py` بها placeholder مبسّط).

**جلسة 5 (2026-08-22)**: المستخدم شغّل الواجهة فعلياً على جهازه وأرسل screenshot
حقيقي لصديقه المبرمج — أول مراجعة UI مبنية على استخدام فعلي، مو نقاش نظري.
اتُّفق على 95% من ملاحظاته: حقول رأس بعرض ثابت (لا عرض شاشة كامل)، شارة حالة
ملوّنة (مسودة=برتقالي/مرحّلة=أخضر/ملغاة=أحمر)، صفوف جدول أقصر (34px)، كتلة
إجماليات يمينية مميّزة بالصافي بخط أكبر، زر "ترحيل" أساسي بارز والبقية ثانوية،
تقليل الحدود لصالح خلفيات وبطاقات بسيطة. تحفّظ واحد: تقسيم الفاتورة لملفات
مكوّنات منفصلة (`InvoiceHeader`, `InvoiceInfo`...) أُجِّل لحين بناء فاتورة
الشراء فعلياً (نفس منطق تأجيل تقسيم `models.py`) — نُظِّم بدلاً منه كدوال بناء
واضحة `_build_document_header/_build_document_info/_build_lines_grid/...` بنفس
الملف. أُعيد بناء `invoice_form.py` بالكامل، **اختُبر end-to-end من جديد بعد
التجميل** (headless): حساب حي، حفظ، ترحيل، شارة الحالة تتحدّث صح، ميزان متوازن
(55=55) — لم ينكسر أي منطق بسبب إعادة التصميم البصري.

**جلسة 6 (2026-08-22)**: مراجعة ثالثة أدق من صديق المستخدم — ميّز بوضوح بين تجميل
وضوابط محاسبية حقيقية (تحقق ما قبل الترحيل = ثغرة فعلية، ليس UX). اعتُرض على اقتراح
واحد: الانتقال لـQTableView+Model/Delegate بدل QTableWidget — أُجِّل لحين وجود عائق
فعلي بمحرر خلايا متقدم (معيار تفعيل واضح مسجَّل، مو رفض نهائي).

نُفِّذ من "المرحلة 1" التي رتّبها: `invoice_validation.py` (تحقق مفروض من `posting.py`
نفسه — لا يعتمد على الواجهة، أي طريق ترحيل آخر محمي تلقائياً)، عرض أعمدة ثابت
(المادة فقط Stretch)، تنقل Enter حقيقي بين الخلايا (كود→كمية→سعر→صف جديد)، تأكيد
قبل الترحيل (`QMessageBox.question`)، اختصارات `Ctrl+S`/`Ctrl+Enter`. اختصارات
F2/F4/F8 والبحث المنبثق الكامل وحقول الأسعار الخاصة بالعميل أُجِّلت للمرحلتين 2/3
كما رتّبهما صديق المستخدم نفسه — لا خلاف على الترتيب.

**اختبار حاسم جديد**: تم التحقق من أن `post_sales_invoice()` يرفض فاتورة فاسدة
(بدون عميل + كمية صفر) **مباشرة من طبقة الخدمة نفسها**، بمعزل تام عن الواجهة —
هذا يثبت الضابط المحاسبي مفروض بشكل صحيح، لا يعتمد فقط على تحقق الواجهة.

**جلسة 7 (2026-08-22)**: المستخدم بعت screenshot حقيقي من جهازه (Windows) يوضّح
"لم يتحسن شيء" — بدل التخمين، أخذنا screenshot فعلي بأنفسنا عبر `QWidget.grab()`
بوضع headless، وطلعت **نفس المشكلة بالضبط**: شارة الحالة قافزة لأقصى يسار الشاشة
منعزلة عن العنوان، بسبب ترتيب خاطئ لعناصر QHBoxLayout مع RTL (`addStretch()` قبل
الشارة بدل بعدها). صُحِّح فوراً. كمان لاحظنا مساحة بيضاء ضخمة بمنطقة الجدول —
سببها الحقيقي: سطر واحد فاضي فقط يشغل مساحة `stretch=1` كاملة. صُحِّح بالبدء
بـ8 أسطر فاضية بدل سطر واحد (نفس منطق "دفتر محاسبي ممتلئ" الذي طلبه صديق
المستخدم). أُضيفت حدود خفيفة (`border: 1px solid #E5E7EB`) للبطاقات لتمييزها
بصرياً عن الخلفية. **أُعيد أخذ screenshot بعد الإصلاح وتأكدنا بصرياً من النتيجة
قبل الإرسال** — هذا نمط جديد مهم: لا نطلب من المستخدم يختبر شي ما تأكدنا منه
بصرياً بأنفسنا أولاً، طالما توفرت أداة screenshot الآلية.

## 10. نمط فتح الشاشات — قرار مُراجَع (نوافذ منبثقة بدل Workspace Tabs)

**تصحيح صريح عن التصميم الأصلي المتفق عليه**: القرار الأولي (موثّق بـ`UI_DESIGN.md`)
كان "Main Window → Workspace Tabs، لا نوافذ فوضوية". **المستخدم غيّر هذا القرار
صراحة**: كل عنصر بالقوائم الرئيسية (عدا "الرئيسية") يفتح الآن كنافذة Qt منفصلة
غير حابسة (non-modal)، متتبَّعة بعنوانها لمنع التكرار (فتح نفس الشاشة مرتين
يُظهر الموجودة بدل نسخة جديدة). هذا ليس خطأ أو تراجعاً غير مقصود — قرار واعٍ
مسجَّل هنا صراحة حتى لا يُفهم لاحقاً كتضارب مع القاعدة الأصلية.

`MainWindow._open_window(title, widget)` هو النمط الموحّد الجديد لكل الشاشات
(دليل الحسابات، قوائم الفواتير/المرتجعات، نماذج الفواتير/المرتجعات). الشاشة
الرئيسية (Dashboard) وحدها تبقى ضمن النافذة الأم مباشرة.

**مُختبر بنجاح** (headless): فتح نفس الشاشة مرتين لا يفتح نسخة مكررة، فتح 3
شاشات مختلفة ينتج 3 نوافذ Qt مستقلة فعلياً، والترحيل الكامل لفاتورة من داخل
نافذة منبثقة يعمل ويُنتج ميزان مراجعة متوازن (50=50) — نفس سلامة النظام
بغض النظر عن نمط عرض الشاشات.

## 11. سجل الجلسات (تكملة)

**جلسة 8 (2026-08-23)**: المستخدم وصديقه عدّلا `invoice_form.py`, `invoice_list.py`,
`return_form.py`, `return_list.py` مباشرة على GitHub (`My_Acc_App`) وطلبا مراجعة +
تحديث المرجعية + تنفيذ نمط النوافذ المنبثقة. استُنسخ المستودع ورُوجعت التعديلات:

**تحسينات حقيقية من المستخدم وصديقه** (مو مجرد تجميل): إصلاح باغ حقيقي بـ`_build_document_info`
(كان `addLayout` مع colspan غلط، صار `addWidget` بـwrapper QWidget صحيح)، إضافة
`_close_current_editor()` قبل أي تعديل جماعي على الجدول (يمنع تعارض محرر خلية مفتوح)،
فصل أنماط تغيير حجم الأعمدة (`Fixed` صراحة لكل عمود عدا `المادة` = `Stretch`)، بناء
شاشتي مرتجع البيع كاملتين (`return_form.py`, `return_list.py`) بنفس النمط المرجعي.

**باغ حقيقي اكتُشف وصُحِّح بجلستنا هذي**: شارة الحالة الملوّنة بقوائم الفواتير
والمرتجعات كانت تستخدم `QTableWidgetItem.setText()` بنص HTML — **`QTableWidgetItem`
لا يدعم HTML إطلاقاً**، فكانت الخلية ستعرض فعلياً وسوم `<span style=...>` كنص خام
حرفي، لا شارة ملوّنة. **تأكَّد الباغ بتشغيل فعلي قبل التصحيح** (النص الحرفي ظهر
بالضبط كما توقعنا)، ثم أُصلح بالطريقة الصحيحة (`setCellWidget` بـ`QLabel` منسَّق
داخل `QWidget` حاوٍ)، **وتأكَّد الإصلاح بتشغيل فعلي وlقطة شاشة حقيقية بعده**.

نُفِّذ نمط النوافذ المنبثقة الكامل (`MainWindow._open_window`) بناءً على طلب
المستخدم الصريح — راجع القسم 10 أعلاه للتفاصيل والمبرر.

**تنويه مهم**: Claude لا يملك صلاحية push مباشر لمستودع GitHub الخاص بالمستخدم —
كل المراجعة والتعديل تمّا محلياً على نسخة مُستنسخة (`git clone`)، والملفات
المعدَّلة تُرسَل للمستخدم ليستبدلها ويرفعها بنفسه.

## 12. سجل الجلسات (تكملة)

**جلسة 9 (2026-08-23)**: طُلب بناء فواتير/مرتجعات الشراء الأربعة مع 4 مطالب:
مرتجع مربوط بفاتورة أصلية أو حر، تنسيق أرقام حي بفاصلة آلاف، فاتورة البيع كأساس
بصري وسلوكي موحّد، ومؤشر يبدأ من يمين الخلايا.

راجعنا رأي مبرمج ثانٍ (8.5/10 حسب تقييمه) — **لم نعتمده 100% بشكل أعمى**، صححنا
نقطتين: (أ) "Return Mode" كحقل enum منفصل غير ضروري — `original_invoice_id`
nullable يحقق نفس التمييز أصلاً، (ب) تفكيك الفورم لـ5 مكوّنات منفصلة (Header/
Meta/LinesTable/Totals/Actions) مؤجَّل — الصنف الأساسي الموحّد `BaseDocumentFormView`
يحقق نفس هدف "لا تكرار" بدون تفكيك إضافي غير مُستخدَم فعلياً بعد. **اعتمدنا
بالكامل** نقطته الأهم: منع تجاوز الكمية القابلة للإرجاع عبر مرتجعات متعددة
متراكمة — كانت فجوة حقيقية موثّقة كـ"معروفة، غير مُنفَّذة"، صارت الآن ضابطاً
مفروضاً بالخدمة.

**بُني**: `app/ui/common/numeric_delegate.py` (تنسيق فاصلة آلاف عبر `displayText`
فقط — القيمة المخزَّنة تبقى نظيفة `Decimal` دائماً، لا يُخزَّن نص منسَّق أبداً)،
`app/ui/common/document_form.py` (`BaseDocumentFormView` — الأساس الموحّد لكل
المستندات الأربعة، بديل حرفي لتكرار 4 نسخ)، `app/ui/common/document_list.py`
(نفس المبدأ للقوائم)، `app/services/returns.py` (`post_sales_return`,
`post_purchase_return`, `get_returnable_lines`, `validate_return_against_original`,
`get_already_returned_quantity`). أربع شاشات رفيعة الآن فوق الأساس المشترك:
فاتورة/قائمة شراء، مرتجع/قائمة بيع، مرتجع/قائمة شراء.

**اختبار end-to-end كامل عبر مسار المستخدم الفعلي بالنافذة الرئيسية**: فاتورة
شراء → فاتورة بيع → مرتجع بيع مربوط (سحب العميل والسعر تلقائياً من الفاتورة
الأصلية، تعديل الكمية المُرجعة يدوياً) → محاولة مرتجع ثانٍ متجاوز للكمية
المتبقية (رُفض برسالة دقيقة: الأصل/المُرجَع سابقاً/المطلوب) → ميزان مراجعة
نهائي متوازن (530=530) عبر كل هالعمليات مجتمعة. **لقطة شاشة فعلية** تؤكد: فاصلة
آلاف ظاهرة بكل الحقول الرقمية والإجماليات، رمز العملة `ل.س` لاصق بكل مبلغ،
حقل الربط بالمستند الأصلي وزر السحب شغّالان بصرياً.

**فجوات معروفة متبقية**: تنسيق العرض (رمز قبل/بعد الرقم) وفاصلة عشرية قابلة
للتخصيص مؤجّلان لشاشة الإعدادات المستقبلية كما قرر المستخدم صراحة — الحساب
والعرض منفصلان تماماً بالتصميم الحالي فلا كلفة تقنية لتأجيلهما.

## 13. مبدأ ثابت: الفصل بين القيمة المحاسبية والعرض (Presentation Formatting)

**الفاصلة، رمز العملة، وعدد المنازل العشرية هي طبقة عرض فقط — ليست جزءاً من
القيمة المخزَّنة أو المُحتسَبة.** القيمة الفعلية بكل مكان بالنظام هي `Decimal`
نظيف بدون أي تنسيق (`app/services/money.py`)، والتنسيق يُطبَّق فقط لحظة العرض
(`NumericGridDelegate.displayText()`, `format_currency()`). هذا يعني أي إعداد
مستقبلي لشكل الفاصلة العشرية (`1,234.56` أو `1.234,56` أو `1 234,56`) سيكون
تغييراً بطبقة العرض فقط، صفر تأثير على الحسابات أو البيانات المخزَّنة.

**محاذاة الجدول** (مبدأ ثابت، لا يُخلَط بينه وبين اتجاه RTL العام للواجهة):
- نصوص (اسم المادة، البيان): تتبع اتجاه RTL الطبيعي للواجهة
- أرقام (كمية، سعر، حسم، ضريبة، إجمالي): محاذاة يمين دائماً (`AlignRight`)،
  بغض النظر عن اتجاه الواجهة — لأنها قيم رقمية LTR بطبيعتها
- تواريخ: محاذاة يمين أيضاً بنفس المنطق

## 14. سجل الجلسات (تكملة)

**جلسة 10 (2026-08-23)**: صديق المستخدم رفض قبول "end-to-end tested" كدليل كافٍ
على صحة القيود — نقطة محقة تماماً: **ميزان مراجعة متوازن إجمالياً لا يثبت أن كل
مبلغ ذهب للحساب الصحيح**، فقط أن مجموع المدين = مجموع الدائن. طلب تحققاً على
مستوى كل حساب لحاله لكل سيناريو (شراء، بيع، مرتجع بيع، مرتجع شراء)، بالإضافة
لاختبار رفض صنف غير موجود بالفاتورة الأصلية، وتسلسل دقيق لمنع تجاوز الكمية.

**نُفِّذ التحقق الدقيق وكل الحالات الست نجحت بأرقام مطابقة تماماً للمتوقع
محاسبياً**: فاتورة شراء (مخزون +200 دائن صندوق -200، COGS=0 تأكيداً)، فاتورة
بيع (كل الحسابات الأربعة بمبالغها الدقيقة)، مرتجع بيع (**عكس بكلفة الوحدة
الأصلية وقت البيع، لا المتوسط المرجّح الحالي وقت المرتجع** — فرق دقيق تم
التحقق منه صراحة)، مرتجع شراء (صندوق ومخزون فقط، صفر تأثير على COGS/المبيعات)،
رفض صنف غريب عن الفاتورة الأصلية، ورفض مرتجع يتجاوز المتبقي (10→6→رفض5،
لأن المتاح 4). راجع `/tmp` بجلسة العمل — الاختبار الكامل بالتفاصيل موثّق
بسجل هذه الجلسة.

تثبيت مبدأ الفصل بين القيمة المحاسبية والعرض بالقسم 13 أعلاه، بناءً على طلب
صديق المستخدم — لا تغيير كود مطلوب، كان مُطبَّقاً أصلاً، فقط توثيق رسمي.

**الأولوية التالية متفق عليها**: سند القيد اليدوي (واجهة)، ثم دليل الحسابات
(واجهة تعديل، الحالي عرض فقط)، دليل المواد، كشف الحساب المُجمَّع بالواجهة،
المخزون، الحسابات الختامية بالواجهة.

## 15. سجل الجلسات (تكملة)

**جلسة 11 (2026-08-23)**: بُني سند القيد اليدوي حسب المواصفات الثمانية التي
ثبّتها صديق المستخدم كـAcceptance Criteria قبل البدء. `journal_edit.py` عُزِّز
بتحقق صارم (`_validate_lines`): حساب مطلوب، مبلغ واحد فقط بالسطر (مو مدين ودائن
معاً، ولا صفر بالاثنين) — الفحص انتقل ليحصل **عند الإضافة مباشرة** (`add_manual_line`)
بدل انتظار الترحيل، برسائل عربية واضحة بدل استثناء SQL خام.

**باغ حقيقي خطير اكتُشف وصُحِّح أثناء تنفيذ الاختبارات الثمانية المطلوبة**:
`ledger.py`, `trial_balance.py`, `rollup.py` كانت **تحسب القيود غير المرحّلة
(DRAFT) ضمن الأرصدة** — أي قيد يُضاف (حتى لو فشل ترحيله لعدم توازنه) كانت
حركاته تدخل كشف الحساب وميزان المراجعة فوراً عند `flush()`، بغض النظر عن حالته.
هذا يعني **قوائم الدخل والميزانية العمومية كانت معرَّضة لنفس الخطأ من قبل**
(كلاهما يعتمد على `rollup.py`). صُحِّح بإضافة `JournalEntry.status ==
JournalEntryStatus.POSTED` كشرط صريح بكل استعلامات الأربعة ملفات. **هذا الباغ
لم يكن ليُكتشف لولا إصرار صديق المستخدم على "لا يكفي Debit=Credit، تحقق من كل
حساب لحاله" من الجلسة السابقة** — نفس المبدأ كشف مشكلة أعمق بجلسة لاحقة.

بُني `app/ui/accounting/journal_voucher_form.py` (Grid-first: رمز حساب → بحث
تلقائي → بيان → مدين/دائن، فرق لحظي ملوّن أخضر/أحمر) و`journal_voucher_list.py`.
**اختبار end-to-end كامل عبر النافذة الرئيسية**: قيد "توريد رأس مال" (صندوق مدين
15,000 / رأس مال دائن 15,000) — الفرق اللحظي أظهر "متوازن" بشكل صحيح، الترحيل
نجح، ورصيد كل حساب صحيح بالضبط بكشف الحساب (15,000 لكل من الاثنين). **لقطة شاشة
فعلية مأخوذة وتؤكد التصميم البصري مطابق للمواصفة** (بطاقة رأسية، شارة حالة خضراء
"مرحّلة"، جدول بفاصلة آلاف، كتلة إجماليات مع فرق ملوّن).

## 16. سجل الجلسات (تكملة)

**جلسة 12 (2026-08-23)**: نُفِّذت 6 تحسينات بصرية على سند القيد (هيدر مضغوط،
حقول بعرض منطقي، شريط ملخص سفلي موحّد، تعطيل زر الترحيل تلقائياً لما القيد
غير متوازن). كُشف باغ RTL بترتيب العنوان (نفس نمط باغ سابق بفاتورة البيع) وصُحِّح.

**تصحيح جذري بمنطق التوازن المحاسبي بكل النظام**: `JournalEntry.is_balanced()`
كان يقارن `debit`/`credit` الخام (عملة المعاملة) بدل `debit_base`/`credit_base`
(العملة الأساسية) — اشتغل صدفة لغاية الآن لأن كل قيد كان بعملة واحدة موحّدة.
بالإضافة، `ledger.py`, `trial_balance.py`, `rollup.py` **الثلاثة كانوا يعانون
من نفس الخطأ** — يجمعون القيم الخام بدل الأساسية، يعني **أي قيد بعملة أجنبية
حقيقية (سعر صرف ≠ 1) كان يظهر برصيد غلط بكل تقرير بالنظام من البداية**، بس
ما انكشف لأنه ما فيه اختبار سابق استخدم سعر صرف ≠ 1 مع تقرير فعلي. صُحِّح
الأربعة (النموذج + 3 ملفات تقارير) ليعتمدوا `debit_base`/`credit_base` حصراً.

**عملة على مستوى السطر (بدل القيد بالكامل) لسند القيد اليدوي فقط الآن** —
بناءً على نقاش عميق مع صديق المستخدم حول حالات محاسبية حقيقية (تحويل دولار
لليرة بقيد واحد). `JournalLine` صار فيه `line_currency_code`/`line_exchange_rate`
اختياريان (migration v5، NULL = يرث عملة القيد الافتراضية، لا يؤثر على أي قيد
قديم). الواجهة تعرض عمودي "العملة"/"سعر الصرف" بالجدول، مع عملة افتراضية بالرأس.
**نطاق محدود عمداً**: الفواتير (بيع/شراء/مرتجعات) بقيت بعملة واحدة على مستوى
المستند بالكامل — توسعتها لعملة-لكل-سطر قرار منفصل مستقبلي لو احتاجه المستخدم
فعلياً، مو افتراضاً الآن.

**اختبار end-to-end**: قيد بسطرين (100 USD بسعر صرف 100، و10,000 SYP مباشرة)
رُحّل بنجاح، متوازن بالعملة الأساسية بدقة (10,000=10,000)، وميزان المراجعة
الكلي متوازن.

**باغ حقيقي في إدارة النوافذ المنبثقة اكتُشف وصُحِّح**: `QWidget` العادي عند
إغلاقه (X) يُخفى فقط بدل أن يُدمَّر فعلياً (سلوك Qt الافتراضي بدون
`Qt.WA_DeleteOnClose`) — فيبقى "موجوداً" بقاموس `_open_windows` لكن مخفياً
للأبد، وإعادة فتح نفس الشاشة تفشل لأنها تحاول `raise_()` على نافذة مخفية بدل
إنشاء واحدة جديدة. أُصلح بسطر واحد (`window.setAttribute(Qt.WA_DeleteOnClose)`).
**مُختبر بدقة على الشاشات العشرة كلها** (فواتير/مرتجعات بيع وشراء بقوائمها
ونماذجها + سندات القيد وقائمتها): فتح → إغلاق → إعادة فتح — كلها نجحت.

## 17. سجل الجلسات (تكملة) — مصفوفة اختبارات تعدد العملات

**جلسة 13 (2026-08-23)**: صديق المستخدم لم يعتبر تعدد العملات "مكتملاً" بدون
14 اختبار محدد. نُفِّذت جميعها (14/14 نجح)، وكشفت فجوة حقيقية وباغ رسالة صغير:

**فجوة حقيقية سُدَّت**: لم تكن هناك دالة عكس (`reverse_manual_entry`) لسند
قيد يدوي مرحّل إطلاقاً — أُضيفت الآن بـ`journal_edit.py`، تحافظ على نفس
`debit_base`/`credit_base` الأصليين بالضبط (لا إعادة تسعير بسعر صرف اليوم).

**باغ رسالة خطأ صُحِّح**: رسالة "القيد غير متوازن" كانت تحسب الفرق بجمع
`debit`/`credit` الخام (عملات مختلفة تُطرح من بعضها مباشرة — رقم بلا معنى
محاسبياً، ظهر فعلياً كـ"8.57" بدل الفرق الحقيقي "50.00 SYP"). صُحِّحت لاستخدام
`debit_base`/`credit_base` بالرسالة أيضاً، لا فقط بفحص القبول/الرفض.

**تأكيد عملي لمخاوف صديق المستخدم بنقطة التقريب**: قيد بمبلغ يورو "مقرَّب
منطقياً" (91.43€ بدل 91.428571...€) ينتج فرق حقيقي 50 ل.س بالعملة الأساسية —
**هذا اترفض من النظام بوضوح تام، لا يُقبل بصمت ولا يُهمَل**. القرار المُتخذ:
لا تسامح تلقائي (tolerance) بفحص التوازن — أي فرق تقريب حقيقي، مهما صغر،
يتطلب من المحاسب حلاً يدوياً (تعديل رقم أو سطر فرق صرف صريح). ميزة "سطر فرق
صرف تلقائي" مؤجَّلة لحين حاجة فعلية مؤكَّدة، لا افتراضية.

**النقاط الأربع الأخرى من ملاحظات صديق المستخدم — موجودة أصلاً، موثَّقة الآن رسمياً**:
- تعريف سعر الصرف ثابت بكل النظام: **1 وحدة عملة أجنبية = X وحدة عملة أساسية**
  (`debit_base = debit × exchange_rate`) — لا عكس هذا التعريف بأي مكان
- `Account.currency_code` موجود أصلاً بالـschema (غير مُستخدَم فعلياً بعد لفرض
  عملة محددة على حساب معيّن) — الأساس جاهز لو احتجنا "Account Currency
  Restriction" مستقبلاً، بدون أي تعديل schema إضافي
- عملة القيد بالرأس = افتراضي فقط لتسريع الإدخال، الحقيقة المحاسبية دائماً
  بمستوى `JournalLine` — هذا هو التصميم المُنفَّذ فعلياً
- فروقات إعادة تقييم العملة (FX revaluation / Home Currency Adjustment):
  **مؤجَّلة عمداً**، تحتاج حسابات أرباح/خسائر عملة مخصصة ومنطق دوري منفصل —
  ستُبنى عند الوصول لشاشات الذمم والمدفوعات، لا قبل ذلك

مصفوفة الاختبارات الكاملة (14): عملة واحدة (SYP)، عملتين متطابقتين (USD/USD)،
مختلطتين بالاتجاهين (USD↔SYP)، ثلاث عملات بقيد واحد، Ledger/Trial
Balance/قائمة الدخل بسعر صرف≠1، رفض فرق تقريب حقيقي، منع تعديل POSTED، عكس
يحافظ على نفس التعادل، وسعر الصرف كلقطة تاريخية ثابتة (لا يتأثر بإعادة تحميل
الجلسة). **أساس تعدد العملات بسند القيد يُعتبر الآن قوياً ومختبراً بدقة —
جاهزون للانتقال لدليل الحسابات كما اقترح صديق المستخدم.**

## 18. سجل الجلسات (تكملة)

**جلسة 14 (2026-08-23)**: صديق المستخدم لم يقبل "14/14" كدليل كافٍ بدون تحديد
الفئات، وكشف فجوتين حقيقيتين بالعكس (`reverse_manual_entry`) لم تُختبرا سابقاً:

**فجوتان سُدَّتا**:
1. لا شيء كان يمنع عكس قيد هو نفسه عكس لقيد آخر (عكس العكس) — أُضيف تحقق صريح
2. لا شيء كان يمنع عكس نفس القيد الأصلي مرتين — أُضيف تحقق يبحث عن عكس موجود
   مسبقاً (`is_reversal_of`) قبل السماح بعكس ثانٍ

**اختبار حاسم جديد لم يكن موجوداً**: التأكد الصريح أن `reverse_manual_entry`
يحافظ على القيمة الأساسية **التاريخية** بالضبط حتى لو "تغيّر" سعر الصرف اليوم
(محاكاة: قيد أصلي بسعر 12,000، عكسه ينتج نفس 1,200,000 وليس 1,300,000 حتى لو
افترضنا سعراً جديداً 13,000 وقت العكس) — **نجح**، لأن النظام أصلاً لا يخزّن أي
"سعر صرف حي" مرجعي، كل قيد يحمل سعره الخاص كلقطة ثابتة دائماً.

**رفض اقتراح إضافة Alembic** — تراجع عن قرار سابق (نمط `PRAGMA user_version`)
بدون مبرر جديد؛ السبب الأصلي (schema لسه بيتغيّر بسرعة) لسه صحيح، والنمط
الحالي أثبت نفسه بـ5 إصدارات ناجحة.

**حُسم سؤال `Account.currency_code`**: الحقل موجود ويبقى **وصفياً فقط** الآن
(يخبر أي عملة "متوقعة" لهذا الحساب) — **غير مفروض برمجياً**، أي `JournalLine`
يقبل أي عملة على أي حساب حالياً بدون تحقق تطابق. توثيق رسمي بـ`models.py` نفسه
يوضح هذا القرار، حتى لا يُفهم لاحقاً كسهو. فرض التطابق قرار منفصل مستقبلي لو
احتجناه فعلياً (مثلاً: منع قيد ليرة على "صندوق دولار") — الـschema لا يمنعنا،
لكن لا حماية فعلية مبنية الآن.

مصفوفة الاختبارات الكاملة الجديدة (15، تغطي كل الأربعة عشر فئة التي حددها):
عملة أساسية، عملة أجنبية، عملتان متعادلتان، عملتان غير متعادلتين (رفض)، فرق
تقريب حقيقي (رفض)، رسالة الفرق بالعملة الأساسية، سعر صرف تاريخي ثابت، عكس
متعدد العملات يحافظ على القيمة الأصلية، منع عكس العكس، منع عكس مزدوج، منع
تعديل/حذف POSTED، DRAFT غائب عن التقارير، POSTED ظاهر بالتقارير، توازن الميزان
الكلي بعد كل العمليات. **أساس تعدد العملات بسند القيد مُثبَت بدقة عالية الآن —
الانتقال لدليل الحسابات معتمد.**

## 19. سجل الجلسات (تكملة) — تحديث ختامي قبل محادثة جديدة

**جلسة 15 (2026-08-24)**: مراجعة مفصّلة (22 نقطة) من صديق المستخدم على تصميم
جدول سند القيد بعد دعم العملة على مستوى السطر، مبنية على screenshots فعلية
من ثلاث شاشات (سند القيد، فاتورة بيع، فاتورة شراء). راجعنا الصور أولاً — لا
حاجة لتعديل نصوص/labels/comboboxes بالفواتير، التصميم متسق وجاهز.

**نُفِّذ بالكامل**:
- عمودا "مدين بالأساسية" و"دائن بالأساسية" — محسوبان آلياً (`quantity × سعر
  الصرف الفعلي`)، **غير قابلين للتحرير اليدوي إطلاقاً** (مُختبر: `flags()` تؤكد
  عدم القابلية للتحرير)، ومرتَّبان **آخر الجدول متجاورين** بناءً على طلب صريح
  (بدل كل عمود أساسي بجانب مدين/دائن الأصلي مباشرة)
- `CurrencyComboDelegate` — عمود العملة صار ComboBox حقيقي (SYP/USD/TRY/EUR/فارغ)
  بدل نص حر عرضة لأخطاء إملائية، بالضبط كما طلب صديق المستخدم بالنقطة 17
- الفرق اللحظي وزر الترحيل يعتمدان حصراً على `debit_base`/`credit_base`
  المحسوبة لكل سطر — لا جمع لعملات مختلفة مباشرة بأي مكان

**باغ حرج اكتُشف وصُحِّح أثناء أول لقطة شاشة فعلية بعد التنفيذ**: الفرق ظهر
`8E-8` بدل `0` بالضبط بعد إعادة تحميل قيد محفوظ. السبب الجذري: `exchange_rate`
يُقرأ من SQLite (تخزين `REAL` تقريبي، لا Decimal حقيقي — نفس القيد الموثّق
بـ`money.py` من بداية المشروع، لكن كان محمياً فقط للمبالغ المالية عبر `money()`،
**لا لسعر الصرف نفسه**). قيمة مثل `1.0` كانت ترجع `0.999999...` وتتضخّم لفرق
ملحوظ عند الضرب بمبالغ كبيرة. **الإصلاح الجذري**: دالة جديدة `rate()` بـ
`money.py` (تقريب 6 منازل، نفس مبدأ `money()` لكن بدقة مناسبة لأسعار الصرف لا
2 منزلة)، طُبِّقت بكل مكان يُقرأ فيه سعر صرف من القاعدة أو يُستخدَم بحساب:
`journal_edit.py` (`add_manual_line`)، و`journal_voucher_form.py` (تحميل القيد،
الحساب اللحظي). كذلك `debit_base`/`credit_base` المحسوبة بالواجهة صارت تُقرَّب
بـ`money()` (منزلتين) بعد الضرب، لا تبقى بدقة 6 منازل السعر — إصلاح تجميلي
مكمّل ضروري لعرض نظيف. **مُختبر ومؤكَّد بصرياً بعد الإصلاح**: الفرق `0.00 ✓
متوازن` بالضبط، لا أي أثر عائم متبقٍ.

**تنبيه مهم لأي محادثة قادمة تقرأ هذا الملف**: أي مكان جديد يتعامل مع
`exchange_rate` مستقبلاً (تقارير، شاشات جديدة، خدمات) **يجب** يمرّ عبر
`rate()` من `app/services/money.py`، لا `Decimal()` مباشرة — نفس قاعدة
`money()`/`qty()` القديمة، الآن تشمل سعر الصرف صراحة.

**حالة المشروع عند إغلاق هذه المحادثة**: محرك المحاسبة الأساسي (فواتير، مرتجعات،
سند قيد، تعدد عملات، تقارير) مبني ومختبر بعمق. الواجهة: فواتير بيع/شراء
ومرتجعاتها + سند القيد جاهزون بالكامل (نمط نوافذ منبثقة). **الخطوة التالية
المتفق عليها**: دليل الحسابات (واجهة تعديل فعلية، الحالي عرض شجري فقط) —
راجع القسم 16 (نموذج `Account`) قبل البدء.

## 20. القاعدة الرسمية لسند القيد متعدد العملات (مرجع ثابت — لا يُعدَّل بدون مراجعة)

**لكل سطر بسند قيد**: عملة أصلية، مبلغ مدين أو دائن أصلي، سعر صرف، ومعادل
بالعملة الأساسية (`debit_base`/`credit_base`).

**علاقة ديناميكية بالاتجاهين** (طلب صريح، تصحيح عن قرار "read-only" أولي):
- تعديل **المبلغ** أو **سعر الصرف** → المعادل الأساسي يُحسب تلقائياً (`مبلغ × سعر = معادل`)
- تعديل **المعادل الأساسي** مباشرة → **سعر الصرف** يُستنتج تلقائياً (`معادل ÷ مبلغ = سعر`)، **والمعادل نفسه لا يُعاد حسابه** — يبقى بالضبط كما كتبه المحاسب
- آخر حقل عدّله المستخدم هو مصدر الحقيقة لتلك اللحظة، بدون تسلسل حسابي دائري

**التوازن دائماً**: `Σ debit_base == Σ credit_base` — لا مقارنة مباشرة بين
مبالغ بعملات مختلفة إطلاقاً (100 USD لا تُطرح من 90 EUR مباشرة بأي مكان بالكود).

**لا إصلاح تلقائي للفرق مهما كان صغيراً**: لو `debit_base != credit_base`،
الفرق يظهر بوضوح (أحمر، رقم دقيق)، **زر الترحيل يُعطَّل تلقائياً**، ولا يوجد
tolerance. القرار محاسبياً للمستخدم فقط:
1. تصحيح مبلغ أو سعر صرف خاطئ، أو
2. إضافة سطر يدوي لحساب "أرباح/خسائر فروقات عملة" حتى يتوازن القيد فعلياً

**النظام لا يفترض أبداً أن أي فرق هو فرق صرف** — هذا قرار محاسبي بشري صرف.

**سعر الصرف لقطة تاريخية ثابتة دائماً**: لا يُعاد حساب أي قيد قديم عند تغيّر
سعر الصرف لاحقاً. إعادة تقييم أرصدة عملات أجنبية (IAS 21 / Home Currency
Adjustment) عملية محاسبية مستقلة تُنشئ قيد فروقات صرف جديد — **مؤجَّلة عمداً**،
لم تُبنَ الآن، لكن التصميم الحالي لا يمنعها مستقبلاً.

**دقة الأرقام**: المبالغ المالية (بما فيها المعادل الأساسي) بمنزلتين عبر
`money()`، سعر الصرف بست منازل عبر `rate()` (`app/services/money.py`) —
**دقتان منفصلتان تماماً، لا تُخلطان**. أي كود جديد يتعامل مع سعر صرف يمرّ
عبر `rate()` حتماً، لا `Decimal()` مباشرة (نفس التحذير من الجلسة السابقة،
مؤكَّد هنا رسمياً كقاعدة دائمة).

## 20.1 عملة الحساب ≠ عملة الحركة (مرجع ثابت — لا يُعدَّل بدون مراجعة)

**القرار المعماري الرسمي** (بنفس صياغة الاتفاق، لتُقرأ حرفياً مستقبلاً بلا لبس):

> **Currency of Account ≠ Transaction Currency.**
> عملة الحساب لا تفرض بالضرورة عملة الحركة. العملة الفعلية للحركة تُحدَّد
> على مستوى سطر القيد، ويُسجَّل المعادل بالعملة الأساسية، مع إمكانية تقييد
> بعض الحسابات بعملة معينة لاحقاً إذا أثبتت قواعد العمل الحاجة لذلك.

**السبب** (وليس مجرد قاعدة تعسفية): حساب "الصندوق" أو "البنك" أو "العملاء"
قد تُسجَّل عليه حركات بعملات مختلفة فعلياً (دفعة عميل بالدولار على حساب
عملاء أساسه SYP، شراء آلة بالدولار وتسجيلها بحساب أصول ثابتة أساسه SYP...).
منع هذا برمجياً ("حساب بالدولار لا يقبل إلا دولاراً") تصميم خاطئ لنظام
متعدد العملات حقيقي — **العملية بحد ذاتها هي من تحمل عملتها**، لا الحساب.

**3 مفاهيم منفصلة يجب ألا تُخلَط** (كل واحد له دور مختلف بالمخطط):
1. **العملة الأساسية للشركة** (مثلاً SYP) — عملة القياس بالتقارير والتوازن.
2. **عملة الحساب الافتراضية/المفضّلة** (اختيارية، وصفية فقط حالياً — حقل
   `Account.currency_code` الموجود مسبقاً) — لا تمنع حركة بعملة أخرى.
3. **عملة الحركة الفعلية** — تُحدَّد فقط على مستوى سطر القيد (`JournalLine`)،
   ولها سعر صرف ومعادل أساسي خاصان بها، بالضبط كما بُني بالقسم 20.

**كل القيود تُحوَّل للعملة الأساسية لأغراض التوازن والتقارير، مع الاحتفاظ
دوماً بالعملة الأصلية والمبلغ الأصلي وسعر الصرف لكل حركة** — لا نقول أبداً
"الأصول/الالتزامات/المصروفات/الإيرادات تُسجَّل دائماً بالعملة الأساسية"،
هذه صياغة خاطئة محاسبياً.

**حالة خاصة يجب تذكّرها عند بناء كشف الحساب لاحقاً**: حساب واحد (كالبنك)
قد يحتوي حركات SYP وUSD وEUR معاً. **لا يجوز جمع مبالغ بعملات مختلفة
بالعملة الأصلية مباشرة بأي مكان** (لا `100 USD - 90 EUR` مباشرة إطلاقاً) —
كشف الحساب يعرض المبلغ الأصلي بعملته + المعادل الأساسي بعمود منفصل، والرصيد
الإجمالي المُجمَّع دائماً بالعملة الأساسية فقط. **لم يُبنَ هذا التمييز بعد
بـ`account_statement_dialog.py` الحالي** (يعرض معادلاً أساسياً فقط دون عمود
عملة أصلية منفصل لكل سطر) — فجوة معروفة، تُغلَق عند الحاجة الفعلية لها،
وليست عائقاً أمام دليل المواد.

**فروق العملة عند التسوية** (سداد لاحق بسعر صرف مختلف عن سعر تسجيل
الالتزام/المستحق الأصلي) عملية محاسبية منفصلة تماماً عن قيمة العملية
نفسها — تُنشئ قيد فروقات صرف (ربح/خسارة) إضافي، ولا تُعاد كتابة القيد
الأصلي بأي حال. **مرجع مقارنة خارجي**: مبادئ IAS 21 (IFRS Foundation) حول
آثار تغيّر أسعار الصرف — [ifrs.org/issued-standards/list-of-standards/ias-21](https://www.ifrs.org/issued-standards/list-of-standards/ias-21-the-effects-of-changes-in-foreign-exchange-rates/).
**لم تُبنَ هذه الآلية بعد بالكود** — مؤجَّلة عمداً، لا تمنعها البنية الحالية.

**التوضيح الحاسم** (كي لا تصبح "الحقل وصفي فقط" ذريعة للتأجيل الدائم): هذا
القرار **موثَّق ونهائي الآن**، وليس معلَّقاً — المؤجَّل فقط هو **فرض** قيد
برمجي على عملة الحساب (إن ثبتت الحاجة له لاحقاً بحساب معيّن)، أما مبدأ "عملة
الحساب لا تساوي عملة الحركة" فهو قرار معماري ثابت من الآن.

## 21. سجل الجلسات (تكملة)

**جلسة 16 (2026-08-25)**: صديق المستخدم صحّح نفسه بمراجعة تالية — جعل عمودي
المعادل بالعملة الأساسية `read-only` بالكامل (كما نفّذنا الجلسة السابقة) **قرار
غير عملي للمحاسب**. البديل الصحيح: علاقة ديناميكية بالاتجاهين (مبلغ/سعر ⇄ معادل)،
مع بقاء منع الإصلاح التلقائي للفرق الحقيقي كما هو (لا تراجع عن ذلك القرار).

**نُفِّذ بالكامل**: `_derive_base_from_amount_and_rate()` (مبلغ أو سعر يتغيّر
→ معادل يُحسب) و`_derive_rate_from_amount_and_base()` (معادل يتغيّر يدوياً →
سعر يُستنتج، المعادل يبقى كما كُتب حرفياً). `_recalculate_totals()` صار يقرأ
القيم من الخلايا فقط (لا يكتب فوقها بعد الآن) — كان يمحي أي تعديل يدوي للمعادل
لو بقي على حاله القديم.

**اختبار end-to-end بأربع سيناريوهات متسلسلة، كلها نجحت ومؤكَّدة بلقطة شاشة
فعلية**: (1) مبلغ×سعر→معادل تلقائي، (2) تعديل المعادل يدوياً (1,520,000)→سعر
مُستنتج (15,200) والمعادل ثابت كما كُتب، (3) فرق حقيقي (20,000) يظهر بوضوح
أحمر وزر الترحيل يُعطَّل تلقائياً **بدون أي إصلاح صامت**، (4) المحاسب يعالج
الفرق يدوياً بسطر إضافي → توازن → ترحيل ناجح → ميزان مراجعة صحيح.

القاعدة الرسمية الكاملة موثّقة بالقسم 20 أعلاه — **مرجع ثابت لأي محادثة قادمة**،
لا يُغيَّر منطق التوازن أو العلاقة الديناميكية بدون مراجعة صريحة لهذا القسم أولاً.

**حالة المشروع**: سند القيد متعدد العملات مكتمل وناضج محاسبياً وتقنياً. **الخطوة
التالية**: دليل الحسابات (واجهة تعديل فعلية).

## 22. سجل الجلسات (تكملة) — 3 تعديلات على سند القيد + إصلاح تحذير commitData

**جلسة 17 (2026-08-26)**: طلب المستخدم 3 تعديلات على تجربة إدخال سند القيد،
بعدما لاحظ رسالة متكررة بالطرفية عند إغلاق البرنامج:
`QAbstractItemView::commitData called with an editor that does not belong to this view`.

**التشخيص الفعلي للتحذير** (لم يكن السبب واضحاً من ظاهر الرسالة): كان هناك
مساران مختلفان تماماً لمعالجة Enter بالشبكة:
- خلية **محدَّدة فقط** (غير قيد التحرير) → يمرّ عبر `eventFilter` المخصص
  بالنموذج (`_move_to_next_cell`)، يطبّق تخطي عمود "الحساب" وإضافة صف جديد بشكل صحيح.
- خلية **قيد التحرير الفعلي** → المفاتيح تصل للمحرِّر (`QLineEdit`/`QComboBox`)
  مباشرة، فيتولاها `QAbstractItemDelegate` الافتراضي بمساره الخاص (ترتيب tab
  قياسي لكيوت، **يتجاهل تماماً** منطقنا). حين يضيف `_on_cell_changed` صفاً
  جديداً (`insertRow`) بشكل متزامن أثناء إغلاق كيوت للمحرِّر القديم، تتزحزح
  فهرسة الصفوف تحته → التحذير.

**الإصلاح** (`app/ui/common/numeric_delegate.py`، `currency_combo_delegate.py`،
`app/ui/accounting/journal_voucher_form.py`):
- `_ReturnKeyEditingMixin` جديد: يعترض Enter/Return **بمستوى المحرِّر نفسه**
  قبل وصوله لمعالج كيوت الافتراضي، وينفّذ `commitData`→`closeEditor`→التنقل
  الموحّد (`_move_to_next_cell`) يدوياً بنفس ترتيب كيوت الداخلي، ثم يُرجع
  `True` لمنع أي معالجة مزدوجة.
- `PlainTextGridDelegate` جديد لعمودي "رمز الحساب"/"البيان" — لم يكن لهما
  delegate مخصص إطلاقاً من قبل، فكانا يتبعان مسار Enter الافتراضي المختلف.
- استدعاء `_add_empty_row()` من داخل `_on_cell_changed` أصبح مؤجَّلاً عبر
  `QTimer.singleShot(0, ...)` بدل التنفيذ المتزامن — نمط قياسي بكيوت لتفادي
  تعديل بنية الجدول (`insertRow`) أثناء ما يزال محرِّر خلية أخرى قيد الإغلاق.
- تم التحقق **بلا رسالة تحذير** عبر اختبار headless (offscreen) يحاكي كتابة
  حقيقية داخل محرِّر فعلي (`QTest.keyClicks` على `QApplication.focusWidget()`
  الفعلي، لا الشبكة مباشرة) + تعبئة آخر صف (المسار الذي يُشغّل الصف الجديد) +
  إغلاق النافذة، مع `qInstallMessageHandler` يلتقط أي رسالة كيوت خام.

**التعديل 1 — العملة الافتراضية تُطبَّق تلقائياً**: `_add_empty_row()` تملأ
عمود العملة بعملة `default_currency_combo` الحالية فور إنشاء أي صف جديد
(أول صف وكل صف لاحق)، بدل تركه فارغاً بانتظار اختيار يدوي. لو غيّر المحاسب
عملة الرأس الافتراضية لاحقاً، تُطبَّق تلقائياً على أي صف **لم يُعدَّل يدوياً**
بعد (`_on_default_currency_changed` + مجموعة تتبّع `_manual_currency_rows`).
أي تعديل صريح لعمود العملة بصف معيّن (عبر `itemChanged` الحقيقي، غير
`_set_cell_silently`) يُسجَّل بهذه المجموعة فيُجمَّد ذلك الصف عن أي تغيير
لاحق بالافتراضي بالرأس — **الافتراضي بالرأس لا يعني "عملة القيد الوحيدة"**،
تماماً كما اتُّفق بالنقاش السابق مع صديق المستخدم.

**التعديل 2 — سعر الصرف Default قابل للتعديل دوماً**: نفس مبدأ التعديل 1
يُطبَّق على عمود سعر الصرف (`_on_default_rate_changed` + `_manual_rate_rows`)
— يُملأ تلقائياً بالقيمة الافتراضية بالرأس، ويبقى قابلاً للتعديل اليدوي
الحر بأي صف. **لم تُبنَ بعد** خدمة "إعدادات أسعار الصرف حسب العملة والتاريخ"
المذكورة بالنقاش — هذا الحقل حالياً يأخذ Default من قيمة الرأس فقط، وسيُستبدل
مصدره لاحقاً بتلك الخدمة دون تغيير مبدأ "Default قابل للتعديل، غير إلزامي".

**التعديل 3 — تنقل Enter حرّ وغير مشروط بالقيمة**: كان موجوداً أصلاً جزئياً
(لا يشترط قيمة للانتقال)، لكن غير موحّد بين خلية محدَّدة وخلية قيد التحرير
(نفس سبب تحذير commitData أعلاه). الآن موحّد تماماً عبر `_ReturnKeyEditingMixin`
— مسار تنقل ثابت واحد، يتخطى فقط عمود "الحساب" (محسوب تلقائياً من الرمز،
غير قابل للتحرير)، ولا يتخطى المعادل الأساسي أو سعر الصرف رغم كونهما
محسوبَين افتراضياً (كلاهما قابل للتحرير اليدوي حسب القسم 20). التحقق يبقى
حصراً عند الحفظ/الترحيل عبر `journal_edit.py` (`add_manual_line`/
`post_manual_entry`) — لم يُمَسّ، رسائل الخطأ فيه تحدد السطر والسبب أصلاً.

**أثر جانبي يستحق الانتباه (وليس عيباً، لكن يُوثَّق صراحة)**: بما أن خلية
العملة/السعر لم تعد فارغة أبداً بأي صف، فإن كل سطر يُحفَظ الآن بعملة وسعر
صرف **صريحين** دائماً (`line_currency_code`/`line_exchange_rate` لن يكونا
`None` عملياً). نتيجة ذلك: أي مسودة محفوظة **تتجمّد** على قيمها وقت الحفظ
عند إعادة فتحها، ولن تتبع تغيير العملة الافتراضية بالرأس بعد ذلك — حتى لو
لم تُعدَّل يدوياً قط قبل الحفظ. هذا يتوافق مع مبدأ "سعر الصرف لقطة تاريخية
ثابتة" بالقسم 20، لكن **لا توجد حالياً وسيلة بالواجهة لفكّ هذا التجميد**
وإعادة صف لوضع "اتّباع الافتراضي تلقائياً" بعد إعادة فتح مسودة — إضافة
مستقبلية صغيرة منفصلة لو احتاجها المستخدم.

**اختبارات إضافية موصى بها قبل اعتبار الموضوع مغلقاً تماماً** (من نقاش سابق
مع صديق المستخدم، لم تُنفَّذ بعد كاختبار آلي دائم بالمشروع، فقط دُقِّق يدوياً
headless بهذه الجلسة): قيد USD/SYP → حفظ → إغلاق → إعادة فتح → تعديل
المعادل → إعادة حساب → حفظ → ترحيل، ثم عكس القيد المرحَّل والتحقق أن قيد
العكس يحتفظ بالقيم الأساسية وتفاصيل العملة الأصلية للقيد المعكوس.

**حالة المشروع**: التعديلات الثلاثة + إصلاح تحذير commitData منفَّذة ومختبرة
headless. **الخطوة التالية المتفق عليها سابقاً تبقى كما هي**: دليل الحسابات
(واجهة تعديل فعلية).

## 23. قواعد عملة الفواتير والمرتجعات (مرجع للتنفيذ القادم — لم يُبنَ بعد)

هذا القسم توثيق مسبق لقرار اتُّخذ بالنقاش مع صديق المستخدم **قبل** بناء
شاشات الفواتير، حتى لا يُعاد نقاش الأساسيات عند الوصول لتلك المرحلة. لا كود
منفَّذ بعد لهذا القسم.

**الفرق الجوهري عن سند القيد اليدوي**: سند القيد يدعم عملة مختلفة لكل سطر
(طبيعي أن يكون طرفا القيد بعملتين مختلفتين). **الفاتورة التجارية عكس ذلك
تماماً — عملة واحدة لكل الفاتورة**، وليس عملة لكل سطر/بند:
- رأس الفاتورة: `currency_id` + `exchange_rate` (وليس `InvoiceLine.currency_id`)
- كل بنود الفاتورة (`InvoiceLine`: الكمية، السعر، الحسم، الضريبة، الإجمالي) بنفس عملة الفاتورة
- عند الترحيل: `invoice amount → exchange rate → base amount → JournalEntry/JournalLine` بالعملة الأساسية فقط
- **السبب**: عملة لكل سطر بالفاتورة تُعقِّد بلا فائدة حقيقية — إجمالي الفاتورة، الخصم، الضريبة، كشف حساب العميل، الطباعة، كلها تحتاج عملة مرجعية واحدة للمستند

**المرتجعات**:
- `Invoice.invoice_type`: `SALE` / `PURCHASE` / `SALES_RETURN` / `PURCHASE_RETURN`
- `Invoice.original_invoice_id` (اختياري) يميّز مرتجع **مرتبط** عن مرتجع **يدوي**
  — لا حاجة لحقل `ReturnMode` منفصل
- **مرتجع مرتبط** (`original_invoice_id != NULL`): يرث العميل/المورد والعملة
  وسعر الصرف والبنود من الفاتورة الأصلية، **العملة غير قابلة للتغيير** بهذا النوع
- **مرتجع يدوي** (`original_invoice_id = NULL`): المستخدم يحدد العميل/المورد
  والعملة وسعر الصرف والبنود بنفسه
- **قاعدة محاسبية حرجة**: قيمة المرتجع التجارية تُحسب بسعر صرف **الفاتورة
  الأصلية وقت إصدارها**، وليس سعر اليوم — لا يجوز إعادة تقييم البضاعة
  المرتجعة بسعر صرف حالي مختلف لمجرد مرور الوقت (نفس مبدأ القسم 20: سعر
  الصرف لقطة تاريخية ثابتة)
- فرق العملة الناتج عن تسوية الذمم (وليس عن قيمة البضاعة نفسها) يبقى منفصلاً
  تماماً — وظيفة محاسبية مستقلة (أرباح/خسائر فروقات عملة، إعادة تقييم أرصدة
  عملات أجنبية) **مؤجَّلة عمداً**، لم تُبنَ بعد

**المخزون**: تكلفة المخزون تُسجَّل بالعملة الأساسية وقت الاقتناء فقط ولا
تُعاد حسابه عند تغيّر سعر الصرف لاحقاً — نفس مبدأ Average Cost المعتمد
مسبقاً بالمشروع، الآن مؤكَّد صراحة أيضاً لحالة الشراء بعملة أجنبية.

**اختبارات مطلوبة عند بناء هذا الجزء لاحقاً** (17 حالة اتُّفق عليها بالنقاش،
تُنقَل هنا للمرجعية): بيع/شراء بكل عملة مدعومة على حدة، قيد/مخزون صحيح
بالعملة الأساسية، مرتجع كامل وجزئي ومرتجعان متتاليان لا يتجاوزان الكمية
الأصلية (مع رفض تجاوزها)، تغيّر سعر الصرف بعد الفاتورة لا يغيّر قيمتها
التاريخية ولا تكلفة المخزون التاريخية، مرتجع مرتبط يرث عملة الفاتورة، مرتجع
يدوي يسمح باختيار عملة مستقلة، منع حذف/تعديل فاتورة مُرحَّلة، عكس فاتورة/مرتجع
يحافظ على القيم الأساسية الصحيحة.

## 24. دليل الحسابات — بطاقة الحساب المنبثقة (جلسة 18)

بُني بالضبط حسب التصميم المتفَق عليه بالنقاش مع صديق المستخدم، مع تسع النقاط
المطلوبة لـv1: شجرة، إضافة، تعديل، تفعيل/تعطيل، فتح بالنقر المزدوج/Enter،
كشف حساب، بحث، إنشاء حساب فرعي سريع (Insert)، حماية الحسابات ذات الحركات.
**لا حذف بهذا الإصدار إطلاقاً** — التعطيل هو البديل الوحيد المتاح، كما اتُّفق.

**الملفات الجديدة**:
- `app/services/account_edit.py`: كل القواعد المحاسبية/البنيوية (نفس مبدأ
  `journal_edit.py` — الواجهة لا تتحقق من شيء بنفسها). القواعد المطبَّقة:
  رمز غير مكرر، الحساب الأب موجود فعلاً، منع الدورات بالشجرة (حساب لا يصبح
  أباً لأحد أحفاده)، منع تحويل حساب له حركات مرحّلة مباشرة إلى حساب تجميعي،
  منع تغيير `account_type` لحساب له حركات (يُفسِد كشف الحساب/التقارير
  التاريخية). إعادة التسمية والحقول غير الحساسة تبقى مسموحة دوماً حتى لو
  للحساب حركات — القيد ينطبق فقط على النوع والتجميعية.
- `app/ui/accounting/account_card_dialog.py`: بطاقة الحساب — `QDialog`
  منبثق (وليس لوحة ثابتة بجانب الشجرة كما طُلب صراحة). يخدم الإنشاء
  والتعديل بنفس النافذة. طبيعة الحساب (مدين/دائن) **مُشتَقة تلقائياً** من
  نوع الحساب، غير قابلة للاختيار اليدوي (نفس الفرق المذكور بالنقاش بين
  الطبيعة الطبيعية والرصيد الفعلي — الأخير قد يخالف الأولى فعلياً، فلا
  داعٍ لحقل إدخال مستقل يخلق تعارضاً). قائمة "الحساب الأب" تستبعد الحساب
  نفسه وكل أحفاده مباشرة بالواجهة (قبل حتى وصول الطلب للخدمة) لتجربة أوضح.
  الرصيد الحالي يُعرَض فقط عند تعديل حساب موجود (عبر `get_account_balance`
  من `rollup.py` — لا حساب مباشر بالواجهة).
- `app/ui/accounting/account_statement_dialog.py`: كشف الحساب — يعيد
  استخدام `app/reports/ledger.py` (`get_account_statement`) الموجود مسبقاً
  بالمشروع بدون أي تعديل عليه. حساب تجميعي: رسالة توضيحية بدل جدول فارغ
  مضلِّل ("رصيده مجموع أرصدة حساباته الفرعية").

**التعديل على `chart_of_accounts_view.py`**:
- زر "+ حساب" بالأعلى (حساب رئيسي جديد).
- `itemActivated` (إشارة Qt قياسية واحدة تُغطّي نقر مزدوج **و** Enter معاً
  تلقائياً — لا حاجة لربط منفصل لكل مسار) تفتح بطاقة الحساب.
- مفتاح `Insert` على حساب محدَّد بالشجرة → بطاقة حساب جديد بأب مُعبَّأ
  تلقائياً (نفس أسلوب `eventFilter` المستخدم بسند القيد للتناسق).
  ↑↓←→ للتنقل والتوسيع/الطي تعمل افتراضياً من `QTreeWidget` نفسه، لم تحتج
  أي كود إضافي.
- كل عقدة تحمل الآن `Account.id` عبر `Qt.UserRole` بالعمود الأول، والحسابات
  غير النشطة تظهر برمادي (بدون أحمر/أخضر إطلاقاً — نفس توجيه الألوان المتفق
  عليه بالنقاش: "البرنامج محاسبي وليس Dashboard").

**لم يُبنَ عمداً بهذه الجلسة** (نفس تحذير over-engineering المذكور بالنقاش):
تبويبات بطاقة الحساب المتعددة (بيانات/رصيد/كشف/إعدادات)، حذف الحساب،
اختيار عملة مقيَّدة فعلياً بحركات القيد (الحقل وصفي فقط حالياً — راجع تعليق
`models.py` عند `Account.currency_code`).

**اختبار headless (offscreen)**: 14 حالة — إنشاء شجرة 3 مستويات، رفض كود
مكرر، رفض دورة بالشجرة، رفض أب غير موجود، حظر تغيير النوع/التجميعية لحساب
له حركات، سماح بإعادة تسمية حساب له حركات، تفعيل/تعطيل غير مقيَّد، بناء
الشاشة الرئيسية وبطاقة الحساب (تعديل وإنشاء) وكشف الحساب (عادي وتجميعي)
ومسار Insert — كلها ناجحة، ولا رجوع بأي اختبار سابق لسند القيد.

**حالة المشروع**: دليل الحسابات (v1 الكامل حسب القائمة المتفَق عليها) مكتمل
ومختبر. **الخطوة التالية**: لم تُحدَّد بعد بالنقاش — تحتاج قراراً من المستخدم.

## 26. دليل المواد — v1 (جلسة 19)

بُني بعد تصميم مُراجَع من صديق المستخدم (5 تعديلات على المسودة الأولى، كلها
مطبَّقة). **حسمت القراءة الفعلية للكود** قبل التصميم أن `Item`/`Warehouse`/
`InventoryMovement` وحساب المتوسط المرجّح موجودون مسبقاً بالمشروع — الناقص
كان الواجهة فقط.

**اكتشاف مهم أثناء البناء، مُوثَّق بتعليقات `item_edit.py` مباشرة**:
`Item.sales_account_id` موجود بالـschema لكنه **غير مستخدَم إطلاقاً** بمحرّك
الترحيل الفعلي (`posting.py`) — المبيعات كلها تمر عبر إعداد عام واحد
(`Setting['default_sales_account_id']`) بغض النظر عن حساب المبيعات
المُختار بكل مادة. لذلك اختياره بالواجهة **وصفي فقط حالياً بلا أثر فعلي
بالقيود** — تركته اختيارياً غير إلزامي (بعكس حساب المخزون والتكلفة،
الإلزاميين فعلياً لأنهما يُستخدَمان مباشرة). هذه فجوة تستحق قراراً لاحقاً:
إما ربط الحقل فعلياً بمحرّك الترحيل، أو حذفه من بطاقة المادة كي لا يوهم
المستخدم بتأثير لا يحدث.

**الملفات الجديدة**:
- `app/services/item_queries.py`: **المصدر الوحيد** لحساب الكمية/متوسط
  التكلفة/قيمة المخزون (`get_item_stock_summary`) — مشتقة حصراً من
  `InventoryMovement` المرحّلة، **لا حقل مخزَّن واحد على `Item`** (ثبّتها
  صديق المستخدم كقاعدة صريحة: "Item master data لا يخزن رصيداً محاسبياً أو
  كمية أو متوسط تكلفة كحقائق مستقلة؛ هذه القيم تُشتق من الحركات المرحّلة").
  `app/services/posting.py` أعيد ربطه ليستدعي هذه الدالة نفسها بدل نسخة
  خاصة به من نفس الخوارزمية — مصدر واحد فقط، تحقَّق بالاختبار T9b أن كلا
  المسارين يُعطيان نفس الرقم بالضبط.
- `app/services/item_edit.py`: قواعد الحماية (نفس مبدأ `account_edit.py`) —
  SKU غير مكرر، اسم مطلوب، `reorder_point ≥ 0`، **FIFO مرفوضة صراحة** (موجودة
  بالـenum لكن غير مُنفَّذة بأي منطق فعلي)، حساب المخزون وحساب تكلفة المبيعات
  **إلزاميان** (يُستخدَمان مباشرة بالترحيل)، حساب المبيعات اختياري (راجع
  الفجوة أعلاه)، كل حساب مُختار يجب أن يكون موجوداً وحساب حركة (`is_group=False`)
  و**نشطاً** و**من النوع الصحيح** (مخزون→أصول، مبيعات→إيرادات، تكلفة
  مبيعات→مصروفات). مادة لها حركات: **قفل `cost_method` وكل الحسابات الثلاثة**
  عن التعديل — موثَّق صراحة بالكود أن هذا **قيد v1 مؤقت**، والخطة المستقبلية
  (تغيير حسابات "اعتباراً من تاريخ" مع بقاء الحركات القديمة على حساباتها
  التاريخية) لا يمنعها شيء بالـschema الحالي، فقط لم تُبنَ الآن. إعادة
  التسمية/التصنيف/الوحدة/حد إعادة الطلب تبقى قابلة للتعديل دوماً.
- `app/ui/inventory/` (حزمة جديدة): `item_card_dialog.py` (بطاقة المادة —
  4 أقسام بلا تبويبات: معلومات أساسية / المخزون والتكلفة (عرض فقط، مشتق
  حياً) / الحسابات المحاسبية (قوائم مفلترة بالنوع من دليل الحسابات، ليست
  نصاً حراً) / إعدادات المخزون؛ FIFO مُعطَّل بالـComboBox بصرياً أيضاً لا فقط
  بالخدمة؛ حقول الحسابات تُقفَل تلقائياً بالواجهة إن كان للمادة حركات) و
  `item_list_view.py` (جدول لا شجرة، بحث فوري + فلتر تصنيف، `itemActivated`
  لفتح البطاقة، مادة غير نشطة تظهر رمادية لكنها لا تُخفى من القائمة الكاملة).

**قرارات مؤقتة مقصودة موثَّقة صراحة بالكود** (نفس أسلوب "مقبول مؤقتاً" الذي
استخدمه صديق المستخدم): `unit` و`category` نصّان حرّان الآن — `category`
سيُستبدل لاحقاً بـ`ItemCategory` (Combobox من قيم موجودة + إمكانية إضافة
جديد) لتفادي "لحوم" مقابل "اللحوم" مقابل "لحوم " بالتقارير، لكن التأجيل
مقصود وموثَّق لا إهمال.

**لا عملة للمادة إطلاقاً** — تأكَّد هذا فعلياً بقراءة الـschema (لا عمود
عملة على `Item` ولا `InventoryMovement`)، متوافق تماماً مع القرار المثبَّت
بالقسم 20.1: عملة الحركة تُحدَّد بمستوى الفاتورة/سطر القيد، لا بمستوى
المادة أو الحساب.

**التسجيل بالقائمة الرئيسية**: `app/ui/main_window.py` — "دليل المواد" كان
عنصر قائمة بديلاً (`_open_not_implemented`)، أصبح يفتح `ItemListView` فعلياً.

**اختبار headless (offscreen)**: 15 حالة — رفض حساب مخزون مفقود/من نوع
خاطئ/تجميعي/غير نشط، رفض FIFO، إنشاء صحيح بحساب مبيعات غير مُحدَّد، رفض
SKU مكرر، ملخص مخزون صفري قبل أي حركة، **دقة المتوسط المرجّح مطابقة تماماً
بين `item_queries` و`posting.py`**، قفل الحقول الحسّاسة لمادة لها حركات
(بسبب محدَّد صراحة لا نوع حساب فقط)، سماح بإعادة التسمية رغم وجود حركات،
تعطيل حر لا يخفي التاريخ، وبناء كل نوافذ الواجهة — كلها ناجحة، وأعدت تشغيل
اختباري سند القيد ودليل الحسابات السابقين للتأكد من عدم وجود أي رجوع (كلاهما
لا يزالان ناجحين بالكامل).

**حالة المشروع**: دليل المواد v1 مكتمل ومختبر ومُسجَّل بالقائمة الرئيسية.
**الخطوة المتفَق عليها بعده مباشرة**: **لا** شاشة جديدة — بناء بيانات اختبار
واقعية شاملة End-to-End (شجرة حسابات، عملاء/موردون، مواد، مستودعات، أرصدة
افتتاحية، عدة عملات، مشتريات/مبيعات/مرتجعات/قيود يدوية/فروقات عملة) ثم فحص
السلسلة كاملة: قيود → دفتر الأستاذ → ميزان المراجعة → المخزون → تكلفة
المبيعات → قائمة الدخل → الميزانية — بدل الاستمرار ببناء شاشات إضافية.

## 27. حسم فجوة sales_account_id + إصلاح خطأ فعلي بمحرّك الترحيل (جلسة 20)

صديق المستخدم رفض ترك `sales_account_id` "وصفياً فقط" — إما تفعيله بالمحرّك
أو إخفاؤه من الواجهة، لا وجود إعداد يوهم المستخدم بأثر لا يحدث. القرار:
**تفعيله الآن**، لأنه أصلاً كان جزءاً من تصميم بطاقة المادة المتَّفق عليه.

**التغيير بـ`posting.py::post_sales_invoice` و`returns.py::post_sales_return`**:
حساب مبيعات كل سطر فاتورة أصبح `item.sales_account_id or default_sales_acc`
(الإعداد العام الآن **احتياطي فقط** لمادة لم تُحدَّد لها حساب مبيعات خاص، لا
مصدراً وحيداً كما كان). التوثيق أعلى `posting.py` وتعليقات `item_edit.py`/
`item_card_dialog.py` عُدِّلت لتعكس هذا — لا تُترَك جملة "وصفي فقط" قائمة
بمكان بينما الكود صار يستخدمه فعلياً، تفادياً لنفس نوع الفجوة التي رصدها
صديق المستخدم للتو.

**خطأ فعلي انكشف أثناء التنفيذ، لم يكن جزءاً من طلب صديق المستخدم لكنه من
نفس العائلة تماماً وأصلحته بنفس الجلسة**: كل من `post_sales_invoice`
و`post_sales_return` كانا يأخذان حسابات المخزون/تكلفة المبيعات من **أول
سطر بالفاتورة فقط** (`item0 = session.get(Item, invoice.lines[0].item_id)`)
ثم يطبّقانها على **إجمالي كل سطور الفاتورة** — أي فاتورة بيع بمادتين لهما
حسابا مخزون/تكلفة مختلفان كانت تُرحَّل بالكامل على حسابات المادة الأولى فقط،
بصمت، بلا أي خطأ ظاهر. **الشراء ومرتجع الشراء لم يكن بهما هذا الخطأ إطلاقاً**
— كانا مبنيين أصلاً بتجميع صحيح لكل حساب على حدة (`inventory_debits: dict`)؛
لم يُطبَّق نفس النمط على البيع والمرتجع وقت بنائهما الأول فقط. الإصلاح:
تجميع مبيعات/تكلفة/مخزون كل سطر بحساب موقعه الفعلي (`sales_credits`/
`cogs_debits`/`inventory_credits` — نفس نمط `inventory_debits` بالشراء
بالضبط)، فتُنتج فاتورة متعددة المواد بحسابات مختلفة سطور قيد منفصلة لكل
حساب مختلف فعلياً.

**لماذا لم يظهر هذا الخطأ بالاختبارات السابقة**: كل الاختبارات حتى الآن
استخدمت مادة واحدة فقط لكل فاتورة تجريبية — بالضبط نوع الفجوة التي حذَّر
منها صديق المستخدم مسبقاً ("اختبار End-to-End حقيقي، لا مجرد نجاح كل شاشة
منفردة"). **اختبار جديد مخصَّص** (`per_item_account_test.py`) يبني فاتورة
بيع ومرتجعاً بمادتين بحسابات مبيعات/مخزون/تكلفة مختلفة تماماً (إحداهما بلا
`sales_account_id` لتغطية الاحتياطي)، ويتحقق سطراً بسطر أن كل حساب أخذ
مبلغه الصحيح بالضبط — نجح بالكامل بعد الإصلاح.

**حالة المشروع**: فجوة `sales_account_id` مُغلَقة، وخطأ التجميع بحسابات
البيع/المرتجع مُصلَح ومختبَر. أعيد تشغيل كل اختبارات الجلسات السابقة
(سند القيد، دليل الحسابات، دليل المواد) — لا رجوع بأي منها. **الخطوة
التالية تبقى كما اتُّفق**: بيانات اختبار End-to-End شاملة، لا شاشة جديدة —
وهذا الاكتشاف بالذات يؤكد أهمية ذلك الاختبار قبل أي توسّع إضافي.

## 28. إصلاحان قبل الانتقال لبيانات الاختبار الشاملة (جلسة 21)

### 28.1 خطأ فعلي حرج: طريقة التكلفة الافتراضية بطاقة المادة — كانت تمنع الحفظ فعلياً

**السبب الجذري**: `CostMethod` بـ`models.py` مُعرَّف بترتيب `FIFO` ثم
`AVERAGE`. كود `item_card_dialog.py` افترض العكس — استخدم **فهرساً ثابتاً**
(`setCurrentIndex(0)` ظناً أنه Average، `model().item(1).setEnabled(False)`
ظناً أنه تعطيل FIFO). النتيجة الفعلية: **FIFO كانت المختارة افتراضياً،
وAverage كانت المُعطَّلة وغير قابلة للاختيار إطلاقاً بالـComboBox** —
عكس المقصود تماماً بالكودين. أي محاولة حفظ مادة جديدة بلا تغيير يدوي تُرسِل
`cost_method=FIFO`، فترفضها `item_edit.py` (رفضها صحيح ومقصود)، لكن **لا
يوجد خيار آخر بالـComboBox يمكن للمحاسب اختياره بدلاً منها** — عطل كامل
لإنشاء أي مادة جديدة، وليس تفصيلاً تجميلياً كما وصفه المستخدم بحق.

**الإصلاح**: استبدال الفهرسة الثابتة بـ`combo.findData(CostMethod.AVERAGE)`
و`findData(item.cost_method)` — يعمل بشكل صحيح بغض النظر عن ترتيب تعريف
القيم بالـenum مستقبلاً، فلا يتكرر هذا الصنف من الخطأ لو تغيّر ترتيب
`CostMethod` لاحقاً. **درس عام يستحق تعميمه على أي كود مشابه لاحقاً**: أي
اختيار بـComboBox مبني على enum يجب أن يُقرأ ويُكتَب **بالبيانات
(`currentData`/`findData`)**، لا بفهارس رقمية ثابتة مطلقاً — الفهرس الثابت
افتراض هشّ ينكسر بصمت عند أي تعديل مستقبلي بترتيب الـenum، تماماً كما حدث هنا.

### 28.2 تحقيق تحذير commitData بدليل المواد — لم يتكرر، والسبب معماري وليس تساهلاً

طُلب مني عدم الاكتفاء بافتراض عدم الأهمية، فحقَّقت فعلياً بدل الافتراض:

- **دليل المواد لا يملك أي جدول قابل للتحرير خلوياً إطلاقاً** — تحقَّقت من
  هذا ببرمجية (`table.editTriggers() == QAbstractItemView.NoEditTriggers`)
  وبتشغيل فعلي: `item_list_view.py` جدول عرض فقط (`NoEditTriggers`)، وبطاقة
  المادة (`item_card_dialog.py`) حقول `QDialog` عادية (`QLineEdit`/
  `QComboBox`/`QDoubleSpinBox`) **وليست** محرِّرات `QAbstractItemView` عبر
  delegate. تحذير `commitData ... does not belong to this view` تحديداً
  صادر من آلية تتبّع المحرِّر↔الفهرس الداخلية بـ`QAbstractItemView` نفسها —
  آلية لا توجد أصلاً بلا محرِّرات delegate مفتوحة. **مستحيل بنيوياً** أن
  يصدر هذا التحذير تحديداً من دليل المواد كما بُني، لا افتراضاً بل حقيقة
  بالكود الفعلي.
- اختبرت أيضاً **السيناريو الوحيد غير المُختبَر سابقاً** بسند القيد (المكان
  الوحيد بالتطبيق الذي يملك محرِّرات delegate فعلية): إغلاق النافذة **أثناء
  محرِّر خلية مفتوح فعلياً لم يُغلَق بعد** (كتابة نص بالخلية بلا ضغط Enter
  ولا نقر خارجها، ثم `.close()` مباشرة) — لم يتكرر التحذير هنا أيضاً، الإصلاح
  السابق (§22) يغطي هذا المسار ضمناً لأن Qt يستدعي `closeEditor` تلقائياً
  عند تدمير الـwidget بغض النظر عن كيفية إنهاء التحرير.
- **الخلاصة الصادقة**: التحذير المُبلَّغ عنه على الأرجح كان أثراً من نافذة
  سند القيد **متروكة مفتوحة بالخلفية** بجلسة اختبار يدوية سابقة (النوافذ
  بهذا التطبيق مستقلة وغير مودالية عبر `_open_window`)، ظهر عند إغلاق
  التطبيق كاملاً بينما كان المستخدم يختبر دليل المواد بالمقدمة — لا خطأ
  إضافي انكشف بدليل المواد نفسه، لكن التحقيق أُجري بالكامل كما طُلب، بنتيجة
  سلبية موثَّقة لا افتراض.

**الاختبارات المطلوبة كلها نُفِّذَت وأُعيد تشغيلها بهذه الجلسة**: إنشاء
مادة بـAverage → حفظ → إعادة فتح → تأكيد بقاء Average (وليس فقط عدم الرفض)،
تعديل مادة، تنقل Enter أثناء تحرير خلايا سند القيد، فتح→إغلاق→إعادة فتح
نافذة دليل المواد، **إضافةً** لكل اختبارات سند القيد ودليل الحسابات ودليل
المواد وفواتير متعددة المواد السابقة — 6 مجموعات اختبار كاملة، كلها ناجحة
بلا رجوع واحد.

**حالة المشروع**: الإصلاحان مكتملان ومختبَران بدقة. **الخطوة التالية**:
بيانات اختبار End-to-End شاملة — لا عوائق متبقية تمنع البدء بها الآن.

## 29. اختبار End-to-End شامل — 11 مرحلة، خطأ محاسبي حقيقي انكشف (جلسة 22)

نُفِّذ السيناريو بالضبط كما طُلب: دليل الحسابات → دليل المواد → أرصدة
افتتاحية → شراء SYP → شراء USD → بيع متعدد المواد (3 مواد بحسابات مختلفة
تماماً بفاتورة واحدة) → مرتجع بيع جزئي مرتبط → مرتجع شراء جزئي مرتبط →
قبض/دفع → سند قيد USD/EUR + USD/SYP + فرق صرف حقيقي بحساب مستقل → Trial
Balance/Income Statement/Balance Sheet. **كل مرحلة تحقَّقت بمبلغها وحسابها
الدقيقين، لا نجاح شامل غامض** — وميزان المراجعة تحقَّق حساباً حساباً (الصندوق،
العميل، المورد، فروقات العملة، المبيعات، البنك) لا فقط أن المدين=الدائن.

### 29.1 خطأ محاسبي حقيقي انكشف — بالضبط كما توقَّع صديق المستخدم

**السبب**: `post_purchase_invoice` كان يخزّن `InventoryMovement.unit_cost`
(المُستخدَم مباشرة بحساب المتوسط المرجّح) من `net_after_all_discounts`
مباشرة — وهذه القيمة بعملة **الفاتورة الأصلية** (USD مثلاً)، **بلا أي
ضرب بـ`invoice.exchange_rate`** لتحويلها للعملة الأساسية. بينما القيد
المحاسبي نفسه (`JournalLine` عبر `_jline`) كان يُحوَّل بشكل صحيح تماماً
دائماً — الخلل محصور حصراً بحركة المخزون، لا بالقيد.

**الأثر العملي**: مادة تُشترى أحياناً بالعملة الأساسية وأحياناً بعملة أجنبية
(بالضبط سيناريو مادة "ب" بهذا الاختبار: شراء SYP بسعر 2000، ثم شراء USD
بسعر 10) كان متوسط تكلفتها المرجّح **يخلط دولاراً خاماً مع ليرة سورية
مباشرة بلا تحويل** — رقم بلا أي معنى محاسبي، ينعكس مباشرة على COGS وقيمة
المخزون بكل تقرير لاحق. **لم يظهر هذا الخطأ بأي اختبار سابق لأن كل اختبار
حتى الآن اشترى مادة واحدة بعملة واحدة ثابتة فقط** — بالضبط نوع الفجوة
التي حذَّر منها صديق المستخدم صراحة قبل بدء هذا الاختبار، ونفس عائلة خطأ
§27 (تجميع حسابات فاتورة متعددة المواد) لكن بموقع مختلف تماماً بالكود.

**كيف انكشف تحديداً**: الاختبار حسب قيمتين متوقَّعتين صراحة قبل التنفيذ —
"لو صحيح: كذا" و"لو فيه الخطأ المشتبه به: كذا" — وقارن النتيجة الفعلية
بالاثنتين معاً، لا بقيمة واحدة "متوقَّعة" فقط. الفعلي طابق تماماً سيناريو
الخطأ (`1431.4286`) لا السيناريو الصحيح (`44285.7143`)، فأكَّد الشك بثقة
تامة قبل أي تعديل بالكود.

**الإصلاح**: تحويل `net_after_all_discounts` للعملة الأساسية بضربه
بـ`invoice.exchange_rate` **قبل** القسمة على الكمية لحساب `unit_cost`،
تماماً بنفس منطق `_jline` المستخدَم للقيد. `_return_unit_cost` (بمرتجع
البيع/الشراء) لم يحتج أي تعديل — يقرأ القيمة المخزَّنة مباشرة من الحركة
الأصلية، فيصحّ تلقائياً بمجرد إصلاح نقطة التخزين الوحيدة.

**درس عام**: أي مكان بالكود يخزّن قيمة مالية مشتقة من فاتورة (وليس القيد
المحاسبي مباشرة) يجب مراجعته صراحة للتأكد من مرورها بـ`invoice.exchange_rate`
قبل التخزين — القيد المحاسبي وحده ليس كافياً كضامن، لأن حركة المخزون طريق
منفصل تماماً بالكود.

### 29.2 نقاط أخرى تحقَّقت بدقة أثناء السيناريو (بلا مفاجآت، لكن توثيقها يستحق)

- مرتجع بيع/شراء **جزئي** مرتبط بفاتورة أصلية: يقرأ كلفة الوحدة من حركة
  الشراء/البيع الأصلية بالضبط (لا متوسط اليوم) — تحقَّق رقمياً بدقة.
- فاتورة بيع بثلاث مواد بحسابات مبيعات/مخزون/تكلفة مختلفة تماماً (إحداها
  بلا حساب مبيعات خاص فاستخدمت الاحتياطي العام): كل مبلغ ذهب لحسابه الصحيح
  بالضبط — تأكيد عملي لإصلاح §27 بسيناريو واقعي حقيقي لا اختبار معزول.
  رصيد العميل الفرعي المُنشأ تلقائياً طابق إجمالي الفاتورة تماماً.
  رصيد المورد الأجنبي بعد شراء وشراء مرتجع جزئي وسداد جزئي — دقيق لليرة.
- سند قيد يدوي USD مقابل EUR متوازن تماماً بالعملة الأساسية رغم عدم وجود
  أي عملة وسيطة مباشرة بينهما بالسطرين — يتحقق فقط لأن كل سطر يحسب معادله
  الأساسي المستقل تماماً (تصميم §20)، لا لأن هناك تحويلاً ضمنياً USD→EUR.
- فرق صرف حقيقي: التزام مُسجَّل بسعر، مُسوَّى بالكامل بسعر مختلف فعلياً،
  بقيد واحد ذري (3 أسطر: إطفاء الالتزام بسعره الأصلي + فرق الصرف + السداد
  الفعلي) — الالتزام انطفأ لرصيد صفر بالضبط، وفرق الصرف ظهر بحسابه المستقل
  بالضبط. **ملاحظة منهجية من أثناء البناء**: محاولة أولى ربطت الفرق بقيد
  تصحيحي منفصل بعد قيد الدفع (بدل قيد ذري واحد) وأنتجت رقماً خاطئاً بسبب
  ازدواج المعالجة — أُعيد التصميم لقيد ذري واحد قياسي (المعالجة المحاسبية
  الصحيحة أصلاً لفرق الصرف عند التسوية)، وهذا خطأ بتصميم سيناريو الاختبار
  نفسه لا بكود التطبيق.
- قائمة الدخل والميزانية العمومية: تحققتا رقمياً بالكامل مقابل حسابات
  مستقلة يدوية (لا مجرد استدعاء الدالة وتصديق ناتجها) — الميزانية توازنت
  تماماً شاملة "صافي الربح غير المُقفل" (راجع تصميمه بـ§ الخاص بـbalance_sheet.py).

**حالة المشروع**: 12 قيداً مرحّلاً و5 فواتير مرحّلة بسيناريو واحد متكامل،
كل رقم تحقَّق يدوياً بمعادلة مستقلة قبل مقارنته بناتج النظام. خطأ محاسبي
حقيقي واحد انكشف وأُصلِح ومُختبَر. أُعيد تشغيل كل الاختبارات السابقة
(سند القيد، دليل الحسابات، دليل المواد، الترحيل متعدد المواد، تحقيق
Qt) — لا رجوع بأي منها. **الخطوة التالية**: تحتاج قراراً من المستخدم
وصديقه — لا قرار متَّفق عليه مسبقاً لما بعد هذه المرحلة.

## 30. طبقة اختبارات محاسبية مركزة (15 حالة) — وخطأ حرج جداً انكشف بالحالة الرابعة (جلسة 23)

صديق المستخدم رفض اعتبار نجاح الـE2E دليلاً على "محرك المحاسبة صحيح بكل
الحالات" — طلب صراحة 15 حالة حافة (edge cases) مركَّزة قبل أي شاشة جديدة،
وقاعدة معمارية عامة: **كل قيمة مالية بالنظام يجب أن يُعرَف بوضوح هل هي
بالعملة الأصلية أم الأساسية، لا يجوز تركها ضمنياً** — من `InvoiceLine` حتى
`Balance Sheet`. الملف الجديد `tests/test_accounting_edge_cases.py` يغطي
الـ15 حالة كاملة، كل حالة بقاعدة بيانات معزولة خاصة بها.

### 30.1 الخطأ المُكتشَف — الأخطر حتى الآن، ولم يكن ليظهر بفحص "هل القيد متوازن؟"

**أثناء بناء الحالة الرابعة تحديداً** (مرتجع شراء بعد تغيّر سعر الصرف، بتعمّد
اختلاف سعر صرف المرتجع نفسه عن سعر الشراء الأصلي) انكشف أن `post_purchase_return`
كان يحسب قيمة البضاعة المُعادة بالقيد المحاسبي (`inventory_credits`/`cash_or_ap`)
من **سعر وسطر المرتجع نفسه** (`line_total.net_after_all_discounts` × سعر
صرف المرتجع)، بينما `InventoryMovement.unit_cost` (حركة المخزون الفعلية)
كانت تُحسَب بشكل صحيح تماماً من `_return_unit_cost` (الكلفة التاريخية من
حركة الشراء الأصلية) — **مصدران مختلفان تماماً لنفس الرقم بنفس القيد
الواحد**، دفتر الأستاذ يقول رقماً وحركة المخزون تقول رقماً آخر لنفس
البضاعة بنفس اللحظة.

**وأثناء إصلاح هذه بالذات انكشف خطأ أخطر بكثير، بنفس العائلة لكن بموقع
مختلف وأثر أفدح**: `_jline()` (الدالة المشتركة لإنشاء أي سطر قيد) تفترض
دائماً أن `debit`/`credit` الممرَّرين لها **بعملة المستند الأصلية**، وتضربهما
بـ`exchange_rate` لحساب `debit_base`/`credit_base`. لكن `post_sales_invoice`
و`post_sales_return` (منذ إصلاح §29) كانا يمرّران لهذه الدالة نفسها مبالغ
COGS/المخزون المُشتقّة من `_average_cost`/`_return_unit_cost` — وهذه القيم
**محوَّلة للعملة الأساسية مسبقاً بالفعل** (لأن `InventoryMovement.unit_cost`
يُخزَّن بالعملة الأساسية دائماً منذ إصلاح §29) — فتتحوَّل **مرتين**: مرة
ضمنية عند الحساب، ومرة إضافية خاطئة داخل `_jline`. **مثال فعلي مقيس**: مبلغ
صحيح قدره 1,200,000 ل.س تحوَّل خطأً إلى 21,600,000,000 ل.س بفاتورة بعملة
USD (سعر صرف 18000) — مضاعفة كارثية، لا فرق تقريب بسيط.

**السبب الحقيقي لعدم اكتشاف هذا رغم كل الاختبارات السابقة (بما فيها الـE2E
الشامل بـ11 مرحلة)**: **كل قيد `is_balanced()` كان لا يزال يتحقق دون خطأ
رغم هذا الخلل** — لأن نفس الخطأ (الضرب المزدوج بنفس سعر الصرف) طُبِّق
بالتساوي على طرفي كل زوج (مدين COGS ودائن المخزون، أو مدين المخزون ودائن
المورد)، فبقي الطرفان متساويين لبعضهما رغم كونهما **كلاهما خاطئين بنفس
المقدار**. **التوازن الإجمالي لا يكفي إطلاقاً للتحقق من الصحة المحاسبية —
فقط التحقق من القيمة المطلقة المتوقَّعة لكل حساب يكشف هذا النوع تحديداً من
الأخطاء**، بالضبط المبدأ الذي أصرّ عليه صديق المستخدم منذ اختبار Trial
Balance الأول (§29)، وهنا ثبتت أهميته الحاسمة عملياً وبالدليل الرقمي، لا
نظرياً فقط. **كل اختبار سابق بعملة أجنبية استخدم SYP لفاتورة البيع نفسها
(سعر صرف 1)** — الضرب بـ1 لا يُظهر أي فرق، فبقي الخطأ مخفياً تماماً حتى
اختبار فاتورة بيع بعملة أجنبية فعلياً (لم يحدث قبل هذه الجلسة بأي اختبار).

**الإصلاح** (`app/services/posting.py`, `app/services/returns.py`):
- دالة جديدة `_jline_base(account_id, debit_base, credit_base)` — لسطر
  قيمته محوَّلة للعملة الأساسية مسبقاً بالفعل (`debit == debit_base` دائماً،
  لا "عملة أصلية" منفصلة ذات معنى لقيمة التكلفة نفسها أصلاً — تكلفة المادة
  مستقلة تماماً عن عملة عملية البيع/الشراء الحالية). تُستخدَم الآن حصراً
  لكل سطر COGS/مخزون بـ`post_sales_invoice`، `post_sales_return`،
  `post_purchase_return`.
- `post_purchase_return`: أُعيد بناؤها لتحسب قيمة البضاعة المُعادة من
  `_return_unit_cost` حصراً (نفس مصدر `InventoryMovement.unit_cost` بالضبط)
  — لا من سطر المرتجع. سطر `cash_or_ap` **قُسِّم لسطرين منفصلين** عند وجود
  ضريبة: جزء التكلفة (`_jline_base`، بلا تحويل إضافي) وجزء الضريبة (`_jline`
  عادية، تحتاج تحويلاً فعلياً بسعر صرف المرتجع) — دمجهما بسطر واحد بمعدّل
  تحويل واحد كان سيُصحِّح أحدهما ويُفسِد الآخر حتماً.
- `_jline` الأصلية بقيت كما هي تماماً (لا تغيير بسلوكها) — تُستخدَم فقط
  الآن للمبالغ الحقيقية بعملة المستند (المبيعات، الضريبة، الصندوق/الذمم).

**تحديث لاختبار §29 نفسه**: اكتُشِف أن اختبار §29 كان يتحقق من الحقل الخاطئ
(`.credit` الخام بدل `.credit_base`) بحالة مرتجع الشراء — الحقل الخام صادف
أن يحمل القيمة الصحيحة (لأن `_return_unit_cost` أصلاً بالعملة الأساسية)،
فمرّ الاختبار رغم أن `.credit_base` (الحقل الفعلي المُستخدَم بكل تقرير) كان
كارثياً خاطئاً. **دُقِّقَت كل الاختبارات (§29 والحالات الـ15 الجديدة) لتتحقق
من `debit_base`/`credit_base` صراحة، لا `debit`/`credit` الخام أبداً عند
فحص القيمة المحاسبية الفعلية.**

### 30.2 الحالات الـ15 — نتيجة كل واحدة

كلها ناجحة بعد الإصلاح (22 تحقّقاً فرعياً بالمجموع):
1. شراء أجنبي ثم محلي لنفس المادة — متوسط صحيح بأي ترتيب.
2. بيع بعد تغيّر المتوسط — COGS يستخدم متوسط لحظة البيع بالضبط.
3. مرتجع بيع بعد تغيّر المتوسط لاحقاً — يقرأ كلفة البيع الأصلية، لا المتوسط
   الحالي الأعلى (فرق كبير تعمَّدت تكبيره بالاختبار ليكون الفشل واضحاً لو حدث).
4/5. مرتجع شراء بعد تغيّر سعر الصرف — **الخطأ المكتشَف أعلاه**، مُصلَح ومختبَر.
6/7. تسوية عميل/مورد أجنبي بسعر مختلف — خسارة وربح صرف محقَّقان، الالتزام
   يُطفأ لصفر بالضبط بكلتا الحالتين.
8. فرق تقريب 0.01 — يُرفَض بوضوح عند الترحيل (لا تسامح صامت بأي مبلغ).
9/10. حساب نقدي واحد بـ3 عملات معاً (SYP+USD+EUR) — الرصيد الإجمالي بالعملة
   الأساسية فقط، بلا جمع عملات خام مباشرة (نفس مبدأ §20.1).
11. منع تعديل مستند POSTED — فاتورة وقيد يدوي كلاهما محميان فعلياً.
12. عكس قيد مرحّل — لا تأثير مزدوج بالتقارير، صافي الحساب يعود تماماً كما
   كان قبل القيد الأصلي.
13. مسودة لا تظهر بأي تقرير مالي — تأكيد مباشر بميزان المراجعة.
14. إعادة فتح مسودة — القيمة المحفوظة لا تتغيّر من تلقاء نفسها (لا يوجد بعد
   جدول إعدادات أسعار صرف مركزي يُختبَر تأثيره؛ فجوة معروفة من §21).
15. تكلفة مخزون بالعملة الأساسية دائماً عبر 3 عمليات شراء بـ3 عملات مختلفة
   تماماً لنفس المادة بنفس القيد التسلسلي.

**حالة المشروع**: خطأ حرج جداً (§30.1) انكشف وأُصلِح ومُختبَر بدقة شديدة —
على الأرجح أخطر خطأ اكتُشِف بكل جلسات المشروع حتى الآن، لأنه كان قادراً
على المرور من كل فحوصات التوازن التلقائية. أُعيد تشغيل كل الاختبارات
السابقة (8 مجموعات كاملة: سند القيد، دليل الحسابات، دليل المواد، الترحيل
متعدد المواد، تحقيق Qt، الـE2E الشامل، الحالات الـ15) — لا رجوع بأي منها.
**الخطوة التالية**: تحتاج قراراً من المستخدم وصديقه.

---

## §31 — خطة المرحلة 1 المتفَق عليها بعد §30 (قبل أي شاشة جديدة)

بعد اكتشاف خطأ §30 (double conversion)، اتُّفِق على عدم الانتقال لأي واجهة
جديدة قبل تثبيت محرك المحاسبة وفق ترتيب صارم:

**إصلاح المحرك → money_types/guards → Fuzz+Oracle مستقل → Regression
كامل (منفصل عن الـfuzz) → `fuzz_report.gate()==True` → Baseline رسمي
→ Alembic → اختبار migration على بيانات حقيقية → حالات عدوانية →
دورة المخزون → فواتير UI → Regression كامل مرة أخيرة.**

### 31.1 حارس الاتساق (لا الحجم) — `app/services/sanity_guard.py`

قرار مُصحَّح أثناء المراجعة: حارس مبني على "المبلغ كبير جداً" مرفوض
عمداً — سيرفض معاملات مشروعة (100,000 USD بسعر صرف مرتفع مثلاً). البديل
المعتمد: حارس **اتساق** يتحقق فقط أن `debit_base`/`credit_base` تطابق
`raw × exchange_rate`، بصرف النظر عن حجم الرقم. مُدمَج فعلياً داخل
`_jline()` بـ`app/services/posting.py` (وليس مجرد ملف منفصل غير مُستخدَم).

### 31.2 `app/services/money_types.py` — طبقة اختيارية، غير مفروضة الآن

كائنات `TransactionAmount`/`BaseCurrencyAmount` جاهزة للاعتماد التدريجي
عند لمس أي دالة ترحيل مستقبلاً لسبب آخر — **لم تُستبدل بها استدعاءات
`_jline`/`_jline_base` الحالية (~15 موضعاً)** لأن هذا حجم refactor غير
مبرَّر الآن بعد أن أثبت sanity_guard.py فعاليته بتكلفة أقل بكثير.

### 31.3 عتبة قبول fuzz — مُشتقة من السياسة الموثَّقة، لا رقماً اعتباطياً

`tests/fuzz_report.py`: `MAX_ROUNDING_DIFF_BASE_CURRENCY = MONEY_QUANT × 2`
(أي 0.02) — مأخوذة مباشرة من `app/services/money.py` (سياسة التقريب
الرسمية: ROUND_HALF_UP على منزلتين)، لا رقماً افتراضياً اختُرع للاختبار.
`AccountingSanityError` مسموح به = صفر، بلا استثناء.

### 31.4 `tests/test_accounting_fuzz_oracle.py` — 200 سيناريو شراء عشوائي

Oracle مستقل تماماً (لا يستدعي أي دالة من `app/services/`) يحسب المتوسط
المرجّح يدوياً بمعادلة منفصلة، ويقارَن بالقيم الفعلية المخزَّنة مباشرة
(`InventoryMovement.unit_cost`, `JournalLine.debit_base/credit_base`)
بعد تشغيل `post_purchase_invoice` الحقيقي. **شُغِّل فعلياً على الكود
الحالي بالمستودع — نجح بكل الـ200 سيناريو دون أي `AccountingSanityError`.**
نطاق هذا الملف محدود عمداً بالشراء فقط (البيع/المرتجع/الضريبة تبقى
مغطاة بـ`test_accounting_edge_cases.py` كحالات يدوية دقيقة).

### 31.5 اكتشاف: `tests/test_migration_safety.py` معطَّل حالياً

غير مرتبط بأي تعديل بهذه الجلسة — موجود مسبقاً بالمستودع. الاختبار يحاول
محاكاة عميل بإصدار v1 ثم حقن migration v2 وهمية، لكن `MIGRATIONS` الفعلية
بـ`app/migrations/runner.py` وصلت أصلاً لـv5، فـ`apply_migrations()` يقفز
مباشرة v1→v5 من أول استدعاء ويتجاهل الحقن اللاحق. **قرار مؤجَّل**: يُصلَح
الآن أم يُستبدَل بالكامل ضمن انتقال Alembic القادم (الأرجح، بما أن هذا
النظام (PRAGMA runner) سيُستبدَل أصلاً).

### 31.6 `tools/migration_manager.py` — مُجهَّز، غير مفعَّل

نسخ احتياطي → فحص إصدار → ترقية → تحقق → `integrity_check` → التالي، مع
عزل فشل أي عميل عن البقية. لا يُستدعى من أي مسار تشغيل حالي — ينتظر
مرحلة إدخال Alembic الفعلية بعد اجتياز بوابة §31.3.

**الخطوة التالية**: تشغيل `test_accounting_fuzz_oracle.py` +
`test_accounting_edge_cases.py` + `test_e2e_scenario.py` معاً، تعبئة
`FuzzReport.regression_suite_passed`، والتحقق من `gate() == True` قبل
أي baseline رسمي.

### 31.7 اشتقاق عتبة التقريب رياضياً (تصحيح على §31.3)

الرقم 0.02 الثابت في §31.3 كان غير مبرَّر رياضياً — تم تصحيحه بتتبّع
مسار التقريب الفعلي بالكود:
1. `invoice_calc.py`: `money(net_final)` — تقريب لعملة المستند.
2. `posting.py` `_jline()`: `money(debit × rate)` — تقريب ثانٍ عند التحويل للأساسية.

خطوتا تقريب مستقلتان لكل سطر قيد، كل واحدة أقصى انحراف ±0.005 (نصف
MONEY_QUANT). العتبة الصحيحة = **0.01 × عدد عمليات الشراء بالسيناريو**
(`tests/fuzz_report.py: ScenarioResult.max_allowed_diff()`)، وليست رقماً
ثابتاً بصرف النظر عن حجم السيناريو. مطبّقة فعلياً الآن، لا نظرياً فقط.

### 31.8 تشغيل البوابة الفعلي — `tests/run_gate.py`

يجمع Regression الحقيقي (تشغيل test_accounting_edge_cases.py +
test_e2e_scenario.py + test_per_item_account_posting.py كعمليات فعلية،
exit code) + fuzz+oracle، ويربطهما بـ`FuzzReport.regression_suite_passed`
فعلياً بدل تركه `None` يدوياً. `test_migration_safety.py` مُستبعَد صراحة
عبر `KNOWN_FAILURES` — يظهر دائماً في `fuzz_report.md`، لا يختفي بصمت،
وفق الشرط المتفَق عليه: "known failure غير متعلق، لكن يجب ألا تختفي من
قائمة المخاطر".

**نتيجة التشغيل الفعلي بتاريخ هذه الجلسة: `gate() == PASS ✅`**
(200/200 fuzz، Regression الثلاثة PASS، صفر AccountingSanityError).
→ الشرطان المطلوبان قبل Baseline محسومان الآن. الخطوة التالية: Baseline
رسمي (git tag + حفظ `reports_out/fuzz_report.md` كجزء من دليل الـbaseline
+ schema snapshot)، ثم Alembic.

## §32 — Alembic: التنفيذ والاختبار الفعلي (بعد تأكيد عدم وجود بيانات إنتاجية)

تحقّقتُ قبل البدء: لا يوجد `data/` ضمن المستودع (مُستبعَد بـ`.gitignore`
أصلاً)، فلا قواعد عملاء حقيقية بمتناول هذه الجلسة إطلاقاً. التنفيذ التالي
كان على قواعد تجريبية بُنيت خصيصاً لهذا الاختبار، لم يلمس أي بيانات فعلية.

### 32.1 الإعداد
- `alembic init alembic`، `alembic/env.py` معدَّل: يقرأ `-x db_path=...`
  لكل قاعدة عميل (بدل رابط ثابت بـ`alembic.ini`)، `render_as_batch=True`
  إلزامي بسبب قيود SQLite على ALTER TABLE.
- Migration أساسية (`ac98294ac8e0`) وُلِّدت عبر `--autogenerate` ضد قاعدة
  فارغة. **راجعتها يدوياً**: 10/10 جداول مطابقة تماماً لـ
  `baseline/schema_snapshot.sql`، `downgrade()` موجود وسليم.

### 32.2 نتائج الاختبار الفعلي (نُفِّذ فعلاً، لا محاكاة نظرية)
1. **عميل جديد كلياً**: `alembic upgrade head` على قاعدة فارغة → 11 جدولاً
   بما فيها `alembic_version`. ✅
2. **عميل قديم موجود مسبقاً** (أُنشئ بـ`Base.metadata.create_all()` كما
   تفعل `app/db.py` حالياً، بلا أي تاريخ Alembic): `alembic stamp head`
   → `current` يُظهر head مباشرة، بلا إعادة تشغيل أي شيء. ✅
3. **قاعدة ببيانات واقعية متنوعة** (3 مشتريات SYP/USD/EUR + مبيعة، عبر
   `post_purchase_invoice`/`post_sales_invoice` الحقيقيتين): أُخذت بصمة
   كاملة (عدد الفواتير، القيود، الحركات المخزنية، رصيد كل حساب،
   `debit_base`/`credit_base` الإجمالي) → `stamp head` → migration فعلية
   بـ`batch_alter_table` (إضافة عمود) → **البصمة بعد الترقية مطابقة
   حرفياً للبصمة قبلها، بلا أي فرق**. هذا يفي بالشرط الذي طُلب صراحة: "لا
   يكفي التأكد أن migration انتهت بدون Exception" — تحقَّقنا من تطابق
   البيانات والمجاميع نفسها، لا فقط عدم وجود خطأ.
4. **اختبار الفشل** (الأهم): عميل سليم + عميل بملف تالف (bytes عشوائية)
   عبر `tools/migration_manager.py` الفعلي. **اكتُشف خلل حقيقي أثناء هذا
   الاختبار نفسه**: `get_current_revision()` كانت تلتقط `OperationalError`
   فقط، بينما ملف تالف تماماً يرفع `sqlite3.DatabaseError` (الصنف الأب)،
   وكان الاستدعاء الأول لها يقع خارج كتلة `try` أصلاً — أي عميل تالف واحد
   كان سيُسقط الأداة بالكامل، عكس مقصود "عزل الفشل" تماماً. **أُصلح**:
   التقاط `DatabaseError` الأشمل + تغليف الاستدعاء الأول بـtry/except.
   بعد الإصلاح: العميل السليم ترقّى بنجاح، العميل التالف فشل وعُزل تماماً
   دون التأثير على الآخر، والنسخة الاحتياطية للعميل التالف أُخذت *قبل*
   محاولة الترقية كما يجب (موجودة رغم فشل الترقية لاحقاً).

### 32.3 ما لم يُنفَّذ عمداً
- لم تُطبَّق أي migration فعلية دائمة على schema المشروع (migration
  اختبار batch mode كانت مؤقتة، حُذفت من `alembic/versions/` بعد إثبات
  الآلية — لم تكن تغييراً منتجيّاً حقيقياً).
- لم يُستدعَ `tools/migration_manager.py` من أي مسار تشغيل حقيقي، ولم
  تُلمَس قواعد `data/companies/*.db` الفعلية (غير موجودة أصلاً بهذه البيئة).
- استبدال `app/migrations/runner.py` (PRAGMA-based) بالكامل بـAlembic في
  `app/db.py` — قرار منتجي يحتاج موافقتكم الصريحة قبل تنفيذه، لم يُنفَّذ بعد.

**الخطوة التالية المقترحة**: قراركم بشأن 32.3 الأخيرة (استبدال
`app/db.py`/`runner.py` فعلياً بـAlembic في مسار تشغيل التطبيق)، ثم العودة
لبقية §31 (حالات العملات/المخزون العدوانية، دورة المخزون، الفواتير UI).

## §33 — دمج Alembic في مسار التطبيق الحقيقي (بعد مراجعة صديق المبرمج)

نُفِّذت قائمة المراجعة العشرية كاملة قبل أي تعديل على app/db.py:

### 33.1 خريطة مسار فتح قاعدة العميل (نقطة 1-2)
`main.py` هو المستدعي الوحيد فعلياً: `get_registry_session()` →
`create_company()` عند الحاجة → `open_company_db(db_filename)` →
كانت تستدعي `apply_migrations(engine)` مباشرة → `Session`. سطح تماس
صغير ومحدود (ميزة مهمة). `app/models.py::create_company_database()`
مسار منفصل غير مستخدَم فعلياً بالتطبيق (سكربت تجريبي بـ`__main__` فقط).

### 33.2 طبقة تكامل معزولة أولاً — `app/migrations/alembic_runner.py`
`ensure_schema_up_to_date(db_path)` توحّد 3 حالات (عميل جديد/قديم بلا
Alembic/يُدار بـAlembic أصلاً)، بنسخ احتياطي **فقط** عند ترقية فعلية
مطلوبة، واستعادة تلقائية عند الفشل. **لم تُستدعَ من app/db.py إلا بعد
اجتياز الاختبار المعزول الكامل** (نقطة 4).

### 33.3 اختبار الحالات الخمس المطلوبة (نقطة 4) — `tests/test_alembic_integration.py`
عميل جديد، عميل قديم على Baseline ببيانات واقعية، عميل لديه migration
مستقبلية حقيقية (أُنشئت وحُذفت أثناء الاختبار)، عميل تالف، عميلين معاً
أحدهما يفشل. **11/11 تحقّق نجح**، بعد اكتشاف وإصلاح **خللين حقيقيين**:
- `tools/migration_manager.py`: التقاط `OperationalError` فقط بدل
  `DatabaseError` الأشمل، والاستدعاء الأول خارج `try` — عميل تالف واحد
  كان يُسقط الأداة بالكامل.
- نفس النمط بالضبط تكرر في `alembic_runner.py` نفسها (`_has_any_tables`
  خارج `try`) — أُصلح بفصل عمليات *القراءة* (بلا حاجة نسخة احتياطية) عن
  *الكتابة* (نسخة احتياطية + استعادة) بوضوح.

### 33.4 الربط الفعلي بـ`app/db.py` (نقطة 5)
`open_company_db()` تستدعي الآن `ensure_schema_up_to_date()` بدل
`apply_migrations()` المباشرة. **`app/migrations/runner.py` لم يُحذف** —
يبقى مُستخدَماً داخلياً من `alembic_runner.py` لتحويل عملاء قدامى لأول
مرة فقط (كما طُلب صراحة: لا حذف قبل نجاح اختبار التكامل الكامل).

### 33.5 اختبار مسار التطبيق الحقيقي كاملاً (نقطة 6) — `tests/test_app_path_after_alembic.py`
عبر `open_company_db()`/`create_company()` الحقيقيتين (نفس ما يستخدمه
`main.py`): فاتورة شراء، فاتورة بيع، مرتجع بيع، سند قيد يدوي، استعلام
مخزون، ميزان مراجعة — **كلها تعمل وتتوازن فعلياً**. كذلك تحقَّق منطق
`main.py` نفسه (بمعزل عن واجهة Qt) يعمل من الصفر عبر Alembic.

### 33.6 `test_migration_safety.py` — استبدال كامل، لا Known Failure متبقٍ
كما أصرّ صديق المبرمج: لم يُترك كـKnown Failure دائم بعد دمج Alembic.
أُعيدت كتابته بالكامل ليختبر نفس الوعد الجوهري (عميل قديم لا يفقد بياناته
عند migration جديدة) لكن عبر `alembic_runner.py` الفعلي. **نجح**.
`tests/run_gate.py` مُحدَّث: 6 ملفات Regression (3 قديمة + 3 خاصة بـ
Alembic)، **`KNOWN_FAILURES` فارغة الآن**.

### 33.7 نتيجة البوابة الكاملة بعد كل ما سبق
`python3 tests/run_gate.py` → **PASS ✅** (6/6 regression، 200/200 fuzz،
صفر known failures، صفر AccountingSanityError).

### 33.8 ما لم يُنفَّذ بعد (قرارات متروكة عمداً)
- حذف `app/migrations/runner.py` نهائياً — يبقى حياً لدوره في تحويل
  العملاء القدامى؛ حذفه قرار منفصل يحتاج فترة مراقبة كافية أولاً.
- خطة rollback الرسمية الموثّقة كسياسة تشغيلية (نقطة 9) — الآلية موجودة
  ومُختبَرة فعلياً بالكود (نسخ احتياطي + استعادة تلقائية عند الفشل)، لكن
  لم تُكتب كإجراء تشغيلي (runbook) منفصل للفريق بعد.

**الخطوة التالية**: العودة لبقية §31 (حالات العملات/المخزون العدوانية،
دورة المخزون الكاملة)، ثم الفواتير UI — لا شاشة جديدة قبلها.

## §34 — توضيح صريح: runner.py وضمان عدم التشغيل المزدوج

راجع رأس `app/migrations/runner.py` نفسه الآن — وُثِّق فيه مباشرة (لا هنا
فقط، حتى يراه أي مطوّر يفتح الملف مباشرة دون قراءة WORKFLOW.md): لماذا ما
زال موجوداً، أين يُستدعى حصراً (فرع واحد فقط بـ`alembic_runner.py`)، متى
يتوقف تلقائياً لكل عميل، وشرط حذفه لاحقاً (فترة مراقبة كافية + موافقة
موثّقة صريحة).

**إثبات رقمي لعدم التشغيل المزدوج** — `tests/test_migration_double_run_safety.py`:
عدّاد استدعاءات فعلي (`unittest.mock.patch`) حول `apply_migrations`،
4 محاولات فتح متتالية لنفس العميل → **استُدعيت مرة واحدة بالضبط**. هذا
إثبات، لا وصف نصي واعتماد على قراءة الكود.

## §35 — الاختبارات العدوانية للعملات والمخزون (بعد Alembic)

`tests/test_aggressive_currency_inventory.py` — 23 تحققاً، Oracle مستقل
تماماً (`class AverageCostOracle`، معادلات يدوية، لا استدعاء لأي كود
إنتاجي). **نجحت جميعاً من أول تشغيل** بلا حاجة لأي إصلاح بالمحرك —
مؤشر جيد على أن إصلاحات §29/§30 والحراسة اللاحقة كانت شاملة فعلاً.

### 35.1-35.4 — المجموعات المُغطّاة
1. Weighted Average عبر SYP/USD/EUR + بيع + مرتجع بيع — يطابق Oracle.
2. تمييز سعر الوحدة عن سعر الصرف (3 أسعار صرف مختلفة لنفس العملة).
3. **الأهم تاريخياً**: مرتجع بيع بعد تغيّر المتوسط — يستخدم التكلفة
   التاريخية لبيعه الأصلي (10,000)، لا المتوسط الحالي بعد شراء جديد.
4. فاتورة بيع بمادتين، كل مادة بحسابات Inventory/COGS/Sales منفصلة
   كلياً — كل مبلغ وصل لحسابه الصحيح (تأكيد إضافي لإصلاح §27).

### 35.5 — إضافة: مرتجع شراء بسعر صرف مختلف عن الأصلي
سيناريو يطابق حرفياً ما وثّقه تعليق الكود بـ`returns.py::post_purchase_return`
كخطأ تاريخي مُصلَح (§30 نمط مشابه): شراء 50×2USD@15,000، مرتجع 10 وحدات
لكنه مُدخَل بسعر صرف 18,000 — **القيد يستخدم التكلفة التاريخية (300,000)
لا سعر صرف المرتجع (360,000 لو كان فيه الخطأ)**. كذلك تحقّقنا أن قيمة
`InventoryMovement` وسطر القيد مصدر واحد متطابق (لا مصدرين مختلفين لنفس
الرقم، وهو بالضبط ما كان يسبب التناقض قبل إصلاح §30).

### 35.6 — مجموعة 5 غير قابلة للاختبار حالياً (فجوة حقيقية، لا قصور اختباري)
بحثتُ بالكود: **لا توجد أي وحدة receipts/payments/settlement/fx_gain/fx_loss
بالمشروع حالياً إطلاقاً**. لا يوجد شيء لاختباره — قبض/دفع بعملة مختلفة عن
الفاتورة وفروقات الصرف عند التسوية ميزة غير مبنية بعد، وليست ثغرة بهذه
الجلسة. يحتاج قراراً منتجياً منفصلاً: هل تُبنى هذه الميزة الآن أم تُؤجَّل؟

### 35.7 نتيجة البوابة الكاملة
`tests/run_gate.py` مُحدَّث: **8 ملفات Regression** (أضيف
`test_migration_double_run_safety.py` و`test_aggressive_currency_inventory.py`)
+ 200 fuzz → **PASS ✅**، صفر Known Failures.

## §36 — الخطوة التالية المتفَق عليها
1. دورة المخزون الكاملة (Full Inventory Lifecycle) — لم تُنفَّذ بعد بهذه
   الجلسة كوحدة منفصلة (جزئياً مغطاة ضمن §35.1، لكن ليست دورة شاملة
   بمفهوم صديق المبرمج: شراء→مخزون→بيع→COGS→مرتجع بيع→إعادة مخزون→
   مرتجع شراء→الرصيد النهائي بأكثر من مادة وعملية).
2. **بعد نجاحها**: Regression + E2E + Fuzz كاملاً مرة أخيرة (تم فعلياً
   الآن ضمن §35.7، لكن يُعاد بعد أي تغيير جديد بدورة المخزون).
3. **Baseline جديد** — صريح: لا يُعتبر Baseline v1 (§31) هو المرجع
   النهائي بعد كل تغييرات Alembic ومرحلتي §33/§35. Baseline v2 يُنشأ
   فقط بعد نجاح دورة المخزون الكاملة، لا قبلها.
4. بعدها فقط: واجهات الفواتير والمرتجعات (UI) — لا قبل ذلك.

## §38 — إصلاح: تعارض اسم مجلد alembic مع المكتبة نفسها

اكتُشف عند تشغيل `main.py` فعلياً على جهاز حقيقي (لأول مرة خارج بيئة
الاختبار): `ModuleNotFoundError: No module named 'alembic.config'`.

**السبب مُثبَت تجريبياً، لا افتراضاً**: مجلد `alembic/` بجذر المشروع (بلا
`__init__.py`، كما ينشئه `alembic init` افتراضياً) يصبح namespace package
مرشّح عند البحث عن `import alembic`. إن كانت حزمة alembic الحقيقية
مثبَّتة، بايثون يتجاوز المجلد المحلي تلقائياً (namespace package لا "يفوز"
على حزمة حقيقية). **لكن** إن لم تكن مثبَّتة، يتحول المجلد المحلي لبديل
فارغ يُقبَل كـ`alembic`، فيفشل تحديداً عند طلب `alembic.config` (غير
موجود بالمجلد المحلي) — بالضبط رسالة الخطأ التي ظهرت.

**الإصلاح**: أُعيدت تسمية المجلد لكامل المشروع: `alembic/` →
`alembic_migrations/` — يزيل احتمال التصادم نهائياً بغض النظر عن حالة
التثبيت، ومهم بشكل خاص لأي تجميد مستقبلي للتطبيق (PyInstaller وغيره)
حيث تختلف آلية حل الاستيراد عن البيئة التطويرية العادية. عُدِّل:
`alembic.ini` (`script_location`)، `app/migrations/alembic_runner.py`،
و`tests/test_alembic_integration.py`/`test_migration_safety.py` (مسارات
اختبار مؤقتة). `tests/run_gate.py` أُعيد تشغيله كاملاً بعد التعديل —
PASS ✅، لم ينكسر شيء.

**تعليمات فورية لأي بيئة تشغيل جديدة (بما فيها جهازك الحالي)**:
```
pip install -r requirements.txt
```
داخل الـvenv المفعَّلة، قبل أي تشغيل لـ`main.py`.

## §39 — قاعدة محاسبية أساسية: معنى InventoryMovement.unit_cost (قبل أي إصلاح)

**القاعدة المعتمدة رسمياً** (وفق ما اتفق عليه صديق المبرمج، وتوافق تصميم
الكود الحالي فعلياً — `posting.py` و`returns.py` كلاهما يكتبان هذه القيمة
بهذا المعنى تحديداً منذ البداية):

> **`InventoryMovement.unit_cost` هو التكلفة التاريخية المعتمدة لهذه
> الحركة بالعملة الأساسية وقت ترحيلها، ولا يُعاد تسعيرها بأثر رجعي
> باستخدام متوسط التكلفة الحالي عند أي إعادة بناء لاحقة للمخزون.**

تفصيلاً حسب نوع الحركة:
- **شراء**: `unit_cost` = تكلفة الوحدة بعد التحويل للعملة الأساسية (سعر
  الفاتورة × سعر الصرف وقتها).
- **بيع**: `unit_cost` = المتوسط المرجّح **وقت البيع تحديداً** (وليس أي
  متوسط لاحق).
- **مرتجع بيع مرتبط بفاتورة أصلية**: `unit_cost` = تكلفة حركة البيع
  الأصلية نفسها (تاريخية).
- **مرتجع شراء مرتبط بفاتورة أصلية**: `unit_cost` = تكلفة حركة الشراء
  الأصلية نفسها (تاريخية).
- **مرتجع حر (غير مرتبط بأي فاتورة أصلية)**: `unit_cost` = المتوسط
  الحالي وقت الإرجاع (لا "أصل تاريخي" موجود ليُقرأ منه — استثناء موثَّق
  ومقصود، لا مخالفة للقاعدة).

**النتيجة المترتبة على هذه القاعدة**: أي دالة تعيد بناء قيمة المخزون من
سجل الحركات (`get_item_stock_summary` وأي دالة مشابهة مستقبلاً) **يجب**
أن تستخدم `movement.unit_cost` المخزَّن فعلياً على كل حركة خروج، لا أن
تعيد حساب متوسط جديد مستقل عند كل حركة خروج. إعادة الحساب المستقل تخالف
هذه القاعدة تحديداً وتنتج تناقضاً بين تقرير المخزون ودفتر الأستاذ — وهذا
بالضبط ما اكتُشف في §39.1 أدناه.

### §39.1 — الاكتشاف الفعلي والإثبات (قبل الإصلاح)

`tests/test_inventory_unit_cost_source_of_truth.py` كُتب **أولاً وشُغِّل
على الكود غير المُصلَح عمداً** لإثبات أن الاكتشاف حقيقي لا وهمي. فشل بدقة
عند نفس النقطة المتوقعة: بعد مرتجع شراء مرتبط (سيناريو A@12,000 →
B@18,000 → بيع → C@20,000 → مرتجع مرتبط بـA):
`summary=1,819,230.77` بينما `oracle(Σ unit_cost)=1,910,000.00` — فرق
90,769.23 بالضبط (= 20 وحدة × فرق المتوسط الملوَّث عن التاريخي). هذا
يطابق تماماً وصف صديق المبرمج: "دفتر الأستاذ يقول رقماً، والتقرير يقول
رقماً آخر".

### §39.2 — الإصلاح المُطبَّق

`app/services/item_queries.py::get_item_stock_summary` — تعديل سطر واحد
جوهرياً: حركات الخروج تُقيَّم الآن بـ`D(m.unit_cost)` المخزَّن فعلياً على
الحركة، لا بمتوسط `total_cost/total_qty` مُعاد حسابه محلياً داخل الدالة.
هذا يُصحِّح أيضاً كل حساب متوسط لاحق (`_average_cost` بـ`posting.py`
يستدعي هذه الدالة نفسها) — أي بيع يقع بعد مرتجع شراء مرتبط سيستخدم الآن
المتوسط الصحيح المُعدَّل فعلياً، لا متوسطاً مبنياً على افتراض خاطئ.

### §39.3 — التحقق بعد الإصلاح (3+1 مصادر مستقلة، لا مصدرين فقط)
لكل مرحلة حرجة: `InventoryMovement` الخام، `get_item_stock_summary()`،
حساب المخزون بدفتر الأستاذ الفعلي، **و**Oracle مستقل رابع (معادلة
summation مباشرة Σ(qty×unit_cost) لا تستدعي أي كود إنتاجي) — **24/24
تحقّق نجح**. كما أُعيد تشغيل `tests/test_full_inventory_lifecycle.py`
بالكامل (نفس الملف من الجلسة السابقة، بلا أي تعديل على منطق Oracle
الخاص به للتحايل) — **40/40 نجح** بعد الإصلاح، مقابل فشل حقيقي واحد
مسجَّل قبل الإصلاح في المرحلة السادسة تحديداً.

### §39.4 نتيجة البوابة الكاملة النهائية
`tests/run_gate.py` مُحدَّث: **10 ملفات Regression** (أضيف
`test_inventory_unit_cost_source_of_truth.py` و`test_full_inventory_lifecycle.py`)
+ 200 fuzz → **PASS ✅**. هذا هو Gate النهائي المرجعي لـBaseline v2.

## §40 — Baseline v2

`v1-accounting-engine-stable` (§31) أصبح مرجعاً تاريخياً فقط — يسبق دمج
Alembic بالكامل (§32-§33) وإصلاح §39. Baseline v2 يُنشأ الآن بنفس
المنهجية الست الخطوات المُتَّبعة سابقاً (tag + تقرير Gate محفوظ + schema
snapshot + بيئة Python/الحزم + commit + تحقّق فعلي من الاستعادة من بيئة
نظيفة).

## §41 — تقرير المراجعة النهائي قبل UI الفواتير

راجع `REVIEW_REPORT.md` بجذر المشروع للتقرير الكامل (A/B/C/D). ملخص:

- **لا نقطة في فئة C تمنع بناء واجهات الفواتير/المرتجعات نفسها** — جاهزة
  ومُختبَرة بعمق (فئة A).
- **تصحيح مهم على استنتاج سابق (§35.6)**: قلت إن القبض/الدفع/فروقات
  الصرف "غير موجودة إطلاقاً" — غير دقيق. المحرك المحاسبي يدعمها فعليًا
  عبر قيود يدوية (مُثبَت رياضيًا بـ`test_e2e_scenario.py` مراحل 9-10،
  بما فيها فرق صرف حقيقي محسوب ومُختبَر). المفقود تحديدًا هو طبقة خدمة
  مخصصة تؤتمت هذا (`post_receipt`/`post_payment`/ربط بالفاتورة/تسوية
  جزئية) — فئة C، لا فئة "غير موجود بالمرة".
- **6 قرارات محاسبية معلَّقة (فئة D)** لم تُفترَض إجاباتها: فرض عملة
  الحساب، عرض الرصيد متعدد العملات، توقيت بناء خدمة القبض/الدفع، نموذج
  التسوية الجزئية، توقيت ربح/خسارة الصرف (محقَّق فقط أم دوري أيضاً)،
  ونموذج إلغاء الفاتورة الكاملة.

بانتظار قرار صديق المبرمج/بلال على نقاط D قبل أي خطوة إضافية.

## §42 — قواعد Settlement/Receipt/Payment/FX (موثَّقة قبل أي كود)

### 42.1 التصميم البنيوي: Invoice → Settlement → Receipt/Payment

جدول `settlements` جديد (لا `balance_due` كحقل مباشر على الفاتورة، كما
اشترط صديق المبرمج): كل سطر Settlement يربط فاتورة واحدة بقيد قبض/دفع
واحد، بمبلغ جزئي أو كلي. فاتورة واحدة يمكن أن يكون لها عدة سطور
Settlement (دفعات متعددة). **الرصيد المستحق يُحسَب ديناميكيًا دائمًا**
(`get_invoice_balance_due`) = إجمالي الفاتورة (`compute_invoice_totals`
— المصدر الوحيد المعتمد أصلًا) ناقص مجموع `amount_foreign` لكل Settlement
مرتبطة — لا حقل مخزَّن قد يفقد تزامنه مع الواقع.

### 42.2 القاعدة المحاسبية لكل تسوية

كل Settlement تُبنى من قيمتين فقط: `amount_foreign` (جزء من مبلغ الفاتورة
الأصلي **بعملة الفاتورة نفسها**، لا بعملة القبض الفعلية) و`settlement_rate`
(سعر الصرف المُعتمَد لهذه التسوية تحديدًا). القيمة الأساسية الفعلية
المقبوضة/المدفوعة = `amount_foreign × settlement_rate` — بصرف النظر عن
العملة الفعلية التي استُلمت بها النقدية فعليًا (حتى لو استُلم SYP مقابل
فاتورة USD، طالما `amount_foreign`/`settlement_rate` يحددان القيمة
بوضوح). هذا يوحّد كل الحالات المطلوبة (11 حالة) بمعادلة واحدة.

### 42.3 فرق الصرف — حسابان منفصلان (وفق قرار صديق المبرمج)

```
fx_diff = (amount_foreign × settlement_rate) − (amount_foreign × invoice.exchange_rate)
```
- **للعميل (Receivable/Asset)**: `fx_diff > 0` → ربح صرف (قبضنا أكثر مما
  سجّلنا)، `fx_diff < 0` → خسارة صرف.
- **للمورد (Payable/Liability)**: العكس تمامًا — `fx_diff > 0` → خسارة
  صرف (دفعنا أكثر مما سجّلنا)، `fx_diff < 0` → ربح صرف.

حسابان منفصلان بشجرة الحسابات الافتراضية: `fx_gain` و`fx_loss` (وليس
حسابًا واحدًا مشتركًا) — القيد يستخدم أحدهما فقط حسب إشارة `fx_diff`،
أبدًا كليهما بنفس القيد الواحد (يمنع بنيويًا احتساب الفرق مرتين — نقطة
12 المطلوب اختبارها).

### 42.4 قواعد الرفض الصريحة (لا افتراض صامت)

- تسوية تتجاوز الرصيد المستحق للفاتورة → `SettlementError` (لا تُنفَّذ
  جزئيًا ولا تُقرَّب).
- تسوية على فاتورة `CANCELLED` أو `DRAFT` (لم تُرحَّل بعد) → `SettlementError`.
- تسوية على فاتورة رُحِّلت نقدًا (`is_cash=True` وقت الترحيل، فلا رصيد
  مستحق أصلًا لأنها سُدِّدت بالكامل عند الترحيل) → `SettlementError`.
  يُكتشَف هذا من السطر الأول بالقيد الأصلي: إن كان حساب الصندوق
  الافتراضي نفسه، فالفاتورة نقدية.

### 42.5 المرتجع بعد وجود تسويات سابقة (قرار موثَّق صراحة، قابل للمراجعة)

**القرار المعتمد الآن**: `balance_due` لكل فاتورة يُحسَب من إجماليها
وتسوياتها **الخاصة بها فقط** — لا يتأثر تلقائيًا بوجود مرتجع مرتبط بها
(المرتجع مستند منفصل تمامًا بحسابه الخاص على حساب العميل/المورد العام،
لا على "رصيد هذه الفاتورة تحديدًا"). أي محاولة تسوية على الفاتورة الأصلية
بعد مرتجع مرتبط بها تُحسَب بنفس القاعدة العادية، بصرف النظر عن المرتجع.
هذا قرار عملي مبسَّط لهذه المرحلة، **وليس نهائيًا بالضرورة** — إذا
احتجتم لاحقًا ربط المرتجع تلقائيًا بخفض الرصيد المستحق لفاتورة أصلها،
هذا تغيير منفصل يحتاج قراركم الصريح حينها.

### 42.6 التحقق المطلوب (12 حالة، Oracle مستقل رياضيًا)
راجع `tests/test_settlement_fx.py` — Oracle يحسب `fx_diff` والرصيد
المتبقي بمعادلة يدوية مباشرة، لا يستدعي `app/services/settlements.py`.

## §43 — تنفيذ طبقة Settlement/Receipt/Payment/FX (اكتشافان حقيقيان أثناء التنفيذ)

التصميم موثَّق كاملاً بـ§42. التنفيذ: `app/models.py::Settlement` (جدول
جديد، migration مُولَّدة عبر autogenerate ومُراجَعة يدوياً)،
`app/services/settlements.py` (`post_receipt`/`post_payment`/
`get_invoice_balance_due`)، حسابا `fx_gain`/`fx_loss` منفصلان بالشجرة
الافتراضية. **26/26 تحققاً نجحت** بـOracle مستقل رياضياً
(`tests/test_settlement_fx.py`) يغطي الحالات الاثنتي عشرة المطلوبة كاملة
+ حالتي دفع (AP) بإشارة معكوسة + رفض استدعاء الدالة الخطأ لنوع الفاتورة
الخطأ.

### 43.1 — اكتشاف حقيقي: هشاشة محاكاة "العميل القديم" بالاختبارات

عند تشغيل البوابة الكاملة، فشلت 3 اختبارات (`test_migration_double_run_safety.py`،
`test_alembic_integration.py`، `test_migration_safety.py`) بخطأ
`table settlements already exists`. **التشخيص**: هذه الاختبارات كانت
تُحاكي "عميلاً قديماً بلا Alembic" عبر `Base.metadata.create_all()`
**الحيّة** مباشرة — وهذا كان يعمل فقط طالما لم يُضَف أي نموذج جديد؛
بمجرد إضافة `Settlement`، أصبحت المحاكاة تُنشئ جدول `settlements` ضمن
"القاعدة القديمة" المُحاكاة أيضاً (لأنه جزء من Base.metadata الحالية)،
فيتصادم لاحقاً مع migration Alembic التي تحاول إنشاءه من جديد.

**الإصلاح**: `tests/_legacy_schema_helper.py` جديد — يبني قاعدة "عميل
قديم" من `baseline/schema_snapshot.sql` **المجمَّد** (10 جداول، يعكس
الشكل الحقيقي وقت تلك النسخة بالضبط)، مع `PRAGMA user_version = 5`
صريحاً (لأن أي عميل حقيقي وصل لهذا الشكل يكون قد مرّ فعلياً بـ
`apply_migrations()` تاريخياً — تركها 0 كان فجوة في المساعد القديم
نفسه). هذا يبقي محاكاة "العميل القديم" صحيحة تاريخياً بصرف النظر عن
نمو Base.metadata مستقبلاً، بعكس الاعتماد على القيمة الحيّة المتغيّرة.

**ملاحظة معمارية موثَّقة للمستقبل (خطر كامن، غير مؤثر حالياً)**:
`_migration_v1_initial_schema` بـ`app/migrations/runner.py` يستدعي
`Base.metadata.create_all()` الحيّة أيضاً — لو وُجد يوماً عميل حقيقي
بـ`PRAGMA user_version` أقل من 5 (لم يكتمل انتقاله للنظام القديم أصلاً)
**بعد** إضافة نموذج جديد لـAlembic، سيقع نفس التصادم فعلياً لا اختبارياً
فقط. **غير مؤثر عملياً الآن** لأنه لا يوجد أي عميل حقيقي إطلاقاً يسبق
إدخال Alembic بهذا المشروع (تأكَّد صراحة من بلال). يُترك موثَّقاً هنا
كخطر معماري كامن يستحق المراجعة إن ظهرت يوماً بيانات عملاء فعلية أقدم
من هذا التاريخ.

### 43.2 — اكتشاف ثانٍ: تصادم كود حساب فعلي

الكود `6105` المُقترَح لـ`fx_loss` بالشجرة الافتراضية تصادم فعلياً مع
حساب فروقات صرف مؤقت أُنشئ يدوياً بـ`tests/test_e2e_scenario.py` (من
الجلسة السابقة قبل وجود هذه الطبقة أصلاً). **الإصلاح**: تغيير كود
`fx_loss` إلى `6106`.

### 43.3 نتيجة البوابة الكاملة
`tests/run_gate.py`: **11 ملفات Regression** (أضيف `test_settlement_fx.py`)
+ 200 fuzz → **PASS ✅**.

**الخطوة التالية**: الخريطة النهائية لمحرك المحاسبة والعملات والمخزون
(`Invoice → Posting → Inventory → COGS → Return → Receivable/Payable →
Receipt/Payment → FX Difference → Reports`) قبل مراجعتها ثم بدء واجهات
الفواتير — كما اتُّفق عليه.

## §44 — قاعدة Cancel/Reverse (D-6) — موثَّقة قبل الكود

### 44.1 الفرق الدقيق بين Return وCancel/Reverse

| | **Return** (`post_return`) | **Cancel/Reverse** (`cancel_invoice`) |
|---|---|---|
| طبيعته | حدث تجاري **جديد** (بضاعة/خدمة عادت فعلياً) | تصحيح خطأ إدخال بحت — لا حدث تجاري جديد |
| تكلفة المخزون | يتفاعل مع التكلفة التاريخية للحركة الأصلية، **يؤثر على المتوسط المستقبلي** | يعكس **نفس** الحركة الأصلية حرفياً (نفس الكمية، نفس `unit_cost`) — لا يُعاد حساب أي شيء |
| الكمية | جزئية أو كلية حسب ما عاد فعلياً | كلية دائماً — إلغاء المستند بأكمله، لا جزء منه |
| القيد | قيد جديد بمنطقه الخاص (`_return_unit_cost`) | عكس حرفي: مدين↔دائن لكل سطر، بنفس `debit_base`/`credit_base` تماماً (نفس نمط `reverse_manual_entry` الموجود فعلياً لليدوي) |
| متى يُستخدَم | خطأ في البضاعة/الكمية بعد أن أصبح الوضع حقيقة تجارية | خطأ في الإدخال نفسه (مادة خطأ، سعر خطأ، فاتورة لعميل خطأ) لم يتحرك بموجبه شيء فعلياً في الواقع |

### 44.2 القاعدة المحاسبية الكاملة

1. **لا تعديل مباشر لمستند POSTED** — كما هو معمول به فعلاً (`ensure_editable`).
2. **الإلغاء ينشئ أثراً عكسياً منفصلاً وقابلاً للتتبع** — لا يُحذف المستند
   الأصلي ولا قيده، فقط `status → CANCELLED` + قيد عكسي جديد
   (`is_reversal_of` يشير للقيد الأصلي، بنفس نمط `reverse_manual_entry`).
3. **لا إعادة احتساب تاريخي** — القيد العكسي يستخدم بالضبط نفس
   `debit_base`/`credit_base` الأصليين (مدين↔دائن)، وحركات المخزون
   العكسية تستخدم بالضبط نفس `unit_cost` الأصلي — لا سعر صرف اليوم، لا
   متوسط تكلفة حالي.
4. **مستند لا يُلغى مرتين** ولا يُلغى **قيد هو نفسه عكسي أصلاً** (نفس
   قاعدتي `reverse_manual_entry` الموجودتين فعلياً، مُعمَّمتان هنا).

### 44.3 التعامل مع Settlement المرتبطة (القرار المعتمد الآن)

**لا يجوز إلغاء فاتورة لها أي Settlement (قبض/دفع) مرتبطة بها.** يُرفض
الإلغاء صراحة (`CancelNotAllowedError`) إن وُجدت تسوية واحدة على الأقل.
السبب: إلغاء فاتورة تلقاها عميل عليها قبض فعلي يعني بالضرورة التعامل مع
استرداد ذلك القبض — قرار مالي منفصل تماماً (استرداد نقدي؟ تحويله لفاتورة
أخرى؟ رصيد دائن للعميل؟) لا يجوز افتراضه برمجياً. **هذا قرار متحفّظ
مقصود**: لو احتجتم لاحقاً إلغاء فاتورة لها تسويات، هذا تصميم منفصل يحتاج
قراركم الصريح حينها (مثلاً: إلغاء التسويات أولاً بآلية مقابلة، ثم إلغاء
الفاتورة).

### 44.4 التعامل مع المخزون عند الإلغاء

لكل `InventoryMovement` مرتبطة بالفاتورة الأصلية (`source_type`/
`source_id`): تُنشأ حركة عكسية مطابقة تماماً (نفس `quantity`، نفس
`unit_cost` الأصلي بالضبط)، لكن باتجاه معاكس (`IN`↔`OUT`). هذا يعيد
الكمية والقيمة إلى حالتها قبل الفاتورة **حرفياً**، بصرف النظر عن أي
عمليات لاحقة غيّرت المتوسط — تماماً كما يفعل مرتجع الشراء المرتبط (§39)،
لكن هنا الانعكاس كامل دائماً (100% من الفاتورة)، لا جزئي.

### 44.5 نطاق التنفيذ الآن

يُطبَّق فقط على **الفواتير** (`cancel_invoice()`) — ليس نطاقه الآن `Return`
نفسها (فهي أصلاً حدث منفصل، لا تُلغى بنفس المنطق) ولا `Settlement`
(الإلغاء مرفوض أصلاً إن وُجدت تسوية، فلا حاجة لعكسها هنا). عكس القيد
اليدوي (`reverse_manual_entry`) موجود ومختبَر أصلاً منذ §35.

### 44.6 التحقق المطلوب
اختبار `tests/test_cancel_invoice.py` يثبت: عكس فاتورة شراء، عكس فاتورة
بيع، رفض إلغاء فاتورة لها تسوية، رفض إلغاء فاتورة ملغاة أصلاً، تطابق
حرفي لقيم المخزون قبل الفاتورة وبعد الإلغاء (لا مجرد "الكمية عادت"، بل
القيمة والمتوسط أيضاً)، وعدم تأثر أي فاتورة أخرى بالإلغاء.

### 44.7 نتيجة التنفيذ الفعلي

`app/services/invoice_cancel.py::cancel_invoice()` — نُفِّذ فعلاً (لا
توثيق نظري فقط)، لأن الاختبار المطلوب يحتاج كوداً حقيقياً ليُثبِت عليه.
**15/15 تحقّق نجح** من أول تشغيل (`tests/test_cancel_invoice.py`): عكس
فاتورة شراء وبيع حرفياً (قيمة/كمية تعود لصفر أو لحالتها قبل الفاتورة
بالضبط)، رفض الإلغاء مع تسوية مرتبطة، رفض الإلغاء المزدوج، وعدم تأثر
فواتير أخرى لنفس المادة عند إلغاء واحدة منها.

**البوابة الكاملة النهائية**: `tests/run_gate.py` — **12 ملف Regression**
+ 200 fuzz → **PASS ✅**، دون اعتبار هذا دليل اكتمال كل المتطلبات
التجارية، بل دليل عدم حدوث Regression (كما اشترط صديق المبرمج صراحة).

D-2 وD-5 يبقيان مؤجَّلين وموثَّقين كما هما (REVIEW_REPORT.md). D-6
محسوم الآن: Return وCancel/Reverse منفصلان تماماً، وتصميم الفواتير
القادم يجب أن يتوافق مع وجود `cancel_invoice()` كمسار إلغاء رسمي.

## §45 — مواصفة UI: اكتشاف أن الواجهة موجودة فعلاً (راجع UI_DESIGN.md §45)

قبل كتابة "مواصفة فاتورة البيع"، اكتُشف أن `app/ui/common/document_form.py`
(623 سطر) **موجود ويعمل فعلياً** ويغطي 9 من 12 نقطة مطلوبة (مُدقَّقة
بالدليل، لا افتراضاً). التفاصيل الكاملة والإضافة v1.1 (الفجوات الثلاث
فقط: عرض الرصيد المستحق/التسويات، زر Cancel منفصل عن المرتجع، Esc) في
`UI_DESIGN.md §45`. لم يُكتَب أي كود UI جديد بعد — بانتظار اعتماد
الإضافة قبل التنفيذ، تماشياً مع "لا تعدّل محرك المحاسبة لمجرد UI؛ إذا
ظهرت حاجة جديدة، أوقف التنفيذ وحددها أولاً" ونفس المبدأ ينطبق هنا على
تصميم الواجهة نفسها.

## §46 — قرار: التكلفة منفصلة لكل مستودع (لا موحّدة على مستوى الشركة)

### 46.1 القاعدة المعتمدة

> **متوسط التكلفة، قيمة المخزون، وCOGS تُحسَب لكل مستودع على حدة.**
> المستودع ليس بُعداً مكانياً للتقارير فقط — هو جزء من إدارة المخزون
> نفسها. عمليات مستودع A لا تؤثر إطلاقاً على متوسط تكلفة مستودع B، حتى
> لو كانا يحملان نفس المادة بكميات وتكاليف مختلفة تماماً.

### 46.2 خريطة التأثير الكاملة (لا افتراض أن مكاناً واحداً كافٍ)

| المرحلة | الدالة | التأثير | الحالة |
|---|---|---|---|
| Purchase | `post_purchase_invoice` | `unit_cost` = سعر الشراء المحوَّل مباشرة — **لا علاقة بالمتوسط أصلاً** | لا تغيير مطلوب |
| Inventory/Average Cost | `get_item_stock_summary()` | المصدر الوحيد لحساب المتوسط — **يحتاج فلترة `warehouse_id`** | **يُعدَّل** |
| Average Cost (المستدعي الداخلي) | `_average_cost()` [posting.py] | يستدعي أعلاه — **يحتاج تمرير `warehouse_id` إلزامياً (لا افتراضي صامت)** | **يُعدَّل** |
| Sale/COGS | `post_sales_invoice` | يستدعي `_average_cost` — `warehouse_id` **محسوبة أصلاً بالسياق** (`_invoice_warehouse_id`) لكن غير مُمرَّرة | **يُعدَّل (سطر واحد)** |
| Sales Return (مرتبط) | `_return_unit_cost` [returns.py] | يقرأ `unit_cost` من حركة البيع الأصلية مباشرة — **مستقل عن المتوسط أصلاً، يحمل مستودعه الصحيح ضمنياً** | لا تغيير مطلوب |
| Sales Return (حر، غير مرتبط) | نفس الدالة، تستدعي `_average_cost` | **يحتاج تمرير `warehouse_id` الخاص بمرتجع نفسه** | **يُعدَّل** |
| Purchase Return (مرتبط) | نفس المبدأ — يقرأ من حركة الشراء الأصلية | لا تغيير مطلوب | — |
| Stock Reports (شاشات القراءة فقط) | `item_card_dialog.py`، `item_list_view.py` | تبقى تعرض **إجمالي كل المستودعات** كعرض تجميعي شرعي (تقرير "إجمالي ما تملكه الشركة") — `warehouse_id` اختياري هنا، `None` = الإجمالي | **بلا تغيير الآن** (خارج نطاق هذه المرحلة — UI لاحقاً) |
| Stock Transfer | `transfer_stock()` [inventory_transfer.py] | **خلل حقيقي إضافي مُكتشَف**: يستخدم تكلفة "آخر حركة دخول" **للمادة عالمياً**، لا تكلفة مستودع المصدر تحديداً — راجع §46.5 | **يُعدَّل** |

### 46.3 التوقيع الجديد

`get_item_stock_summary(session, item_id, warehouse_id=None)` — إن
`None`: إجمالي كل المستودعات (لأغراض عرض/تقرير تجميعي شرعي فقط، **ممنوع
استخدامها بهذا الشكل لأي قرار تسعير/ترحيل**). `_average_cost(session,
item_id, warehouse_id)` — **بلا قيمة افتراضية إطلاقاً**: أي استدعاء
لحساب تكلفة قيد فعلي يجب أن يمرّر مستودعاً صراحة، لمنع أي انزلاق صامت
مستقبلي لنفس الخلط الذي اكتُشف الآن.

### 46.4 قاعدة النقل بين المستودعات (Transfer)

الوحدات المنقولة تدخل مستودع الوجهة **بتكلفة مستودع المصدر الحالية
وقت النقل** (وليس بآخر سعر شراء عالمي للمادة)، ثم **تندمج طبيعياً ضمن
متوسط الوجهة المرجّح** (تماماً كأي دخول عادي — لا معاملة خاصة إضافية).
مثال: نقل 100 وحدة بتكلفة 1,000 (متوسط A الحالي) لمستودع B الذي لديه
50 وحدة بمتوسط 3,000 → متوسط B الجديد = (50×3,000+100×1,000)/150 = 1,667.

### 46.5 اكتشاف إضافي أثناء الفحص (خلل حقيقي، لا افتراض)

`transfer_stock()` الحالية تقرأ **آخر `InventoryMovement` باتجاه IN
للمادة بأي مستودع** (`.order_by(movement_date.desc()).first()`، بلا
فلترة `warehouse_id`) — أي لو اشتُريت المادة مؤخراً لمستودع C بسعر مختلف
تماماً، سيُستخدَم هذا السعر خطأً عند نقلها من A إلى B، بدل تكلفة A
الفعلية. هذا خلل موجود بمعزل عن قرار "موحّد/منفصل" أصلاً — سيُصلَح ضمن
هذه الدفعة نفسها.

### 46.6 نتيجة التنفيذ الفعلي

**الملفات المعدَّلة**: `item_queries.py` (`get_item_stock_summary` تقبل
`warehouse_id` اختياري)، `posting.py` (`_average_cost` تفرض `warehouse_id`
إلزامياً، `post_sales_invoice` يمرّرها)، `returns.py` (`_return_unit_cost`
تفرضها للحالة الحرة)، `inventory_transfer.py` (النقل يستخدم متوسط
المصدر الحالي، لا آخر شراء عالمي — الخلل الإضافي المُكتشَف بـ§46.5)،
`opening_balances.py` (`warehouse_id` اختياري بدل الافتراضي الصامت دائماً).

**الاختبارات**:
- `test_warehouse_aggregate_view.py` (سابقاً PRE_FIX) — أُعيد تأطيره:
  يوثّق أن `warehouse_id=None` يبقى تجميعياً **عمداً** (لا خلل)، ويؤكد
  التحديد الصريح يُرجع تكلفة منفصلة تماماً.
- `test_warehouse_cost_isolation.py` — **12/12 تحقّق نجح** من أول تشغيل:
  الحالات السبع المطلوبة كاملة (عزل المتوسط، بيع من A/B منفصل، شراء
  جديد لا يمسّ الآخر، مرتجع بيع/شراء بالتكلفة التاريخية الصحيحة، تعدد
  المستودعات لنفس المادة، النقل A→B يحمل متوسط A ويندمج بمتوسط B).

**اكتشاف إضافي أثناء الإصلاح (خلل باختبار قديم، لا بالمنتج)**:
`test_per_item_account_posting.py` فشل بعد الإصلاح — السبب: أنشأ
مستودعاً باسم `"الرئيسي"` بينما `get_default_warehouse()` يبحث تحديداً
عن `"المستودع الرئيسي"` (فرق نصي). قبل الإصلاح كان هذا غير مؤثر (التكلفة
مُختلَطة بلا فلترة مستودع أصلاً)؛ بعد الإصلاح الصحيح، الفاتورة سقطت
لمستودع افتراضي **جديد فارغ** فأصبحت التكلفة صفراً. أُصلح بتحديد
`warehouse_id` صراحةً بالفاتورتين (أكثر واقعية من الاعتماد على تطابق
اسم ضمني) — **نفس نمط هشاشة "العميل القديم" المُكتشَف بـ§43** يتكرر هنا
بصيغة مختلفة: اعتماد اختبار قديم على سلوك ضمني غير موثَّق كان الإصلاح
الصحيح يكشفه حتماً.

**نتيجة البوابة الكاملة**: `tests/run_gate.py` — **14 ملف Regression**
+ 200 fuzz → **PASS ✅**.

### 46.7 الخطوة التالية
المحرك جاهز الآن لربط ComboBox المستودع بالواجهة (`Invoice.warehouse_id`
موجود، الترحيل يستخدمه صحيحاً بتكلفة معزولة لكل مستودع). **لم يُضَف أي
تعديل UI بعد** — بانتظار تأكيدكم، تماشياً مع "لا نضيف ComboBox قبل نجاح
اختبارات المحرك". لا Baseline جديد بعد أيضاً — بانتظار اكتمال UI
المستودع + بقية إضافات §45 (Esc، الرصيد المستحق، Cancel) معاً أولاً.

## §47 — ربط ComboBox المستودع بواجهة الفواتير (تنفيذ فعلي)

كل التعديلات في `app/ui/common/document_form.py` وحده — ينطبق تلقائياً
على الشاشات الأربع (بيع/شراء/مرتجع بيع/مرتجع شراء) لأنها جميعاً فورمات
رفيعة فوق `BaseDocumentFormView`.

### 47.1 التنفيذ
- `warehouse_combo` بصف مستقل في رأس المستند (لا يتصادم مع صف رابط
  المرتجع)، عنصر أول placeholder بلا `userData` صالح.
- **لا سقوط صامت**: `_validate_warehouse_selected()` تُستدعى في
  `_save_draft()` و`_post()` — رسالة خطأ واضحة ومنع كامل للحفظ/الترحيل
  إن لم يُختَر مستودع.
- `_post()` يُزامن `invoice.warehouse_id` من الـComboBox مباشرة قبل
  الترحيل (يحمي من تعديل الاختيار بعد آخر حفظ مسودة).
- **مرتجع مرتبط**: `_load_from_original()` و`_load_invoice()` يرثان
  `warehouse_id` من الفاتورة الأصلية تلقائياً **ويُقفَل** الـComboBox
  (`setEnabled(False)`) — لا يمكن اختيار مستودع مختلف (§47.3 القاعدة).
- `_refresh_editability()`: القفل يُطبَّق أيضاً لو المستند POSTED، أو
  لو مرتجع مرتبط بصرف النظر عن حالة الترحيل.
- `stock_transfer.py`، `_average_cost()`، `get_item_stock_summary()`:
  **بلا أي تعديل جديد** — استُخدم المحرك الموجود من §46 حرفياً كما هو.

### 47.2 الاختبار التكاملي الحقيقي (لا Mock للخدمات)
`tests/test_ui_warehouse_integration.py` — عبر `SalesInvoiceFormView`/
`PurchaseInvoiceFormView` الفعليتين (PySide6 offscreen)، مستودعين A
(تكلفة 1,000) وB (تكلفة 9,000) بالضبط كما طُلب. **9/9 تحقّق نجح**:
اختيار UI → `Invoice.warehouse_id` → الترحيل → `InventoryMovement.warehouse_id`
→ COGS منفصل تماماً لكل مستودع (10,000 لـA، 90,000 لـB، بلا تداخل) →
رفض كامل للترحيل بلا اختيار مستودع.

اكتشاف عابر أثناء كتابة الاختبار (خطأ اختباري بحت، لا بالمنتج): نسيت
تعبئة حقل العميل/المورد أول مرة (رسالة خطأ حقيقية من `invoice_validation.py`
ضبطت الأمر فوراً) واستخدمت `journal_entry_id` بدل `entry_id` الصحيح
باسم عمود `JournalLine` — كلاهما صُحِّح مباشرة.

### 47.3 نتيجة البوابة الكاملة
`tests/run_gate.py` — **16 ملف Regression** (أُضيف `test_qt_editor_lifecycle.py`
القديم للتأكد من عدم كسره + `test_ui_warehouse_integration.py` الجديد)
+ 200 fuzz → **PASS ✅**. لم يُعدَّل أي منطق محاسبي بهذه الخطوة إطلاقاً —
فقط ربط UI بالمحرك الموجود من §46.

### الخطوة التالية
مراجعة شاشة فاتورة البيع بندًا بندًا (كما اقترح صديق المبرمج) قبل بناء
بقية الشاشات، أو تنفيذ بقية فجوات §45 (Esc، الرصيد المستحق/التسويات،
Cancel/Reverse بالواجهة) — بانتظار قراركم على الترتيب.

## §48 — مراجعة شاملة قبل التوسع بالواجهات (تقرير الإغلاق)

مراجعة مركزة كاملة عبر 9 محاور (تثبيت القرارات، العملات، المستودعات،
Settlement/FX، افتراضات العملة الثابتة، التقارير، Cancel/Return،
التوثيق، بلا Abstractions جديدة). التقرير الكامل حسب الفئات A/B/C/D في
`REVIEW_REPORT.md` (أُعيد كتابته بالكامل — النسخة السابقة كانت منحرفة
عن الواقع، راجع §48.5).

### 48.1 القواعد التي تم التحقق منها فعلياً بهذه الجلسة
- نقل مخزون **بالاتجاهين** (A→B وB→A) — لم يُختبَر سابقاً إلا اتجاه واحد.
- تسلسل قبض/دفع متعدد (3+3 دفعات) بأسعار صرف مختلفة، بـOracle تراكمي
  مستقل لصافي فرق الصرف عبر 6 تسويات معًا.
- تطابق ميزان المراجعة مع محرك المخزون (warehouse-aware) بعد سلسلة
  عمليات مركّبة (شراء بعملتين + نقل + تسويات).
- عدم وجود أي مسار يعيد الخلط بين المستودعات (فحص شامل، لا افتراض).

### 48.2 الاختبارات الجديدة المُضافة
`tests/test_comprehensive_review.py` — 8 تحققات (نقل B→A، تسلسل تسويات
بـOracle تراكمي، تطابق تقارير/محرك مخزون).

### 48.3 هل اكتُشفت أخطاء حقيقية؟
**لا خطأ محاسبي جديد** بمنطق الترحيل نفسه — كل الأخطاء المكتشفة هذه
الجلسة كانت إما توثيقية (انحراف REVIEW_REPORT.md، §48.5) أو معمارية
منخفضة الخطورة (غياب `base_currency` داخل قاعدة الشركة، §48.4). لا حاجة
لإثبات "فشل قبل الإصلاح" لأنه لا يوجد إصلاح محاسبي بهذه الدفعة.

### 48.4 اكتشاف معماري (لا محاسبي) — راجع REVIEW_REPORT.md للتفصيل الكامل
`base_currency` غير موجود داخل قاعدة الشركة نفسها (فقط بـ`registry.db`
المنفصلة). الأثر الفعلي محدود حاليًا (تسمية `Account.currency_code`
الافتراضية فقط، غير مؤثر على أي حساب فعلي) — **لم يُصلَح**، موثَّق
كقرار مستقبلي معلَّق.

### 48.5 تصحيح انحراف توثيقي حقيقي (Documentation Drift)
`REVIEW_REPORT.md` كان يصف Settlement/Cancel/Warehouse كفئة C (غير
منفَّذة) رغم تنفيذها الكامل لاحقًا (§42-47) — انحراف حقيقي بين التوثيق
والكود. **أُعيد كتابة الملف بالكامل** ليعكس الحالة الفعلية. `ENGINE_MAP.md`
كان محدَّثًا فعلاً بلا انحراف (تحقّق مباشر، لا افتراض).

### 48.6 أمور لم تُنفَّذ بعد (فئة C محدَّثة)
- حذف `runner.py` نهائيًا (مؤجَّل بقرار، §34).
- `base_currency` داخل قاعدة الشركة (§48.4، جديد).

### 48.7 أمور يمكن تأجيلها بأمان (فئة B/Technical Debt)
- IAS 21 إعادة التقييم الدوري (D-5، فرق واضح عن الفرق المحقَّق المُنفَّذ).
- عرض العملات التفصيلي بكشف الحساب (D-2).
- `journal_voucher_form.py` تسمية SYP بعرض إجمالي القيد اليدوي (عرضي بحت).
- اختبار صريح لمحاولة تلاعب مباشرة بـSettlement (الحماية الحالية:
  غياب أي دالة خدمة للتعديل، لكن غير مُختبَرة بحالة صريحة).

### 48.8 قرارات محاسبية لا تزال معلَّقة (D محدَّثة: 3 فقط)
1. فرض `Account.currency_code` برمجيًا أم يبقى وصفيًا؟
2. عرض تفصيلي لكل عملة بكشف الحساب؟
3. IAS 21 إعادة التقييم الدوري — الآن أم لاحقًا؟

### 48.9 نتيجة البوابة الكاملة
`tests/run_gate.py` — **17 ملف Regression** + 200 fuzz → **PASS ✅**.

### القرار
لا نقطة تمنع الاستمرار بواجهات الفواتير. القرارات العشرة المطلوبة
(§48 كاملة) مُغلَقة. بانتظار توجيه صديق المبرمج للمرحلة التالية —
مراجعة شاشة فاتورة البيع بندًا بندًا، أو إكمال فجوات §45 المتبقية
(Esc، الرصيد المستحق/التسويات، Cancel بالواجهة).

## §49 — مراجعة مناعة Settlement ضد التلاعب (قبل اعتباره مستقرًا)

### 49.1 القاعدة الرسمية (موثَّقة قبل الاختبار)

**`Settlement` غير قابل للتعديل أو الحذف عبر أي مسار خدمة رسمي — بالتصميم،
لا بفحص إضافي.** `app/services/settlements.py` **لا يُصدِّر** أي دالة
`update_settlement()` أو `delete_settlement()` إطلاقاً — المسار الوحيد
لإنشاء تسوية هو `post_receipt()`/`post_payment()`، وكلاهما يُنشئ سجلاً
جديداً فقط، لا يُعدِّل موجوداً. هذا نفس مبدأ حماية `JournalEntry`/`Invoice`
المرحّلة (لا `ensure_editable()` منفصلة هنا لأن ببساطة **لا يوجد فعل
تعديل معروض أصلاً** ليُحرَس).

هذا لا يمنع التلاعب المباشر عبر ORM خام (`session.query(Settlement)...`)
— لا شيء بهذا المشروع يمنع ذلك لأي نموذج، بما فيها `JournalLine` نفسها؛
هذا خارج نطاق طبقة الخدمة بالتصميم (يُفترض أن كل تعديل يمر عبر
`services/`، لا عبر ORM مباشرة من كود تطبيقي حقيقي).

### 49.2 نطاق الاختبار (7 محاولات)
لا تعديل، لا حذف (بالتأكد أن الدالتين غير موجودتين أصلاً)، تجاوز
الرصيد، تسوية فاتورة DRAFT (لم تُختبَر صراحةً من قبل — كل الاختبارات
السابقة رحّلت الفاتورة أولاً)، تسوية فاتورة CANCELLED (مُختبَرة سابقاً،
تأكيد إضافي)، تسوية بفاتورة من النوع الخطأ (مُختبَرة سابقاً)، وتأكيد
عدم تأثر فاتورة/عميل آخر بتسوية غير متعلقة به.

### 49.3 نتيجة الاختبار الفعلي
`tests/test_settlement_tamper_resistance.py` — **10/10 تحقّق نجح من أول
تشغيل، بلا أي خلل حقيقي مكتشَف**، بما فيها الحالة الوحيدة غير المُختبَرة
صراحةً من قبل (تسوية فاتورة DRAFT لم تُرحَّل بعد — كل الاختبارات
السابقة كانت ترحّل الفاتورة أولاً بدون استثناء، فهذه أول مرة تُختبَر
هذه الحالة تحديداً بمعزل). لا حاجة لإثبات فشل-قبل-إصلاح لأنه لا يوجد
إصلاح — الكود الحالي كان صحيحاً أصلاً لكل الحالات السبع.

**ملاحظة موثَّقة (لا خلل)**: "تكرار نفس مبلغ التسوية مرتين" (نقطة
ضمنية بطلب صديق المبرمج) لا يُمنَع بنيوياً إن كان المبلغان معاً ضمن
الرصيد المستحق — لأن دفعتين حقيقيتين بنفس القيمة سيناريو تجاري مشروع
تماماً (لا يمكن للمحرك تمييزه عن نقر مزدوج بالخطأ بدون معرّف idempotency
من الواجهة). هذا مسؤولية طبقة UI (تعطيل زر الإرسال بعد النقر)، لا خللاً
بمحرك التسوية — موثَّق كـTechnical Debt لواجهة القبض/الدفع المستقبلية.

## §50 — تقرير الإغلاق النهائي (قبل الانتقال لبقية واجهات الفواتير)

### ما أصبح مكتملاً ومختبَراً
محرك الترحيل الكامل (شراء/بيع/مرتجعات) بعملات متعددة ومستودعات منفصلة
التكلفة، Settlement/Receipt/Payment/FX بمناعة كاملة ضد 7 محاولات تلاعب،
Cancel/Reverse منفصل عن Return، Alembic بمسار التطبيق الحقيقي، ComboBox
المستودع بالواجهة (4 شاشات) بلا سقوط صامت. **18 ملف Regression + 200
سيناريو fuzz، PASS فعلياً وليس افتراضاً.**

### ما بقي مؤجَّلاً (بقرار موثَّق، لا نسيان)
IAS 21 (D-5)، عرض العملات التفصيلي بكشف الحساب (D-2)، حذف `runner.py`
نهائياً (§34)، اختبار "نقر مزدوج/idempotency" لواجهة القبض/الدفع
المستقبلية (§49.3).

### Technical Debt الحالي (موثَّق، غير مُصلَح، غير عاجل)
**العملة الأساسية للشركة بلا مصدر حقيقة رسمي داخل قاعدة الشركة نفسها**
— موجودة فقط بـ`registry.db` المنفصلة (`CompanyRecord.base_currency`).
أي قيمة SYP حالية بالكود (نماذج، إعدادات، عرض) هي **Default تاريخي/Technical
Debt، لا تعريفاً للعملة الأساسية**. لم يُنفَّذ أي تصميم لمصدر حقيقة
موحَّد (`Company → Base Currency` داخل قاعدة الشركة) — يحتاج قراركم
للتصميم النهائي قبل أي تنفيذ.

### القرارات المحاسبية التي ما زالت تحتاج قراراً (3، بلا تغيير عن §48)
1. فرض `Account.currency_code` برمجياً أم يبقى وصفياً؟
2. عرض تفصيلي لكل عملة بكشف الحساب؟
3. IAS 21 — الآن أم لاحقاً؟

### الخلاصة
لا خلل جوهري جديد اكتُشف بهذه المراجعة. محرك المحاسبة والمخزون **مستقر
بهذه المرحلة**. جاهزون للانتقال لبقية واجهات الفواتير والمرتجعات، مع
الحفاظ على القرارات المعمارية الحالية بلا Repository/UoW/DDD أو طبقات
إضافية.

## §52 — قرار موثَّق: invariant حالة الفاتورة داخل get_invoice_balance_due()

**السؤال المطروح (Bilal)**: هل غياب فحص `status` داخل
`get_invoice_balance_due()` تصميم مقصود (دالة استعلام حسابية محايدة)،
أم يجب أن يكون invariant على مستوى الخدمة؟

**القرار: invariant على مستوى الخدمة — نُقل الفحص إليها فعلياً.**

**السبب**: لا صيغة "استعلام محايد" منطقية هنا فعلياً. "الرصيد المستحق"
مفهومه محاسبياً غير موجود قبل الترحيل — لا قيد وُلِد، فلا ذمة (مدينة أو
دائنة) تحمل هذا الرصيد أصلاً؛ رقم `totals.grand_total` لفاتورة DRAFT
صحيح حسابياً كـ"إجمالي مسودة" لكنه ليس "رصيداً مستحقاً" بأي معنى
محاسبي. الدليل الحاسم: `_invoice_receivable_or_payable_account_id`
بنفس الملف (السطر الذي يسبق هذه الدالة مباشرة) **يفرض بالفعل** هذا
الضبط بالضبط (`invoice.journal_entry_id is None → SettlementError`) —
غياب الفحص المطابق بـ`get_invoice_balance_due()` كان فجوة اتساق حقيقية
بين دالتين بنفس الملف تخدمان نفس المفهوم، لا قراراً مقصوداً، واكتُشفت
بالاختبار فقط لأنها لم تُختبَر صراحة من قبل على فاتورة DRAFT.

**التنفيذ**: `get_invoice_balance_due()` ترفض `SettlementError` الآن
لأي فاتورة `status != POSTED` قبل أي حساب. كل مستدعٍ (الواجهة
`SettlementDialog`/`document_form.py`، أو أي كود مستقبلي) يحصل على
هذا الضمان تلقائياً من نقطة واحدة، بدل الاعتماد على كل مستدعٍ ليتحقق
بنفسه (وهو بالضبط ما حدث بالخطأ في `SettlementDialog` بالجولة
السابقة). لا تعديل على `_post_settlement()` نفسها — كانت أصلاً تتحقق
من الحالة قبل استدعاء هذه الدالة، فالتغيير هنا صرف اتساق، لا تعديلاً
سلوكياً بمسار الترحيل.

**الاختبار**: `tests/test_settlement_fx.py`،
`tests/test_settlement_tamper_resistance.py`، `tests/test_comprehensive_review.py`
كلها تستدعي هذه الدالة فقط على فواتير POSTED فعلياً — راجعتها سطراً
بسطر قبل التعديل للتأكد من عدم وجود أي استدعاء قائم على فاتورة DRAFT
كان يعتمد (عن قصد أو سهواً) على السلوك القديم. لا أحد كذلك. gate()
الكامل (19/19 + 200 fuzz) PASS بعد التعديل بلا أي كسر.

## §54 — قرار موثَّق: ترقيم الفواتير (invoice_no) غير مُنفَّذ فعلياً

**اكتُشف أثناء بناء اختبار المرتجع المرتبط (§ التالي)**: حقل `invoice_no`
يبقى دائماً بصيغته الداخلية المؤقتة (`DRAFT-{id(self)}`) حتى بعد
الترحيل — **لا يوجد أي كود يُولِّده فعلياً عند الترحيل**، رغم أن الحقل
بالواجهة يعرض نص Placeholder صريح "يُولَّد عند الترحيل"، ورغم وجود
معامل `ref_prefix` (`JE-SAL`/`JE-PUR`/`JE-SRET`/`JE-PRET`) يُمرَّر لكل
شاشة من الشاشات الأربع بالضبط لهذا الغرض — **لكنه غير مُستخدَم بأي
مكان بالكود إطلاقاً**، فحص مباشر أكّد ذلك (`grep` لم يجد أي استخدام
غير التعريف والتمرير).

**الأثر العملي**: أي فاتورة حقيقية بالتطبيق تحمل رقماً داخلياً غير
تسلسلي وغير قابل للقراءة (`DRAFT-139694177183744` مثلاً) بدل رقم
فاتورة فعلي — يظهر هكذا بالطباعة والتقارير وكشوف الحساب أيضاً.

**لم أُنفِّذ حلاً من عندي** — صيغة الترقيم قرار عمل (تسلسلي بسيط؟ لكل
نوع مستند مرة أخرى؟ بادئة سنة/شهر؟ إعادة تصفير سنوياً؟) يحتاج قرار
Bilal، لا افتراضاً تقنياً. الاختبارات الجديدة (§ التالية) استخدمت
`invoice.invoice_no` الفعلي (الصيغة الحالية القبيحة لكن الصحيحة) بدل
افتراض رقم، لتبقى صحيحة بصرف النظر عن القرار النهائي.

## §55 — فصل واضح: ما أُثبِت بالاختبار الفعلي، وما لم يُختبَر بعد (طلب Bilal، بند 9)

### أُثبِت فعلياً (اختبار PySide6 حقيقي، لا Mock، gate PASS 20/20 + 200 fuzz)
- دورة فاتورة بيع كاملة: إنشاء (مستودع/عميل/عملة/سعر صرف/مادة/كمية/سعر/خصم/ضريبة)
  → DRAFT → إعادة فتح → تعديل → POSTED → قفل الحقول (`tests/test_ui_full_sales_lifecycle.py`)
- Settlement: رفض DRAFT (بقرار §52)، قبض جزئي×2، قبض كامل، الرصيد يصل
  صفراً بالضبط بتحقق Oracle مستقل + ميزان مراجعة فعلي متوازن
- Cancel: نجاح بلا تسوية (قيد عكسي فعلي + عكس حركة مخزون)، رفض فعلي من
  المحرك مع تسوية مرتبطة، عزل كامل عن مستندات أخرى
- عزل المستودعات: COGS منفصل تماماً (A=10,000 وB=90,000)، شراء إضافي
  بمستودع A لا يمسّ متوسط B، مرتجع مرتبط يرث ويُقفَل على نفس المستودع
  (`tests/test_ui_warehouse_integration.py` المُوسَّع)
- Esc يعمل فعلياً (معالجة صريحة بالـdelegate، لا اعتماداً على كيوت)

### لم يُختبَر بعد (بصراحة، لا ادعاء اكتمال)
- دورة كاملة لفاتورة شراء (بُنيت فقط أجزاء منها كتمهيد لاختبارات أخرى،
  لا كسيناريو مستقل مثل فاتورة البيع)
- مرتجع شراء بدورة كاملة مماثلة (رُوجعت البنية المشتركة فقط، لا سيناريو
  مخصص)
- سلوك مستخدم يومي إضافي غير مطلوب صراحة: حذف بند من فاتورة DRAFT بعد
  إضافته، الطباعة الفعلية (زر "طباعة" موجود بالواجهة لكن غير مُختبَر
  بهذه الجولة ولا في أي جولة سابقة)
- تعدد المستخدمين/التزامن الفعلي على نفس قاعدة العميل (خارج نطاق أي
  اختبار حتى الآن بالمشروع كله، ليس فقط هذه الجولة)

### اكتُشف وأُصلِح فعلياً بهذه الجولة (لا افتراضياً)
1. §52: `get_invoice_balance_due()` لا تتحقق من `status` — أصبحت
   invariant بمستوى الخدمة.
2. §53: طريقة الدفع (`is_cash`) لم تكن مخزَّنة إطلاقاً — عمود جديد +
   هجرة Alembic (`f3a1c9b2e4d0`) + تزامن كامل بـ`document_form.py`.
3. ثغرة اختبار (لا محرك): `test_alembic_integration.py` كان يستخدم
   نموذج ORM الحالي لإدراج بيانات بقاعدة قديمة مجمَّدة — يفشل تلقائياً
   مع أي عمود جديد يُضاف للنموذج مستقبلاً (وليس فقط `is_cash`). أُصلِح
   بإدراج SQL خام مطابق للـschema المجمَّدة، محصَّن ضد أي تعديل نموذج
   لاحق.
4. §54: `invoice_no` غير مُولَّد فعلياً عند الترحيل (موثَّق أعلاه، بلا
   حل مقترح من عندي).

## §56 — Phase 2 مكتملة: تصنيف عمل صريح للحساب + allow_reconciliation

قرار Bilal الصريح نُفِّذ حرفياً: `account_type` (الطبيعة المحاسبية:
أصل/خصم/...) لا يقرر شيئاً عن السلوك التجاري — أُضيف `AccountSubtype`
(عام/عميل/مورد/صندوق/بنك/مصروف/إيراد/أخرى) كحقل منفصل تماماً، ومعه
`allow_reconciliation` كقاعدة عمل مستقلة تماماً عن كليهما.

**الأهم — القاعدة مفروضة بالخدمة فعلياً، لا الواجهة فقط**:
`_invoice_receivable_or_payable_account_id()` بـ`settlements.py` ترفض
`SettlementError` الآن لأي حساب `allow_reconciliation=False`، بصرف
النظر عن نوعه أو رقمه. مُثبَت بـ`test_allow_reconciliation_enforcement.py`:
تعطيل الصلاحية يدوياً على حساب عميل حقيقي له تسويات سابقة يرفض أي
تسوية جديدة فوراً، وإعادة التفعيل تسمح فوراً بلا أي إعادة ترحيل —
القاعدة ديناميكية بالكامل، لا قفل نهائي.

`get_or_create_party_account()` تُصنِّف كل حساب طرف جديد تلقائياً
(CUSTOMER/SUPPLIER + allow_reconciliation=True) — هذا مصدر الحقيقة
الوحيد، لا استنتاجه لاحقاً بأي مكان آخر بالكود.

**بطاقة الحساب** (`account_card_dialog.py`) تعرض الحقلين صراحة الآن:
ComboBox "نوع الحساب الفرعي" + Checkbox مستقل "يسمح بتسوية الفواتير" —
لا تفعيل تلقائي لأحدهما بناءً على الآخر.

**هجرة Alembic** (`a7c4f8d1b3e2`) مع Backfill حقيقي (لا عمود فارغ):
تُصنِّف تلقائياً الحسابات الافتراضية المعروفة عبر Settings الموجودة
فعلياً (default_cash_account_id → CASH، كل حساب تحت ar_parent/ap_parent
→ CUSTOMER/SUPPLIER + allow_reconciliation=True)، مُختبَرة على بيانات
عميل واقعية فعلياً (لا قاعدة فارغة فقط).

**ثغرة اختبار جوهرية اكتُشفت وأُصلِحت بنطاق أوسع أثناء هذا العمل**:
النمط الذي كسر `test_alembic_integration.py` مرة بـ§53 (استخدام ORM
الحيّة لمحاكاة "عميل قديم") تكرر بـ3 ملفات إضافية عند إضافة عمودي
Account هذين (`test_alembic_integration.py` مرة أخرى بموضع مختلف،
`test_migration_safety.py`، `test_migration_double_run_safety.py`) —
نمط متكرر فعلياً، لا حالة معزولة. أُصلِح جذرياً بتوسيع
`tests/_legacy_schema_helper.py` بدالتين عامتين
(`seed_legacy_account`, `seed_legacy_chart_of_accounts`) بـSQL خام
محصَّن ضد أي عمود يُضاف لنموذج Account مستقبلاً — كل الاختبارات
الأربعة تستخدمهما الآن بدل استدعاء الـORM مباشرة. اكتُشف بالمناسبة خطأ
حقيقي بصيغة تخزين الـEnum (SQLAlchemy تخزن اسم القيمة بأحرف كبيرة
"ASSET" لا "asset") أثناء بناء هذا الإصلاح — صُحِّح بكل الدوال
والهجرة معاً.

gate() PASS — 22/22 regression + 200/200 fuzz.

## §57 — Phase 2 مُغلَقة رسمياً: 4 مجموعات اختبار + تصحيح معماري حقيقي

**تصحيح جوهري قبل الإغلاق**: الفحص السابق (§56) كان يتحقق من
`allow_reconciliation` فقط، متجاهلاً `subtype` تماماً — لا يطابق تصميم
Bilal الأصلي ("Customer/Supplier **و** allow_reconciliation=True معاً").
اكتُشف مباشرة أثناء كتابة بند اختباره الثامن ("تغيير subtype من
Customer إلى General يجب أن ينعكس على صلاحية التسوية") — لو تُرِك
الفحص القديم، هذا البند كان سيفشل فعلياً لأن subtype وحده لم يكن يؤثر
على القرار إطلاقاً. صُحِّح فوراً: `_invoice_receivable_or_payable_account_id()`
تتحقق الآن من **الشرطين معاً** (`subtype in (CUSTOMER, SUPPLIER)` ثم
`allow_reconciliation`)، لا أحدهما فقط.

**4 مجموعات اختبار جديدة (52 تحقّقاً إجمالياً)**:
1. `test_account_reconciliation_rules.py` (13 تحقّقاً): كل تركيبة
   subtype مطلوبة (Customer/Supplier/Expense/Income/Cash/Bank/General/
   Group)، Customer+allow_reconciliation=False يُرفَض حتى بتجاوز
   الواجهة (استدعاء الخدمة مباشرة)، تغيير subtype ديناميكياً ينعكس
   فوراً (بالاتجاهين)، عميل بكود/بادئة غير تقليدية إطلاقاً (`ZZZ-999-CUSTOM`)
   يعمل بنفس القدر — لا اعتماد على رقم الحساب مؤكَّد تجريبياً لا نظرياً فقط.
2. `test_phase2_migration_data_integrity.py` (13 تحقّقاً): Backfill
   صحيح على بيانات عميل واقعية عبر مسار التطبيق الفعلي
   (`ensure_schema_up_to_date`، لا Alembic CLI مباشرة — الفرق مهم:
   عميل قديم بلا `alembic_version` يحتاج stamp-then-upgrade، لا upgrade
   عادي)، حسابات المجموعات/غير المصنَّفة تبقى GENERAL بلا تخمين،
   تشغيل الهجرة مرتين لا يغيّر شيئاً (idempotent فعلياً لا نظرياً)،
   حساب جديد يُحفَظ ويُستعاد **من القرص عبر اتصال جديد تماماً** (لا
   نفس كائن Python بالذاكرة).
3. `test_invoice_cycle_customer_supplier.py` (20 تحقّقاً): دورة
   Invoice→POSTED→Settlement→Balance كاملة لعميل ومورد معاً (لا عميل
   فقط)، تسوية جزئية×2 ثم كاملة، رفض DRAFT، رفض تجاوز الرصيد (والرصيد
   لا يتأثر بمحاولة مرفوضة)، رفض حساب فقد allow_reconciliation لاحقاً.
4. `test_currency_lifecycle_final.py` مُوسَّع: أُضيف تحقق ميزان مراجعة
   فعلي متوازن (`get_trial_balance().is_balanced`) — يُثبِت أن كل قيم
   العملة/سعر الصرف/base debit-credit/الرصيد المستحق/realized FX التي
   أثبتها Group 1 سابقاً لم تتغيّر إطلاقاً بعد إضافة subtype/
   allow_reconciliation (نفس السيناريو، 46/46 تحقّقاً، بلا أي تعديل
   على القيم المتوقعة).

gate() PASS — **26/26 regression + 200/200 fuzz**. Phase 2 مُغلَقة.
لا الانتقال لـPhase 3 قبل: (أ) حسم `base_currency` (سؤال معلَّق منذ
§ السابقة، لم يُجَب بعد)، (ب) اعتماد Bilal لمواصفة Voucher + Opening
Balances التنفيذية المختصرة قبل أي كود — لم تُكتَب بعد، هذه الجولة
اقتصرت على إغلاق Phase 2 فقط كما طُلِب صراحة.

## §58 — Phase 3A (جزء 1، مُنفَّذ): حسم base_currency

قرار Bilal: `registry.db` يبقى مصدر الحقيقة الوحيد القابل للتعديل — لا
نسخة ثانية مستقلة تُعدَّل يدوياً. الحل: `open_company_db()` (`app/db.py`)
تُزامِن `CompanyRecord.base_currency` إلى `Settings['base_currency']`
بقاعدة الشركة في **كل** فتح (لا مرة واحدة عند الإنشاء فقط) — قيمة
للقراءة فقط بمنظور قاعدة الشركة، تُستبدَل تلقائياً بقيمة `registry.db`
الحقيقية حتى لو عُدِّلت يدوياً من الداخل (مُثبَت تجريبياً، لا افتراضاً).

`get_base_currency(session)` (`app/services/posting.py`) هي المدخل
الوحيد المعتمد لأي كود محاسبي — ترفع `PostingError` واضحة لو غابت
(قاعدة "يتيمة" بلا سجل registry)، لا تفترض SYP أبداً.

**ثغرة حقيقية أُصلِحت أثناء هذا العمل**: `journal_voucher_form.py` كان
فيه `base_currency = "SYP"` مكتوبة حرفياً — بالضبط النمط الذي حذّر منه
Bilal (`if currency == "SYP"`). أُصلِحت لاستخدام `get_base_currency()`.

مُختبَر بـ`tests/test_base_currency_source_of_truth.py` (7 تحقّقات):
رفض إنشاء شركة بلا عملة صريحة، تخزين موحَّد بأحرف كبيرة، مزامنة فعلية
عند الفتح، تغيير لاحق بالـregistry ينعكس تلقائياً عند إعادة الفتح،
قاعدة يتيمة تُرفَض بوضوح، تلاعب يدوي مباشر بالـcache يُستبدَل تلقائياً.

gate() PASS — **27/27 regression + 200/200 fuzz**.

## §59 — Phase 3A (جزء 2): مواصفة تصميم Phase 3 الكاملة — بانتظار الاعتماد

`PHASE3_DESIGN_SPEC.md` (ملف جديد بجذر المشروع) يغطي الدورة الكاملة
Opening Balances→Chart of Accounts→Customers/Suppliers→Inventory→
Vouchers→Settlement→FIFO/Manual→Multi-Currency→FX→Reports التي طلبها
Bilal، بإجابات مقترحة لكل نقاطه العشر. **لا كود تنفيذي لأي منها بعد**
— أربعة أسئلة صريحة بنهاية المستند تحتاج قرار Bilal قبل Phase 3B، أهمها
آلية تحويل العملة عبر العملة الأساسية كجسر وحيد بين عملة الفاتورة
وعملة السند (§4 بالمستند) — القرار الذي يُبنى عليه كل حساب FX لاحقاً.

## §60 — Phase 3A مُعتمَدة بالكامل؛ Phase 3B جاهزة للبدء (لا كود بعد)

Bilal اعتمد Phase 3A رسمياً (registry.db + مزامنة + إزالة SYP الصلبة،
27/27 + 200 fuzz). أوضح أن "read-only mirror" يعني إنفاذ منطقي بمستوى
التطبيق (`open_company_db` تستبدل القيمة عند كل فتح) لا قيداً على
مستوى SQLite نفسها — مؤكَّد أن هذا يكفي، لا حاجة لـtriggers أو صلاحيات
قاعدة بيانات إضافية.

أربعة قرارات معمارية إضافية حُسِمت وأُضيفت لـ`PHASE3_DESIGN_SPEC.md`
(لا كود، توثيق تنفيذي فقط — طلب Bilal الصريح: "لا يبدأ المبرمج
بالكود الآن... يحوّل القرارات إلى قواعد تنفيذية واختبارات قبول"):

1. **جسر العملة (§4 بالمستند)**: العملة الأساسية فقط، بخطوتين منفصلتين
   دائماً (عملة الفاتورة→أساسية، عملة السند→أساسية) — لا Cross Rate
   مصطنع أبداً. حالتا قبول إلزاميتان أُضيفتا بالأرقام الدقيقة، أهمهما:
   Invoice=EUR + Voucher=GBP يجب أن ينتج fx_amount صحيحاً بلا أي علاقة
   مباشرة EUR↔GBP بالنظام.
2. **الفاتورة الافتتاحية (§7)**: `Invoice` عادية + `is_opening_balance`،
   لكن بـ6 قيود صارمة مفروضة معاً (لا الحقل وحده) — أهمها: مسار ترحيل
   منفصل تماماً `post_opening_invoice()` لا يستدعي
   `post_sales_invoice`/`post_purchase_invoice` إطلاقاً (فلا إيراد/COGS
   مكرَّر)، بينما تبقى قابلة للتسوية بلا أي تعديل على
   `get_invoice_balance_due`/FIFO لأنها `Invoice` عادية بمنظور تلك
   الدوال بالكامل.
3. **حساب التوازن**: `Settings['opening_balance_clearing_account_id']`
   — إعداد شركة إلزامي (نفس فلسفة `base_currency` بالضبط)، نوعه
   EQUITY حصراً، لا اختراع اسم/حساب تلقائي. نفس الحساب يُستخدَم لأرصدة
   المخزون الافتتاحية أيضاً (لا حساب ثالث منفصل).
4. **Idempotency (§9 بالمستند)**: رفض صريح لإعادة الترحيل (لا upsert
   صامت) عبر `Setting` لكل نطاق (`opening_balances_accounts_posted_at`
   وما يقابلها) — التصحيح يمر عبر `cancel_invoice`/عكس القيد الموجودَين
   فعلاً، لا آلية جديدة.

**ترتيب Phase 3B المُعتمَد**: حسابات (3B-1) → مخزون (3B-2) →
عملاء/موردون (3B-3) → محرك السندات (3B-4) → تخصيص FIFO/يدوي (3B-5) →
واجهة (3B-6) → E2E كامل (3B-7). كل خطوة تُبنى وتُختبَر بمعزل قبل
التالية.

**الخطوة التالية المُعتمَدة**: تنفيذ 3B-1 فقط (Opening Account
Balances) — لا الثلاث خطوات الأولى دفعة واحدة.

gate() PASS — 27/27 regression + 200/200 fuzz (بلا تغيير — هذه الجولة
توثيق فقط، لا كود).

## §61 — Phase 3B-1 مُنفَّذة ومُختبَرة: الأرصدة الافتتاحية للحسابات

نطاق صارم كما اعتمده Bilal حرفياً: **حسابات عامة فقط** —
`app/services/opening_balances.py::post_opening_account_balances()`.
لا مخزون، لا فواتير عملاء/موردين، لا سندات — تلك 3B-2/3B-3/3B-4.

**لا آلية ترحيل جديدة**: يُعاد استخدام `add_manual_line()` و
`post_manual_entry()` الموجودتين فعلياً بـ`journal_edit.py` حرفياً —
القيد الافتتاحي قيد يدوي عادي بمنظور المحرك، فقط بقواعد تحقق إضافية
قبل بنائه. العكس/التصحيح يُعاد استخدام `reverse_manual_entry()`
الموجودة فعلياً أيضاً — لا آلية عكس جديدة، فقط مسح
`Setting[opening_balances_accounts_posted_at]` بعدها للسماح بإعادة
الإدخال (§9 بالمواصفة).

**كل القواعد التسع من طلب Bilal مُنفَّذة**: حساب Clearing إلزامي من
إعدادات الشركة (`Settings['opening_balance_clearing_account_id']`) —
غير موجود/Group/Inactive/ليس EQUITY كلها تُرفَض؛ حسابات
Revenue/Expense تُرفَض من الرصيد الافتتاحي نفسه (لا تشويه ربحية)؛
Idempotency برفض صريح لا upsert صامت؛ القيد المرحَّل غير قابل للتعديل
(نفس قاعدة `ensure_editable` الموجودة)؛ يدعم عدة عملات لكل سطر
(`line_currency_code`/`line_exchange_rate`، آلية موجودة فعلاً بسند
القيد اليدوي، لا جديدة).

**بق حقيقي اكتُشف وأُصلِح أثناء البناء** (لا افتراضياً): الوصول المبكر
لـ`entry.lines` (relationship) قبل إضافة سطر التوازن التلقائي يُخزِّن
مجموعة قديمة (cache) بذاكرة SQLAlchemy لا تلتقط السطر المُضاف لاحقاً
عبر `add_manual_line()` (تُدرِج عبر `entry_id` مباشرة، لا عبر
`entry.lines.append()`) — فيفشل `is_balanced()` داخل
`post_manual_entry()` لاحقاً بفارق كامل قيمة أول سطر، رغم أن كلا
السطرين موجودان فعلياً بقاعدة البيانات. أُصلِح باستعلام `JournalLine`
مباشرة بدل `entry.lines` لحساب فرق التوازن، مع `session.expire(entry,
["lines"])` قبل `post_manual_entry()` لضمان قراءة نظيفة كاملة.

**جدول جديد `opening_balance_entries`** (هجرة `b8e2d5f7a1c9`): سجل
تدقيق فقط يحفظ المُدخَل الأصلي كما أدخله المستخدم — لا يستبدل
JournalEntry/JournalLine كمصدر الحقيقة المحاسبية، ولا يُقرَأ منه أي
حساب فعلي.

**Acceptance Gate كامل** (`tests/test_opening_account_balances.py`،
34 تحقّقاً) يغطي كل الحالات العشر التي طلبها Bilal حرفياً: Base=USD
بعملة الشركة وبعملة أجنبية، مدين ودائن معاً بعملية واحدة لعدة حسابات،
رفض سطر غير صالح (مدين+دائن معاً / صفر بكليهما)، رفض حساب غير
موجود/Group/Inactive/Revenue/Expense، رفض clearing account بكل صوره
غير الصالحة، Idempotency (رفض + تأكيد عدم تضاعف الرصيد)، عكس لا يحذف
التاريخ ويسمح بإعادة الإدخال، إغلاق وإعادة فتح فعلي (اتصال جديد تماماً
لا نفس الجلسة) يحافظ على النتيجة، ميزان مراجعة متوازن بكل خطوة.

gate() PASS — **28/28 regression + 200/200 fuzz**.

**التالي**: مراجعة Bilal للكود والاختبارات قبل الانتقال لـ3B-2
(Opening Inventory) — لا انتقال تلقائي، كما اشترط صراحة.

## §62 — Phase 3B-1 مُغلَقة رسمياً (CLOSED) بعد مراجعة كود فعلية

Bilal راجع الكود الفعلي (لا الملخص) لأربعة ملفات: `opening_balances.py`،
النموذج، الهجرة، اختبارات القبول. لم يطلب إعادة بناء — فقط تصحيحات
توثيقية ثلاث، لا منطقية:

1. **مصطلح "Audit"**: `OpeningBalanceEntry` كان موصوفاً بالتعليقات
   كـ"سجل تدقيق" — تسمية مضلِّلة قليلاً لأنه لا يحمل
   `created_by`/`created_at`/معرّف عملية (Audit Trail كامل). صُحِّح في
   `models.py` والهجرة والخدمة والاختبارات إلى "سجل تفصيلي" (Opening
   Balance Detail Record) — البنية والغرض صحيحان كما هما، فقط الاسم.
   لا حقول جديدة أُضيفت (لم يُطلَب ذلك الآن).
2. **دلالة النجاح ليست commit فعلياً**: وُثِّق صراحة بـdocstring
   `post_opening_account_balances()`: نجاح الدالة يعني جاهزية ضمن
   transaction المُستدعي فقط، لا تثبيتاً نهائياً على القرص — نفس قاعدة
   بقية خدمات المشروع (لا service يستدعي `commit()`/`rollback()`
   بنفسه)، مُختبَرة صراحة (بند 11، الجولة السابقة).
3. **اتجاه `exchange_rate`**: وُثِّق صراحة أنه دائماً "وحدات العملة
   الأساسية مقابل وحدة واحدة من عملة السطر" (`base = amount × rate`،
   لا معكوساً) — مُختبَر فعلياً بقيمة ≠ 1 (EUR 1,000 @ 1.1 → 1,100
   USD بالضبط، الاختبار §2 موجود منذ الجولة السابقة، لم يكن ينقص شيئاً).

**الاختباران المطلوبان للإغلاق كانا موجودين فعلاً ضمن الـ43**: عملة
أجنبية بسعر ≠ 1 (§2)، وReverse→Corrected Repost الكامل (§9: 5,000 →
POST → REVERSE → 7,000 مُصحَّحة → POST → الرصيد النهائي = 7,000
بالضبط) — أُشير إليهما صراحة لـBilal بدل إعادة كتابتهما.

**الحكم النهائي (Bilal)**: 3B-1 = **CLOSED ✅**. الانتقال لـ3B-2
(Opening Inventory) مُعتمَد.

gate() PASS — 28/28 regression + 200/200 fuzz (43/43 لملف 3B-1
تحديداً، بلا تغيير منطقي — توثيق فقط هذه الجولة).

## §63 — PHASE3B2_DESIGN_SPEC.md مكتوبة ومُعتمَدة من حيث المبدأ (Bilal) — التصميم مُغلَق، التنفيذ لم يبدأ بعد

Phase 3B-1 مغلقة رسمياً (§62). قبل أي كود لـ3B-2 (المخزون الافتتاحي)،
كتبتُ `PHASE3B2_DESIGN_SPEC.md` تغطي القيود الستة عشر التي حددها
Bilal حرفياً + Acceptance Gate بـ12 مجموعة مطابقة لطلبه.

**اكتشاف مهم أثناء كتابة المواصفة (لا الكود)**: توجد دالة قديمة
`set_item_opening_balance()` بنفس الملف، سابقة لكل مواصفة Phase 3،
**لا تفي بمعظم القيود الستة عشر** — أهمها: فحص التكرار فيها بمفتاح
`item_id` فقط (لا `item_id+warehouse_id` معاً)، وهو **خطأ فعلي** يمنع
بالضبط الحالة المطلوبة (نفس المادة بمستودعين افتتاحيين مختلفين)؛ ولا
تُنشئ أي `JournalEntry` مقابل إطلاقاً (حركة مخزون فقط، بلا أثر
محاسبي). القرار المعتمَد بالمواصفة: استبدالها بالكامل بـ
`post_opening_inventory()` جديدة (نفس فلسفة `post_opening_account_balances()`
حرفياً)، لا توسيعها.

**تأكيد صريح لمبدأ Bilal الأهم ("لا نكرر آلية حساب المخزون")**: راجعت
`_average_cost()`/`get_item_stock_summary()` — تحسبان من
`InventoryMovement` مباشرة دائماً، فحركة افتتاح حقيقية تدخل نفس الجدول
تلتقطها تلقائياً بلا أي تعديل على أي منهما. بند #15 (بيع بعد الافتتاح
يستخدم تكلفة الافتتاح) نتيجة طبيعية للتصميم، لا كود إضافي.

**القرار المعماري الوحيد الذي كان مفتوحاً (عكس الرصيد الافتتاحي) —
حُسِم بالكامل، راجع §64 مباشرة أدناه لنصه الحرفي.** لم يعد سؤالاً
مفتوحاً بأي صيغة.

لا كود تنفيذي بعد — gate() PASS بلا تغيير (28/28 + 200 fuzz، هذه
الجولة توثيق فقط).

## §64 — قرار محسوم نهائياً: عكس رصيد المخزون الافتتاحي (Phase 3B-2، لا كود بعد)

Bilal اعتمد `PHASE3B2_DESIGN_SPEC.md` من حيث المبدأ، بقرار معماري واحد
يحتاج تثبيتاً قبل الكود:

**`reverse_opening_inventory()` تُرفَض صراحة إذا وُجدت حركة بيع لاحقة
منشورة (POSTED) اعتمدت فعلياً على تكلفة الرصيد الافتتاحي** (فحص لكل
(item, warehouse) بمعزل تماماً — بيع من مستودع آخر لنفس المادة لا
يمنع عكس رصيد مستودع مختلف). يُسمح بالعكس فقط حين لا توجد أي حركة
كهذه، ثم يُعاد إنشاء الرصيد بالقيمة الصحيحة.

**السبب (Bilal، أوسع من مجرد الاتساق مع `cancel_invoice()`)**: عكس
حركة الافتتاح بعد استخدامها فعلياً بحساب COGS لبيع لاحق يكسر السلسلة
التاريخية الكاملة `Opening Inventory → Inventory Movement → Average
Cost → COGS → Journal` بأثر رجعي — يخالف "History is never re-priced
retroactively" (§39) تماماً كما لو أُعيد تسعير فاتورة بيع قديمة.

كذلك تأكَّد صراحة: `Warehouse.is_active` عمود موجود فعلاً بالنموذج
الحالي — لا حاجة لعمود جديد أو هجرة لهذا الغرض؛ الهجرة الوحيدة
المطلوبة لـ3B-2 تبقى `opening_inventory_entries` فقط.

باقي بنود المواصفة (استبدال `set_item_opening_balance()` بالكامل،
Idempotency بنطاق دفعة واحدة لكل الشركة، عدم تعديل `_average_cost()`/
`get_item_stock_summary()`، لا آلية حساب تكلفة ثانية) مُعتمَدة كما
كُتبت بلا تعديل.

**لا كود تغيَّر بهذه الجولة** — توثيق فقط في `PHASE3B2_DESIGN_SPEC.md`
و`WORKFLOW.md`، بناءً على طلب Bilal الصريح. `gate()` شُغِّل للتأكيد
فقط (لم يتغيّر، ولا يُعتبَر تغييراً بالسلوك): **28/28 regression +
200/200 fuzz PASS**.

ملاحظة توضيحية بخصوص الـcommit (طلب Bilal توضيحها): الـcommit هنا هو
commit محلي بمستودع Git داخل بيئة العمل الحالية لغرض التوثيق والتتبع
فقط — لا رفع فعلي لـGitHub (نفس القيد المذكور سابقاً بخصوص تعارض مجلد
`alembic/`، لم يُحسَم بعد من طرف Bilal، القرار برفعه يبقى بيده).

**التالي**: التصميم مُغلَق من حيث المبدأ (Bilal). التنفيذ يبدأ فور
تأكيد Bilal أنه راجع النسختين الفعليتين لهذين الملفين (لا وصفهما فقط)
ووجدهما مطابقتين لما أُعلِن هنا.

## §65 — 3 تصحيحات فنية دقيقة على PHASE3B2_DESIGN_SPEC.md (Bilal، لا كود)

مراجعة دقيقة لـBilal على نص المواصفة الملصوق مباشرة بالمحادثة (لا
الملف)، ثلاثة تصحيحات حقيقية — ليست تجميلية:

1. **Acceptance Gate #3 كان يوحي بخطأ محتمل**: الصياغة الأصلية
   ("Opening→Sale→COGS=قيمة الافتتاح دائماً") تتجاهل أن النظام
   Perpetual Weighted Average — لو وُجد شراء بسعر مختلف بين الافتتاح
   والبيع، COGS الصحيح هو المتوسط المرجَّح الجديد، لا قيمة الافتتاح
   وحدها. صُحِّح لاختبارين منفصلين: (أ) بلا أي IN لاحقة → COGS=قيمة
   الافتتاح تماماً، (ب) مع شراء لاحق بسعر مختلف → COGS من المتوسط
   المرجَّح الجديد (مثال محسوب: Opening 100@5 + Purchase 100@10 → sale
   20 → COGS=20×7.5=150، لا 100 ولا سعر الشراء الأخير وحده). الاختبار
   الثاني أقوى لإثبات أن الافتتاح **يندمج** بمحرك المتوسط الموجود، لا
   يُعامَل استثناءً.
2. **Acceptance Gate #11 كان غير صحيح بإطلاقه**: "مجموع قيمة الافتتاح
   = رصيد حساب المخزون بميزان المراجعة" ينهار فوراً لو وُجدت حركات
   شراء/بيع لاحقة على نفس الحساب. صُحِّح لمسارين: تحقق بحالة معزولة
   بالكامل (قبل أي حركة أخرى، الرصيد الإجمالي = قيمة الافتتاح فعلاً)،
   وتحقق منفصل صحيح دائماً بصرف النظر عن أي حركات لاحقة (سطر القيد
   الافتتاحي نفسه بـ`debit_base` يطابق `quantity×unit_cost_base`
   المُدخَلة — مقارنة أثر القيد لا رصيد الحساب الإجمالي).
3. **`reverse_opening_inventory()` كانت تفتقد فحصاً حرجاً**: `source_type`
   المُقترَح أصلاً (`"opening_balance"`) كان **مطابقاً** لنفس القيمة
   المُستخدَمة بقيود الحسابات الافتتاحية (3B-1) — يجعل الدالة عاجزة عن
   التمييز الموثوق بين النوعين لو مُرِّر لها قيد من النوع الخطأ. صُحِّح:
   قيد المخزون الافتتاحي (وحركاته `InventoryMovement` المرتبطة) يحصل
   على `source_type="opening_inventory"` مختلف صراحة عن `"opening_balance"`
   (3B-1) — و`reverse_opening_inventory()` تتحقق من هذا كأول خطوة
   إلزامية، ترفض `OpeningBalanceError` واضحاً غير ذلك.

**كل الثلاثة طُبِّقت فعلياً بالملف** (لا وصفاً فقط هذه المرة — راجع
§66 أدناه بخصوص مشكلة توصيل الملفات نفسها).

لا كود تغيَّر — `gate()` للتأكيد فقط: 28/28 regression + 200/200 fuzz
PASS (بلا تغيير).

## §66 — ملاحظة صريحة: مشكلة توصيل نسخ الملفات لـBilal (منفصلة عن محتوى الكود)

Bilal أبلغ مرتين متتاليتين أن النسخة التي يراها من `PHASE3B2_DESIGN_SPEC.md`
(عبر أداة البحث/الملفات لديه، بما فيها إشارته لـ"version_id: 1") لا
تعكس آخر تعديلاتي، رغم أن مصدر الحقيقة بهذه البيئة (الملف الفعلي +
`git log`) يؤكد أن التعديلات مُطبَّقة فعلاً هنا في كل مرة تحقَّق منها.
هذا التناقض **ليس خطأً بمحتوى الكود أو التوثيق نفسه** — كل تحقق مباشر
بهذه الجولة (`grep`, `git log`, قراءة الملف مباشرة) يؤكد أن النسخة
الصحيحة موجودة. المشكلة تبدو بمسار توصيل الملف لـBilal (تخزين
مؤقت/فهرسة قديمة بأداته)، خارج نطاق التحكم من هذه البيئة مباشرة. تم
إرسال نسخة جديدة من الملفين بشكل صريح ومستقل (لا داخل zip فقط) لتقليل
احتمال هذا الالتباس — Bilal يحتاج التحقق من كونه يفتح أحدث نسخة
مُرسَلة فعلياً، لا نسخة مخزَّنة لديه من رسالة سابقة.

---

## §67 — Phase 3B-2 مُنفَّذة ومُختبَرة

كل ما يلي شُغِّل فعلياً بهذه البيئة (سكربتات حقيقية عبر `subprocess`
وتشغيل مباشر، لا وصف نظري) — لا نتيجة أدناه "متوقَّعة"، كل رقم نتيجة
تشغيل فعلي موثَّق بالمحادثة المقابلة:

**الكود:**
- `OpeningInventoryEntry` أُضيف بـ`app/models.py` (سجل تفصيلي، نفس دور
  `OpeningBalanceEntry` بـ3B-1 — لا يستبدل `JournalEntry`/`InventoryMovement`
  كمصدر حقيقة).
- Migration `c2d6b4a9f1e7_add_opening_inventory_entries.py` — جدول
  `opening_inventory_entries` بالحقول العشرة المطلوبة بالمواصفة، FKs
  إلى `journal_entries`/`items`/`warehouses`/`inventory_movements`.
  لا هجرة أخرى لنفس الغرض.
- `post_opening_inventory()` و`reverse_opening_inventory()` أُنفِّذتا
  بالكامل بـ`app/services/opening_balances.py` — راجَعتُ التنفيذ بنداً
  بنداً مقابل القيود الـ16 بـ`PHASE3B2_DESIGN_SPEC.md` §1: **PASS على
  الـ16 كلها** (#1 warehouse إلزامي — dataclass، لا Optional، لا
  fallback؛ #2 تحقق item/warehouse نشطين؛ #3 quantity>0؛ #4
  unit_cost>=0 مع قبول صريح للصفر؛ #5 تخزين unit_cost_base بالعملة
  الأساسية؛ #6 قيمة السطر = quantity×unit_cost_base بالضبط؛ #7-8
  InventoryMovement حقيقية + source_type="opening_inventory" لكل من
  القيد والحركة معاً، source_id=journal_entry.id؛ #9 لا استدعاء
  لـ_average_cost()/get_item_stock_summary() أثناء الإدخال؛ #10 فحص
  التكرار بمفتاح (item_id,warehouse_id) معاً؛ #11 Idempotency بـSetting
  على مستوى الشركة كاملة؛ #12 لا commit/rollback داخل الخدمة، rollback
  ذرّي كامل مُختبَر؛ #13 لا COGS ولا Revenue — طرفان فقط
  (Inventory/Clearing)؛ #14 نفس Clearing Account المُعتمَد بـ3B-1
  حرفياً (`opening_balance_clearing_account_id`)؛ #15 بيع لاحق يستخدم
  تكلفة الافتتاح تلقائياً عبر محرك المتوسط الموجود بلا كود إضافي؛ #16
  migration + reopen فعلي مُختبَران).
- `set_item_opening_balance()` **حُذفت نهائياً** من `opening_balances.py`
  — لا مسار قديم متبقٍّ. تحقَّق `grep` مباشر: لا استدعاء إنتاجي واحد
  متبقٍّ بأي ملف، فقط ذكرها التاريخي بالتوثيق (هذا الملف،
  `PHASE3B2_DESIGN_SPEC.md`) وبتعليق توضيحي بـ`test_full_inventory_lifecycle.py`.
- الاستدعاءات القديمة الأربعة (بثلاثة ملفات اختبار) نُقلت لـ
  `post_opening_inventory()`:
  - `test_e2e_scenario.py`: أثر محاسبي حقيقي جديد — حقوق الملكية
    ارتفعت فعلياً لـ500,300,000 (رأس المال 500,000,000 + قيمة المخزون
    الافتتاحي 300,000) لأن القيد الافتتاحي أصبح يُنشَأ فعلياً الآن
    (لم يكن يُنشَأ إطلاقاً بالدالة القديمة). أثبتُّه رقمياً بتشغيل
    الاختبار فعلياً، لا افتراضاً.
  - `test_full_inventory_lifecycle.py`: كان يبني `JournalEntry` يدوياً
    بعد الدالة القديمة لتعويض غياب القيد المحاسبي منها — حُذف ذلك الكود
    اليدوي بالكامل لأن `post_opening_inventory()` تفعله بنفسها الآن؛
    الاختبار يفحص المسار الحقيقي post_opening_inventory→JournalEntry→
    InventoryMovement→Average Cost→Sale→COGS عبر Oracle مستقل
    (`InventoryLedgerOracle`) لا يستدعي أي كود إنتاجي لحساب القيمة
    المتوقَّعة.
  - `test_accounting_edge_cases.py`: استدعاءان — الأول (حالة #2،
    اختبار تحديث المتوسط المرجَّح) نُقل مباشرة؛ الثاني كان بلا أثر فعلي
    على نتيجة الاختبار أصلاً (ملفوف بـ`try/except` عام، لا شيء لاحقاً
    يتحقق من أثره) — وُثِّق ذلك صراحة بدل حذفه بصمت.

**unit_cost=0** (نقطة حساسة راجعها Bilal مرتين): مُختبَر بعمق — دفعة
بسطرين (أحدهما تكلفته صفر، الآخر لا)، أُثبت أن القيد بقي متوازناً، أن
عدد أسطر القيد = 2 بالضبط (Dr المادة غير الصفرية + Cr Clearing — لا
سطر ثالث مخفي بقيمة صفر للمادة الأخرى)، أن `InventoryMovement` للمادة
صفرية التكلفة موجودة فعلاً بكمية صحيحة، وأن `opening_inventory_entries`
يحتفظ بسجلها التفصيلي رغم غياب أي أثر بدفتر الأستاذ.

**Oracle مستقل**: كل قيمة متوقَّعة بالاختبار محسوبة يدوياً
(`quantity × unit_cost_base` مباشرة بقيم Decimal حرفية، أو
`InventoryLedgerOracle` بـ`test_full_inventory_lifecycle.py`) — لا
استدعاء لـ`post_opening_inventory()`/`_average_cost()`/
`get_item_stock_summary()` لحساب أي قيمة متوقَّعة بأي اختبار.

**Warehouse isolation**: مادة واحدة بمستودعين مختلفين، تكلفتان مختلفتان
تماماً (5 مقابل 20)، بيع من كل منهما يستخدم متوسط مستودعه فقط —
COGS منفصلان بالضبط، بلا تداخل.

**Weighted Average بعد Opening+Purchase**: Opening 100@5 → Purchase
100@10 → المتوسط = (100×5+100×10)/200 = 7.5 بالضبط → بيع 20 →
COGS=150 بالضبط (Oracle مستقل يحسب هذا الرقم مباشرة). يثبت أن الافتتاح
يدخل فعلياً بمحرك المتوسط المرجَّح الموجود، لا معاملة استثنائية.

**rollback**: دفعة بثلاثة أسطر، السطر الثاني بمادة غير موجودة → فشل
ذرّي كامل → صفر `InventoryMovement`/`JournalEntry`/`opening_inventory_entries`
بعد `session.rollback()` من المُستدعي، لا `Setting` قفل خطأً، إعادة
المحاولة الصحيحة تنجح نظيفة.

**idempotency**: محاولة ثانية للدفعة (حتى بمادة جديدة كلياً لم تُذكَر
بالأولى) تُرفَض، والكمية لا تتضاعف.

**reopen**: إغلاق فعلي (`session.close()` + `engine.dispose()`) وفتح
اتصال جديد تماماً بنفس ملف قاعدة البيانات — القيد، الحركة، ميزان
المراجعة، واستعلام المخزون كلها سليمة بعد إعادة الفتح.

**GL ↔ Inventory reconciliation**: اختباران منفصلان (تصحيح Bilal §65
نقطة #1) — معزول (قبل أي حركة أخرى: رصيد حساب المخزون بميزان المراجعة
= قيمة الافتتاح بالضبط) وأثر القيد نفسه (`debit_base == quantity ×
unit_cost_base` لكل سطر، صحيح دائماً بصرف النظر عن حركات لاحقة).

**reverse — الحالات الثلاث المطلوبة صراحة**:
1. Opening فقط بلا بيع لاحق → reverse ينجح، الكمية تعود لصفر، القفل
   يُزال، إعادة الإدخال تنجح.
2. Opening + بيع POSTED لاحق من نفس (item, warehouse) اعتمد على تكلفة
   الافتتاح → reverse يُرفَض صراحة بـ`OpeningBalanceError`.
3. Opening بمستودع A فقط (لا B بنفس الدفعة) + بيع لاحق بمستودع B منفصل
   تماماً (رصيده من مصدر آخر لا علاقة له) → reverse لـA ينجح، B لا يتأثر
   إطلاقاً. مُختبَر صراحة كحالة مستقلة (§4، Case 3).
   إضافياً: قيد بمستودعين A/B بنفس الدفعة، بيع لاحق من A فقط → عكس
   القيد **كاملاً** يُرفَض (القيد وحدة محاسبية واحدة، لا عكس جزئي لسطر
   دون آخر بنفس المستند).
   وأيضاً: `reverse_opening_inventory()` ترفض صراحة قيداً من نوع
   `source_type="opening_balance"` (قيد حسابات 3B-1) — لا تخلط النوعين
   إطلاقاً، يطابق تصحيح §65 نقطة #3.

**عدم خلط 3B-1/3B-2**: تحقَّق `grep` مباشر — `source_type="opening_balance"`
لا يزال يُستخدَم حصراً بمسارات 3B-1 (`post_opening_account_balances`
واختباراتها + القيود اليدوية المشابهة بملفات الاختبار)، و
`source_type="opening_inventory"` يظهر حصراً بمسار 3B-2. لا تداخل.

**النتائج النهائية المُشغَّلة فعلياً بهذه البيئة:**
- `tests/test_opening_inventory.py`: **54/54 PASS**
- `python tests/run_gate.py` (مع `QT_QPA_PLATFORM=offscreen`):
  **29/29 Regression PASS + 200/200 Fuzz PASS + gate()==True**
- `python -m pytest tests/ -q`: **PASS** (بعد إزالة `sys.exit()` غير
  الضروري بنهاية `test_opening_inventory.py` الذي كان يكسر آلية
  الاستيراد الداخلية لـpytest تحديداً — لم يكن يؤثر على `run_gate.py`
  لأنه يشغّل الملفات كسكربتات منفصلة عبر `subprocess`، لكنه كان يمنع
  تشغيل الحزمة كاملة بـpytest مباشرة).

**تنبيه صريح غير مُخفى**: بيئة العمل هذه بلا `.git` (النسخة المرفوعة
zip لا تتضمن مجلد `.git`)، فلم يكن ممكناً تشغيل `git status`/`git diff`
فعلياً كما طُلب — بدلاً من ذلك استُخدم `diff -rq` مباشر بين نسخة الرفع
الأصلية والنسخة المعدَّلة، وأكَّد أن التغييرات محصورة تماماً بالملفات
المتوقَّعة (`app/models.py`، `app/services/opening_balances.py`،
`tests/run_gate.py`، وثلاثة ملفات اختبار قديمة، بالإضافة لملف الاختبار
والهجرة الجديدين) — لا أي تغيير جانبي غير مقصود بأي ملف آخر.
